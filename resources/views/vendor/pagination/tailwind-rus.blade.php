{{--
    Русскоязычная пагинация — оставлена как псевдоним.

    Раньше это была отдельная копия разметки, и она разъезжалась с основной:
    правку оформления делали в tailwind.blade.php, а кабинет (список заказов)
    звал именно этот файл и оставался со старым видом. Теперь тут только
    подключение основной вьюхи — источник правды один.
--}}
@include('pagination::tailwind', ['paginator' => $paginator, 'elements' => $elements] + (isset($summary) ? ['summary' => $summary] : []))
