<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Вложения в заявках
    |--------------------------------------------------------------------------
    |
    | Список разрешённых расширений — БЕЛЫЙ, а не чёрный: форму заполняет кто
    | угодно из интернета, и перечислять то, что запрещено, здесь опаснее, чем
    | перечислить то, что можно. Исполняемых типов и SVG в списке нет намеренно
    | (SVG — это документ со скриптом внутри).
    |
    */
    'upload_extensions' => \Modules\Forms\Services\FormService::DEFAULT_UPLOAD_EXTENSIONS,

    /*
    |--------------------------------------------------------------------------
    | Диск для вложений
    |--------------------------------------------------------------------------
    |
    | НЕ public: заявка может содержать паспорт, договор или медицинскую
    | справку, а всё, что лежит в storage/app/public, доступно по прямой ссылке
    | без авторизации (см. CLAUDE.md про медиатеку). Скачивание идёт отдельным
    | маршрутом под auth+admin.
    |
    */
    'upload_disk' => 'local',
    'upload_dir'  => 'form-uploads',

    /*
    |--------------------------------------------------------------------------
    | Ограничение частоты отправки
    |--------------------------------------------------------------------------
    |
    | На один IP. Защищает не столько от спама (для него есть ловушка и
    | каптча), сколько от заваливания базы и почты автоматикой.
    |
    */
    'throttle' => [
        'attempts' => 5,
        'minutes'  => 10,
    ],

    /*
    |--------------------------------------------------------------------------
    | Хранение заявок
    |--------------------------------------------------------------------------
    |
    | Ноль — хранить бессрочно. Иначе команда forms:prune удалит заявки старше
    | указанного числа дней: персональные данные не должны лежать вечно просто
    | потому, что никто не удалил.
    |
    */
    'keep_days' => 0,
];
