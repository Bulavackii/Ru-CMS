<?php

namespace Modules\Menu\Console\Commands;

use Illuminate\Console\Command;
use Illuminate\Support\Facades\DB;
use Modules\Menu\Models\Page;

/**
 * Первичное наполнение раздела «Страницы» демо-контентом.
 *
 * Вызывается мастером установки (InstallController::finish → self::seed(false)),
 * поэтому сразу после установки в CMS есть несколько содержательных страниц —
 * их видно и в админке, и на сайте по адресу /page/{slug}.
 *
 *   php artisan pages:seed-default            # дозаполнить недостающее (идемпотентно)
 *   php artisan pages:seed-default --reset    # перезаписать контент дефолтных страниц
 *
 * Заметка про slug: статические маршруты /about, /faq, /contacts и т.п. заняты
 * собственными Blade-страницами (routes/web.php), поэтому у демо-страниц свои
 * адреса — конфликта нет.
 */
class SeedDefaultPagesCommand extends Command
{
    protected $signature = 'pages:seed-default {--reset : Перезаписать контент дефолтных страниц}';

    protected $description = 'Наполнение раздела «Страницы» демо-контентом (о проекте, возможности, вопросы)';

    /** Канонический набор демо-страниц. */
    public static function definitions(): array
    {
        return [
            [
                'title' => 'О проекте',
                'slug'  => 'o-proekte',
                'meta_title' => 'О проекте — RU CMS',
                'meta_description' => 'RU CMS — модульная система управления сайтом на Laravel: новости, страницы, меню, categories и SEO из коробки.',
                'meta_keywords' => 'cms, laravel, модульная cms, управление сайтом',
                'homepage_order' => 1,
                'content' => <<<'HTML'
<p><strong>RU CMS</strong> — модульная система управления сайтом на Laravel. Она собрана так,
чтобы typical-задачи контент-менеджера решались без разработчика: добавить новость,
собрать меню, опубликовать страницу, настроить SEO-поля.</p>

<h2>Что внутри</h2>
<ul>
  <li><strong>Модульность.</strong> Каждый раздел — отдельный модуль: новости, страницы, меню,
      категории, слайдшоу, поиск, локализация. Ненужное отключается в один клик.</li>
  <li><strong>Контент без кода.</strong> Визуальный редактор, загрузка изображений и видео,
      черновики и публикация по готовности.</li>
  <li><strong>Навигация.</strong> Меню для шапки, подвала и боковой панели с вложенностью
      до трёх уровней и перетаскиванием пунктов мышью.</li>
  <li><strong>SEO из коробки.</strong> Meta-теги у каждой записи, карта сайта, человекопонятные адреса.</li>
</ul>

<h2>Технологии</h2>
<p>Laravel, PostgreSQL и Tailwind CSS. Интерфейс администратора адаптивный — им можно
пользоваться с планшета и телефона.</p>

<blockquote>
  <p>Эта страница создана автоматически при установке. Отредактируйте её в разделе
  <em>Страницы</em> или удалите, если она не нужна.</p>
</blockquote>
HTML,
            ],
            [
                'title' => 'Возможности',
                'slug'  => 'vozmozhnosti',
                'meta_title' => 'Возможности RU CMS',
                'meta_description' => 'Обзор возможностей RU CMS: публикации и товары, меню и навигация, категории, медиатека, поиск, локализация и SEO.',
                'meta_keywords' => 'возможности cms, функции, модули, seo, каталог товаров',
                'homepage_order' => 2,
                'content' => <<<'HTML'
<p>Ниже — краткий обзор того, что доступно сразу после установки. Все разделы находятся
в панели управления в меню слева.</p>

<h2>Контент</h2>
<ul>
  <li><strong>Новости и публикации</strong> — с обложками, категориями и SEO-полями.</li>
  <li><strong>Страницы</strong> — статические материалы: о компании, условия, контакты.</li>
  <li><strong>Товары</strong> — шаблон с ценой, остатком и корзиной, если нужен небольшой каталог.</li>
  <li><strong>Слайдшоу</strong> — баннеры вверху или внизу главной страницы.</li>
</ul>

<h2>Навигация и структура</h2>
<ul>
  <li>Меню для шапки, подвала и боковой панели — сколько угодно, каждое настраивается отдельно.</li>
  <li>Вложенность до трёх уровней, порядок пунктов — перетаскиванием.</li>
  <li>Категории для группировки материалов и фильтрации.</li>
</ul>

<h2>Работа с сайтом</h2>
<table>
  <thead>
    <tr><th>Задача</th><th>Где решается</th></tr>
  </thead>
  <tbody>
    <tr><td>Добавить статью или товар</td><td>Новости → Создать</td></tr>
    <tr><td>Изменить пункты меню</td><td>Меню → Изменить</td></tr>
    <tr><td>Загрузить изображения</td><td>Файлы</td></tr>
    <tr><td>Настроить мета-теги</td><td>SEO, либо поля в самой записи</td></tr>
    <tr><td>Сменить оформление</td><td>Темы</td></tr>
  </tbody>
</table>

<h2>Дополнительно</h2>
<p>Поиск по сайту, локализация интерфейса на несколько языков, уведомления,
импорт и экспорт материалов, специальные возможности для посетителей.</p>
HTML,
            ],
            [
                'title' => 'Частые вопросы',
                'slug'  => 'chastye-voprosy',
                'meta_title' => 'Частые вопросы — RU CMS',
                'meta_description' => 'Ответы на частые вопросы по работе с RU CMS: публикация материалов, меню, изображения, SEO и резервные копии.',
                'meta_keywords' => 'faq, вопросы и ответы, помощь, инструкция',
                'homepage_order' => 3,
                'content' => <<<'HTML'
<p>Короткие ответы на вопросы, которые чаще всего возникают в начале работы.</p>

<h2>Как опубликовать материал?</h2>
<p>Откройте <em>Новости → Создать</em>, заполните заголовок и содержимое, отметьте
«Опубликовать сразу» и сохраните. Черновик без этой отметки виден только в админке.</p>

<h2>Почему меню не отображается на сайте?</h2>
<p>Меню показывается, если оно <strong>активно</strong> и содержит хотя бы один пункт.
Проверьте это в разделе <em>Меню</em>. Там же выбирается позиция: шапка, подвал или боковая панель.</p>

<h2>Как добавить изображение в текст?</h2>
<p>В редакторе нажмите кнопку вставки изображения — файл загрузится на сервер автоматически.
Все загруженные файлы доступны в разделе <em>Файлы</em>.</p>

<h2>Что писать в SEO-полях?</h2>
<ul>
  <li><strong>Meta Title</strong> — до 60 символов, кратко и по делу.</li>
  <li><strong>Meta Description</strong> — до 160 символов, это текст сниппета в поиске.</li>
  <li><strong>Ключевые слова</strong> — несколько фраз через запятую.</li>
</ul>

<h2>Можно ли изменить адрес страницы?</h2>
<p>Да, поле «Slug» задаёт адрес вида <code>/page/vash-adres</code>. Учтите: после смены адреса
старые ссылки перестанут работать, а поисковикам потребуется время на переиндексацию.</p>

<h2>Как сменить внешний вид?</h2>
<p>Раздел <em>Темы</em> — цвета, шрифты, логотип и фоновая текстура. Изменения применяются
ко всему сайту сразу.</p>
HTML,
            ],
        ];
    }

    /**
     * Создаёт демо-страницы. Идемпотентно: страница переиспользуется по slug.
     * $reset=true — перезаписывает контент и SEO-поля дефолтных страниц
     * (правки владельца в других страницах не трогаются никогда).
     */
    public static function seed(bool $reset = false): void
    {
        DB::transaction(function () use ($reset) {
            foreach (self::definitions() as $def) {
                $attributes = [
                    'title'            => $def['title'],
                    'content'          => $def['content'],
                    'meta_title'       => $def['meta_title'],
                    'meta_description' => $def['meta_description'],
                    'meta_keywords'    => $def['meta_keywords'],
                    'published'        => true,
                    // Показываем на главной: сразу после установки видно, что раздел
                    // «Страницы» работает и как выглядит вывод страниц на главной.
                    // Порядок задаётся homepage_order.
                    'show_on_homepage' => true,
                    'homepage_order'   => $def['homepage_order'],
                ];

                $page = Page::where('slug', $def['slug'])->first();

                if (! $page) {
                    Page::create($attributes + ['slug' => $def['slug']]);
                    continue;
                }

                if ($reset) {
                    $page->update($attributes);
                }
            }
        });
    }

    public function handle(): int
    {
        $reset = (bool) $this->option('reset');
        self::seed($reset);

        $slugs = collect(self::definitions())->pluck('slug')->implode(', ');
        $this->info(($reset ? 'Демо-страницы перезаписаны' : 'Демо-страницы проверены/созданы')
            . " ({$slugs}).");

        return self::SUCCESS;
    }
}
