<?php

return [

    /*
    |--------------------------------------------------------------------------
    | 📛 Название приложения
    |--------------------------------------------------------------------------
    |
    | Это имя вашего приложения. Оно может отображаться в уведомлениях,
    | шаблонах писем, заголовках страниц и других элементах интерфейса.
    |
    */
    'name' => env('APP_NAME', 'Laravel'),

    /*
    |--------------------------------------------------------------------------
    | 🔢 Версия приложения
    |--------------------------------------------------------------------------
    |
    | Показывается в подвале админки и используется UpdateService при проверке
    | обновлений. Раньше нигде не была объявлена — оба потребителя молча
    | использовали дефолт '1.0.0'. Задайте APP_VERSION в .env, если версия
    | должна отличаться.
    |
    */
    'version' => env('APP_VERSION', '1.0.0'),

    /*
    |--------------------------------------------------------------------------
    | 🔗 Страницы проекта в сетях
    |--------------------------------------------------------------------------
    |
    | Показывают подвал сайта и подвал панели через хелпер social_links().
    | Раньше список был записан прямо в двух шаблонах, и адреса уже разошлись
    | между ними. Пустое значение убирает значок из ряда — ссылки в никуда
    | быть не должно.
    |
    */
    'social' => [
        'vk'     => env('SOCIAL_VK', 'https://vk.com/ru_cms'),
        'max'    => env('SOCIAL_MAX', 'https://max.ru'),
        'rutube' => env('SOCIAL_RUTUBE', 'https://rutube.ru'),
        'github' => env('SOCIAL_GITHUB', 'https://github.com/Bulavackii/Ru-CMS'),
    ],

    /*
    |--------------------------------------------------------------------------
    | 🌍 Среда окружения приложения
    |--------------------------------------------------------------------------
    |
    | Определяет окружение, в котором сейчас работает приложение.
    | Например: local, staging, production, testing.
    | Используется для изменения поведения сервисов.
    |
    */
    'env' => env('APP_ENV', 'production'),

    /*
    |--------------------------------------------------------------------------
    | 🛠️ Режим отладки
    |--------------------------------------------------------------------------
    |
    | Включает отображение подробных ошибок и стеков вызова при сбоях.
    | ⚠️ Никогда не включайте в production-среде!
    |
    */
    'debug' => (bool) env('APP_DEBUG', false),

    /*
    |--------------------------------------------------------------------------
    | 🌐 Базовый URL приложения
    |--------------------------------------------------------------------------
    |
    | Используется в Artisan-командах, генерации ссылок, уведомлениях, письмах.
    | Рекомендуется задать полный домен в `.env` (например: https://example.com)
    |
    */
    'url' => env('APP_URL', 'http://localhost'),

    /*
    |--------------------------------------------------------------------------
    | 🕒 Часовой пояс по умолчанию
    |--------------------------------------------------------------------------
    |
    | Указывается системный часовой пояс, который будет использоваться
    | при работе с датами и временем (Carbon, date() и пр.).
    | Пример: 'Europe/Moscow'
    |
    */
    'timezone' => env('APP_TIMEZONE', 'Europe/Moscow'),

    /*
    |--------------------------------------------------------------------------
    | 🌍 Язык интерфейса приложения
    |--------------------------------------------------------------------------
    |
    | Основной язык интерфейса. Используется для переводов сообщений и текстов.
    | Может быть переопределён пользователем или на уровне маршрута.
    |
    */
    'locale' => env('APP_LOCALE', 'ru'),

    /*
    |--------------------------------------------------------------------------
    | ✍️ Язык оригинала контента
    |--------------------------------------------------------------------------
    |
    | На каком языке заполняются сами записи (новости, страницы, меню). Для
    | него переводы не хранятся: он и есть оригинал, к которому откатываемся,
    | когда перевода нет. Отдельный ключ нужен потому, что 'locale' выше
    | меняется на лету при app()->setLocale() — использовать его как «язык
    | оригинала» нельзя, иначе оригиналом окажется текущий язык посетителя.
    |
    */
    'content_locale' => env('APP_CONTENT_LOCALE', 'ru'),

    /*
    |--------------------------------------------------------------------------
    | 🔄 Резервный язык (на случай отсутствия перевода)
    |--------------------------------------------------------------------------
    |
    | Если перевод на текущем языке отсутствует — будет использован fallback.
    | Обычно 'en' — английский.
    |
    */
    'fallback_locale' => env('APP_FALLBACK_LOCALE', 'en'),

    /*
    |--------------------------------------------------------------------------
    | 🤖 Язык Faker для генерации фейковых данных
    |--------------------------------------------------------------------------
    |
    | Используется при генерации данных сидерами (factory) и тестами.
    | Например: 'ru_RU', 'en_US', 'de_DE'
    |
    */
    'faker_locale' => env('APP_FAKER_LOCALE', 'en_US'),

    /*
    |--------------------------------------------------------------------------
    | 🔐 Ключ шифрования
    |--------------------------------------------------------------------------
    |
    | Используется Laravel для шифрования данных (например, куки).
    | Ключ должен быть длиной 32 символа. Генерируется через:
    | php artisan key:generate
    |
    */
    'key' => env('APP_KEY'), // Автоматически генерируется в bootstrap/app.php, если отсутствует

    /*
    |--------------------------------------------------------------------------
    | 🔑 Предыдущие ключи шифрования (Laravel 12+)
    |--------------------------------------------------------------------------
    |
    | Позволяют расшифровать старые значения, зашифрованные ранее
    | другими ключами. Полезно при ротации ключей.
    |
    */
    'previous_keys' => [
        ...array_filter(
            explode(',', env('APP_PREVIOUS_KEYS', ''))
        ),
    ],

    /*
    |--------------------------------------------------------------------------
    | 🔐 Алгоритм шифрования
    |--------------------------------------------------------------------------
    |
    | Используемый алгоритм при шифровании значений. По умолчанию:
    | AES-256-CBC — надёжный симметричный алгоритм.
    |
    */
    'cipher' => 'AES-256-CBC',

    /*
    |--------------------------------------------------------------------------
    | 🧰 Драйвер режима обслуживания (maintenance mode)
    |--------------------------------------------------------------------------
    |
    | Управляет тем, как приложение "уходит на обслуживание".
    | Режим maintenance можно активировать через:
    | php artisan down
    |
    | Поддерживаемые драйверы: "file", "cache"
    | "cache" — позволяет управлять режимом на нескольких серверах.
    |
    */
    'maintenance' => [
        'driver' => env('APP_MAINTENANCE_DRIVER', 'file'),
        'store'  => env('APP_MAINTENANCE_STORE', 'database'),
    ],

];
