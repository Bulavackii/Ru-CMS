<?php

return [

    /*
    |--------------------------------------------------------------------------
    | 🚫 Запрещённые к загрузке типы
    |--------------------------------------------------------------------------
    |
    | Медиатека принимает файлы ЛЮБОГО типа, кроме перечисленных здесь. Раньше
    | было наоборот — узкий белый список из двух десятков расширений, — и любой
    | шрифт, субтитры, чертёж или архив приходилось переименовывать или класть
    | на сервер руками.
    |
    | Но «любой» не означает «совсем любой», и вот почему. Файлы лежат в
    | storage/app/public и доступны по прямой ссылке БЕЗ авторизации (так и
    | задумано — это картинки сайта). Значит:
    |
    |   — .php и родня исполнятся на сервере через симлинк public/storage.
    |     Это не «риск», а прямой захват сайта любым, кто может загрузить файл.
    |   — .html, .svg, .xhtml откроются в браузере КАК СТРАНИЦЫ вашего домена,
    |     то есть скрипт внутри получит доступ к сессии администратора.
    |   — .exe и прочие исполняемые — не опасны для сервера, но домен станет
    |     раздавать их посетителям от своего имени.
    |
    | Если какой-то тип нужен, уберите его отсюда осознанно и закройте раздачу
    | на уровне веб-сервера. Для nginx правило описано в CLAUDE.md, для Apache
    | мастер установки кладёт .htaccess в storage/app/public сам.
    |
    */
    'blocked_extensions' => [
        // Исполняется на сервере
        'php', 'php3', 'php4', 'php5', 'php7', 'php8', 'phtml', 'phps', 'phar',
        'cgi', 'pl', 'py', 'rb', 'sh', 'bash', 'zsh', 'jsp', 'asp', 'aspx',
        // Исполняется у посетителя от имени домена
        'html', 'htm', 'xhtml', 'shtml', 'mhtml', 'svg', 'svgz', 'swf', 'js', 'mjs',
        // Настройки веб-сервера
        'htaccess', 'htpasswd', 'user.ini',
        // Исполняемые файлы систем
        'exe', 'msi', 'com', 'bat', 'cmd', 'scr', 'vbs', 'ps1', 'jar', 'dll', 'so', 'app',
    ],

    /*
    |--------------------------------------------------------------------------
    | 📦 Наибольший размер файла, КБ
    |--------------------------------------------------------------------------
    |
    | Было 10 МБ — мало для видео и звука, ради которых библиотеку и открывали.
    | Реальный предел всё равно не выше того, что разрешает PHP
    | (upload_max_filesize и post_max_size): хелпер max_upload_kb() берёт
    | наименьшее из трёх, поэтому поднять только это число недостаточно —
    | смотрите php.ini.
    |
    */
    'max_size_kb' => (int) env('FILES_MAX_SIZE_KB', 262144),

    /*
    |--------------------------------------------------------------------------
    | 🎞️ Что считать медиа
    |--------------------------------------------------------------------------
    |
    | По этим спискам редактор решает, чем вставить файл: картинкой, проигрывателем
    | звука, проигрывателем видео или карточкой для скачивания.
    |
    */
    'video_extensions' => ['mp4', 'webm', 'ogv', 'mov', 'm4v'],
    'audio_extensions' => ['mp3', 'ogg', 'oga', 'wav', 'weba', 'm4a', 'flac', 'aac'],

];
