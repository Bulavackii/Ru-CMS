<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Файл-замок установки
    |--------------------------------------------------------------------------
    |
    | Его наличие означает «система установлена»: по нему middleware решают,
    | пускать ли на мастер и уводить ли посетителя на /install.
    |
    | Путь настраиваемый НЕ ради гибкости на бою, а ради тестов. Раньше они
    | требовали настоящий storage/install.lock, и прогон в момент, когда
    | владелец проходит мастер, заставлял создавать этот файл в его рабочем
    | каталоге — установка сбрасывалась на середине. Теперь phpunit.xml
    | указывает сюда свой путь, и боевой замок никто не трогает.
    |
    | Значение — путь ОТНОСИТЕЛЬНО storage/.
    |
    */

    'lock' => env('INSTALL_LOCK_PATH', 'install.lock'),

];
