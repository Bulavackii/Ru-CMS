# RU CMS — рабочая памятка для Claude

> **Этот файл — точка входа в сессию.** Прочитай его целиком до первой правки кода.
> Здесь: как работать, чего не делать, что уже сделано и на чём остановились.
> Держи файл актуальным — дописывай сюда итоги каждой сессии.

**Что за проект:** Laravel 12, модульная HMVC-CMS («RuShop CMS» / «RU CMS»).
Репозиторий: `Bulavackii/Ru-CMS` (публичный, GitHub), remote `origin` настроен.

---

## 🔴 Железные правила

Нарушать нельзя — это прямые указания владельца проекта.

| Правило | Подробности |
|---|---|
| **Коммиты только по-русски** | Заголовок и тело коммита пишутся на русском. Трейлер `Co-Authored-By:` — фиксированный формат, его не переводим. |
| **Общение и тексты — по-русски** | Ответы пользователю, комментарии в коде, строки интерфейса, сообщения об ошибках. |
| **Пуш напрямую в `master`** | PR-флоу не настроен. Коммит → `git push origin master`. |
| **БД — только PostgreSQL** | Осознанный выбор («опенсорсная БД»). В мастере установки нет выбора MySQL/SQLite. Конфиги других драйверов в `config/database.php` остались — они безвредны. Ничего в приложении не должно опираться на MySQL-специфичный синтаксис. Тесты при этом гоняются на SQLite (`phpunit.xml`) ради скорости — отдельное осознанное решение, поэтому миграции обязаны быть драйвер-нейтральными (Schema Builder, а не сырой SQL). |
| **Миграции — только в `database/migrations/`** | НЕ по модулям. Подробности ниже. |

---

## ⚡ С чего начать сессию

```bash
git log --oneline -5                    # что было в прошлый раз
git status --short                      # незакоммиченное
php vendor/bin/phpunit --no-coverage    # базовая линия: должно быть 171/171
```

Если падает много тестов и в диффах мелькает редирект на `/install` —
**не ищи регрессию**, скорее всего просто пропал `storage/install.lock`
(см. «Особенности песочницы»).

---

## 🗺️ Архитектура: где что лежит

- **Модули:** `modules/<Name>/` — `Controllers/`, `Models/`, `Providers/`, `Views/`, `Routes/`.
  Подключаются через `app/Providers/ModuleServiceProvider.php`.
- **Миграции:** все до единой в `database/migrations/` — и ядра, и модулей.
  Раньше были раскиданы по 14 папкам внутри модулей; всё консолидировано.
  **Миграция, положенная в `modules/<Name>/Migrations/`, молча никогда не выполнится.**
  Ни `ModuleServiceProvider`, ни модульные провайдеры больше не зовут `loadMigrationsFrom()`.
- **Переводы:** `resources/lang/<locale>/<group>.php` (см. отдельный раздел).
- **Лейаут админки:** `resources/views/layouts/admin.blade.php` — **Tailwind + Font Awesome + Alpine**.
  Bootstrap в проекте **нет**. Доступны `@stack('scripts')` и (с 22.07.2026) `@stack('styles')` —
  вьюха с собственным точечным CSS подключает его через `@push('styles')...@endpush`
  (пример: `admin/dashboard/index.blade.php`).
  Плавающей кнопки быстрых действий («+» внизу справа) больше нет — убрана вместе с
  компонентом `components/admin/quick-actions.blade.php` (было почти дублирование:
  такие же быстрые действия уже показывает шапка дашборда).
  Новые вьюхи пиши на Tailwind.
  (Прежнее предупреждение про Bootstrap-вёрстку вьюх модуля Localization снято
  28.07.2026 — проверено грепом: `index/create/edit/settings` уже переписаны на
  Tailwind + Alpine, импорт-модал открывается через `x-show`, а не `data-bs-toggle`.)
- **`System::Views.admin.modules` — не лейаут и никогда им не был.** Это конкретная
  страница «Модули» (`modules/System/Views/admin/modules.blade.php`, namespace `System`
  указывает на `modules/System/Views`, так что даже корректная ссылка на неё —
  `System::admin.modules`, без лишнего `Views.`). Четыре вьюхи Localization расширяли
  именно эту несуществующую комбинацию (`System::Views.admin.modules`) и падали с 500
  на каждый заход — почищено 22.07.2026, см. «Архитектурные грабли». Для новых страниц
  наследуйся напрямую от `layouts.admin`.

---

## 🌍 Локализация (ключевая подсистема)

**Языки интерфейса: РОВНО ДВА — `ru` (эталон) и `en`.**
`be`, `kk`, `de`, `fr`, `it` удалены вместе со сведением мастера установки к
двум флагам. Каталог `resources/lang` содержит только `ru/` и `en/` — проверять
`ls resources/lang`, а не верить записям ниже по файлу.

⚠️ **Числа вида «× 7 локалей» в журнале траншей ниже — археология.** Они были
верны на момент записи и оставлены как история: по ним видно, что и когда
переводилось. Сегодняшнее число файлов на ключ — два. Добавляешь ключ —
добавляй в `ru` и `en`, паритет сверяется так:

```bash
php -r '$r=array_keys((include "resources/lang/ru/admin.php")["files"]); $e=array_keys((include "resources/lang/en/admin.php")["files"]); var_dump(array_merge(array_diff($r,$e),array_diff($e,$r)));'
```

- `config/app.php`: `locale` = `ru`, **`fallback_locale` = `en`**.
  Отсутствующий ключ уезжает в английский, а не в русский — помни об этом.
- `app()->langPath()` → `resources/lang` (каталога `lang/` в корне нет).
- **`resources/lang/ru/install.php` — эталонный словарь.** Структура ключей задаётся там,
  английский обязан повторять её один-в-один (сейчас по 245 ключей).
  Добавляешь ключ — добавляй сразу в оба файла.
- Часть строк содержит разметку (`<span class="font-mono">`, `<strong>`, `<a href=":url">`)
  и выводится через `{!! __() !!}`. Это осознанно и безопасно: файлы пишет разработчик,
  пользовательских данных там нет. Ссылки получают адрес параметром `:url`.
- Строки для Alpine-биндингов прокидываются через `@js(__('...'))`, например
  `x-text="submitting ? @js(__('install.database.submitting')) : @js(...)"`.

### Графический редактор переводов

`/admin/localization/translations` — правка словарей прямо из админки.

- `modules/Localization/Services/TranslationFileService.php` — чтение/запись языковых
  файлов, dot-нотация, статистика, создание и удаление локалей.
- `modules/Localization/Controllers/Admin/TranslationController.php` — контроллер.
- `modules/Localization/Views/admin/translations/{index,edit}.blade.php` — Tailwind + Alpine.
- Тесты: `tests/Unit/Services/TranslationFileServiceTest.php` (15 шт.).

Что важно про него знать:
- Пишет **в те же файлы**, что и разработчик руками. Отдельного хранилища в БД нет —
  двух источников правды не возникает, переводы версионируются в git.
- **Шапка файла сохраняется** при перезаписи: всё до `return` (включая `<?php` и
  комментарии-пояснения) переносится в новый файл.
- Перед записью создаётся `.bak`, запись атомарная (tmp + rename).
- Пустое поле = ключ удаляется из файла (текст уедет в `fallback_locale`), а не пишется
  пустая строка.
- Прогресс считается по ключам эталона. «Не переведено» = ключ отсутствует **либо**
  значение дословно совпадает с эталонным. Это эвристика: «Email» законно одинаков
  в русском и английском, поэтому проценты — ориентир, а не точная метрика.
- Коды локалей и имена групп валидируются регуляркой — защита от выхода за пределы
  `resources/lang` через `../`. Эталон (`ru`) и fallback (`en`) защищены от удаления.

---

## 🧙 Мастер установки (`modules/Install/`)

**Порядок шагов:** welcome → requirements → database → admin → **smtp** → license → demo → finish.
(`features` — необязательная страница-«отступление», в счётчике шагов не участвует.)

- Порядок форсируется на сервере флагами сессии `install.completed.<step>` —
  перепрыгнуть вперёд по прямому URL нельзя (`STEP_PREREQUISITES` + `guardStep()`).
- Индикатор шагов — общий партиал `Install::partials.steps`, а не копипаста по страницам.
- **Шаг SMTP необязательный:** есть кнопка «Пропустить», плашка «Необязательный шаг»
  у заголовка, поясняющая сноска и точка-маркер в чипе шага. Без почты CMS ставится
  и работает — теряется только восстановление пароля по e-mail.
- Первый шаг = выбор языка (4 флага, инлайн-SVG: Windows вообще не рисует эмодзи флагов,
  их нет в Segoe UI Emoji). Выбор кладётся в `session('install_locale')`,
  `LocalizationMiddleware` применяет его ко всем последующим шагам, не трогая БД
  (её может ещё не быть).
- На шаге admin запоминается `install_admin_id`; на finish админ логинится
  автоматически и попадает сразу в `/admin`.
- Дизайн: светлое «стекло», у каждого шага свой акцентный цвет через
  `@section('accent', '#hex')` → CSS-переменная `--accent`. Тултипы свои, через
  `data-tip`, а не нативный `title`.
- Названия системных требований (`PHP >= 8.5`, `GD / Imagick`, `Writable: storage/`) —
  **технические идентификаторы**: они же служат ключами для выбора подсказки во вьюхе.
  Переводятся только расшифровки, сами названия одинаковы на всех языках.

### Общий набор классов шага (11.08.2026)

Все семь шагов + необязательная страница «Возможности» собраны из ОДНОГО набора
`.ins-*`, объявленного в `resources/views/layouts/frontend-install.blade.php`:
`.ins-head` (шапка полосой: знак, надзаголовок «ШАГ NN · УСТАНОВКА», название,
строка о шаге), `.ins-label`/`.ins-input`, `.ins-callout` (указание, без которого
шаг не пройти), `.ins-help`/`.ins-help__cap` (ответ на частую заминку),
`.ins-foot`/`.ins-act`/`.ins-act--go`/`--dim`/`--dev`, `.ins-back`, `.ins-check`,
`.ins-feats`/`.ins-feat` (+`--card` с описанием), `.ins-reqs`/`.ins-req`,
`.ins-score`, `.ins-countdown`, `.ins-facts`, `.ins-warn-list`, `.ins-locked`.
**Новый шаг не заводит свои копии этих классов** — набор уже дважды начинал
разъезжаться по вьюхам.

⚠️ `.ins-locked` намеренно НЕ flex: внутри строки есть разметка
(`<span class="font-mono">путь</span>`), и во flex-контейнере она становится
отдельным элементом со своим `gap` — перед закрывающей скобкой появлялся лишний
пробел. Общее правило: **строка с инлайновой разметкой внутри — не flex.**

### Финальный экран отделён от тяжёлого запроса (11.08.2026)

Было: `GET /install/finish` в ОДНОМ запросе прогонял два десятка сидеров, писал
`storage/install.lock`, входил админом и тут же отдавал итоговую страницу.
Владелец поймал вживую: «всё записалось, соединения нет, повисело и закинуло
в Кабинет». Разбор цепочки — три звена, каждое само по себе логично:

1. запрос длинный, и обрыв на последних байтах терял страницу;
2. замок к тому моменту уже создан → `BlockIfInstalled` уводит `/install/*`
   на `route('login')`;
3. админ уже вошёл (авто-вход прошёл до обрыва) → форма входа под `guest`
   отправляет его в личный кабинет.

Стало: тяжёлый запрос заканчивается коротким редиректом на **`/install/done`**
(`InstallController::done()`), а сама страница живёт в отдельной группе
маршрутов **без `block.if.installed`** — то есть работает уже после установки.
`BlockIfInstalled` при наличии `install.summary` в сессии тоже уводит на итог,
а не на вход. Нет сводки (чужой браузер, закладка) — уводим на главную сайта.

- **Порядок в конце `finish()` несущий:** сводка кладётся в сессию, дальше
  ЯВНЫЙ `session()->save()`, и только потом создаётся файл-замок. Пока замка
  нет — мастер открыт и шаг можно пройти заново (сидеры идемпотентны); раз
  замок появился — сводка гарантированно на диске.
- Отсчёт на финальном экране уводит **на главную страницу сайта** (просьба
  владельца), кнопка в панель рядом. На нуле подпись меняется на «Открываем…»:
  раньше счётчик замирал на «через 0 с» всё время загрузки.
- Регресс закреплён `tests/Feature/InstallFinishTest.php` (5). Тест на старом
  коде падал ровно тем же редиректом на `/login`, что видел владелец.

⚠️ **`SkipDatabaseForInstall` форсирует `session.driver = file` на всех адресах
`install*`.** Поэтому `/install/done` обязан оставаться под тем же
`skip.install.db` — иначе он читал бы ДРУГОЙ store, чем `/install/finish`, и
сводки бы не нашёл. В тестах из-за этого `withSession()` пишет в `array`, а
запрос читает `file`: в `setUp()` нужен `config(['session.driver' => 'file'])`.

- **Кнопка «Я разработчик» на шаге лицензии** показывается, только если
  `DEVELOPER_MODE` действительно прописан в файле `.env`. `isDeveloperMode()`
  читает сам файл, а не `env()`: переменные окружения разбираются один раз на
  буте и переживают перезапись `.env` (её делает шаг базы данных) — пропади
  ключ, `env()` продолжал бы отдавать прежнее значение. Пояснений под кнопкой
  нет намеренно, её видит один разработчик.
- **Чистка кешей после установки** (`config:clear` + `cache:clear`) идёт через
  `after_response()` и стоит ~250 мс — измерено, это НЕ причина «зависания».

---

## 🩹 Особенности песочницы (читать до отладки странных падений)

- **Нет рабочего `pdo_mysql`.** `php artisan <что угодно>` падает с «could not find driver»:
  `ModuleServiceProvider::boot()` дёргает БД, а реальный `.env` смотрит в MySQL.
  Обход для разовых команд:
  ```bash
  APP_ENV=testing DB_CONNECTION=sqlite DB_DATABASE=:memory: php artisan <cmd>
  ```
  В PowerShell: `$env:APP_ENV='testing'; $env:DB_CONNECTION='sqlite'; $env:DB_DATABASE=':memory:'; php artisan ...`
- **Тесты гоняй через `php vendor/bin/phpunit --no-coverage`,** а не `php artisan test`
  (последний сначала грузится с реальным `.env`). `phpunit.xml` уже переопределяет
  `DB_CONNECTION`/`DB_DATABASE`/`CACHE_STORE`/`SESSION_DRIVER`/`CAPTCHA_ENABLED`.
- **Правил `.blade.php` — почисти кеш вьюх перед прогоном:**
  `rm -rf storage/framework/views/*.php`. Устаревшие скомпилированные вьюхи однажды
  уже маскировали починенный баг компиляции Blade.
- **`storage/install.lock` несущий для тестов.** Файл git-ignored и машинно-локальный.
  Без него middleware `RedirectIfNotInstalled` уводит *каждый* запрос на `/install`,
  и валится ровно ~36 тестов. Симптом в диффе теста:
  `-'http://127.0.0.1:8000'` / `+'http://127.0.0.1:8000/install'`. Восстановить:
  ```bash
  printf 'Installed' > storage/install.lock
  ```
  ⚠️ **Но сначала подумай, не удалил ли его пользователь намеренно.** На `/install`
  пускает middleware `block.if.installed`, а он требует, чтобы файла НЕ было. То есть
  пока владелец проекта смотрит мастер установки в браузере, lock-файла быть не должно,
  и эти 36 падений — ожидаемое поведение, а не регрессия. Не восстанавливай файл молча:
  проверь тесты с ним, отчитайся числом, а состояние верни как было.
  Ни один участок кода lock не удаляет (проверено): пропал — значит, его убрали руками.
- **`bootstrap/cache/services.php` регенерируемый** — если что-то выглядит задвоенным,
  сначала проверь его:
  ```bash
  rm bootstrap/cache/services.php bootstrap/cache/packages.php
  APP_ENV=testing DB_CONNECTION=sqlite DB_DATABASE=:memory: php artisan package:discover
  ```
  (`composer dump-autoload -o` дёргает `package:discover` пост-хуком, а тому нужна рабочая
  БД-конфигурация — иначе кеш молча остаётся протухшим.)
- **Диагностика IDE врёт про `$errors`.** На `$errors->any()` во вьюхах анализатор пишет
  «Call to a member function any() on a non-object of type array». Ложное срабатывание:
  `$errors` — это `ViewErrorBag`. Игнорируй.
- **Тёмная тема во всей админке не работает ни на одной странице — молча, без ошибок.**
  `public/assets/css/tailwind.min.css` (2,9 МБ, закоммичен, не через `@vite`/JIT, а
  отдельный статический билд) содержит **вообще ни одного** `dark:`-варианта — ни
  `dark:bg-gray-900`, ни `dark:text-white`, ничего. Проверено систематическим
  грепом (в файле экранирование `\:`/`\/` в селекторах, наивный grep без этого
  ничего не найдёт и создаёт ложное впечатление, что классы есть). При этом сам
  переключатель темы (`components/admin/dark-mode-toggle`, `localStorage`,
  `document.documentElement.classList.add('dark')`) исправно работает и класс
  `.dark` на `<html>` появляется — просто применять его visually нечему, в
  загруженном CSS нет ни одного селектора с этим классом. Сотни `dark:`-классов
  по всей админке (навбар, сайдбар, дашборд, все вьюхи модулей) годами не имели
  никакого эффекта. Не чинили: `package.json` держит одновременно
  `tailwindcss: ^3.4.19` и `@tailwindcss/vite: ^4.3.3` — похоже на незавершённый
  переход на Tailwind 4 (см. «Не сделано» про мажорные апгрейды), и пересборка
  этого файла — отдельная, рискованная задача, требующая сначала разобраться,
  каким именно скриптом/конфигом его вообще собирали. Обнаружено 22.07.2026 при
  проверке, что новые классы вроде `bg-white/70`/`text-[11px]` реально
  рендерятся — заодно выяснилось, что **opacity-модификаторы (`bg-white/NN`) и
  произвольные значения (`text-[…]`, `shadow-[…]`) в этой сборке тоже отсутствуют
  целиком** (не только `dark:`) — она включает полный стандартный набор утилит
  (все классы, все цвета/оттенки, все брейкпоинты), но **не** JIT-генерируемые
  комбинации. Как проверить самому, что класс реально попал в сборку:
  ```bash
  grep -aoF '.имя-класса' public/assets/css/tailwind.min.css | wc -l   # экранируй : как \: и / как \/
  ```
  Практическое правило на будущее: новые Tailwind-классы в шаблонах — **только
  из стандартной палитры** (никаких `/NN`-прозрачности и `[...]`-произвольных
  значений), а любая полупрозрачность/блюр — литеральным CSS в `<style>`
  (пример: класс `.admin-glass` в `layouts/admin.blade.php`, добавлен
  22.07.2026 взамен `bg-white/80 dark:bg-gray-900/80 backdrop-blur` в навбаре
  и подвале админки — та же полоса, тот же визуальный эффект, но реально
  работает). Опасный алиас: `shrink-0` в сборке тоже отсутствует, есть только
  старое имя `flex-shrink-0`.
- **Сборка — палитра Tailwind v2, а не v3 (проверено 22.07.2026).** Кроме `dark:`,
  opacity и arbitrary в `tailwind.min.css` отсутствует и **половина цветов v3**.
  Есть только core-палитра: `gray, red, yellow, green, blue, indigo, purple, pink`.
  НЕТ: `emerald, teal, amber, sky, lime, rose, cyan, violet, orange, fuchsia`.
  Практически: `bg-emerald-*`/`bg-amber-*` и т.п. НЕ рендерятся (badge «опубликовано»
  в модуле Menu годами был бесцветным). Для акцента бери `indigo` (он есть и стал
  общим акцентом редизайна), для статусов — `green`/`yellow`/`red`. Проверять:
  `grep -aoF '.bg-emerald-500' public/assets/css/tailwind.min.css | wc -l`.
- **Вариант `peer-checked:` в сборке тоже ОТСУТСТВУЕТ** (как и `accent-*`). Любой
  тумблер на `<input class="peer sr-only"> + peer-checked:bg-* / peer-checked:left-6`
  визуально НЕ переключается — трек и кнопка застревают в одном положении (в модуле
  Menu так «не работали» тумблеры «Активен/Показывать на сайте»). Рабочий тумблер —
  через настоящий CSS-селектор `input:checked ~ .track/.knob`: добавлен класс
  `.admin-toggle` в `layouts/admin.blade.php` (там же живут `.admin-glass`/`.admin-card`
  /`.admin-hint`/`.admin-icon-badge` — общие утилиты редизайна админки, literal CSS
  вместо неработающих Tailwind-вариантов).
- **Утилит `-translate-y-1/2` / `-translate-x-1/2` в сборке НЕТ — классический приём
  центрирования `absolute top-1/2 -translate-y-1/2` молча не центрирует.** В файле есть
  только `.transform` в v2-стиле (объявляет `--tw-translate-*` + `transform`), а самих
  сдвиговых классов нет вовсе, поэтому элемент позиционировался верхним краем на
  середине контейнера и «свисал» вниз. Так лупа в поле поиска на `/admin/menus`
  вываливалась из инпута (репортнул владелец скриншотом 25.07.2026) — иконка была не
  «слишком крупной», как показалось сначала, а именно нецентрированной. Приём
  использовался в **12 шаблонах** (поля поиска админки и фронта, `admin/files`,
  auth-страницы, Slideshow public/admin, Search) — то есть баг был сквозной и тихий.
  **Починка системная:** партиал `layouts/partials/tw-compat.blade.php` восстанавливает
  недостающие утилиты литеральным CSS (включая комбинацию обеих осей отдельным правилом
  с более высокой специфичностью — иначе второй класс перебивал бы первый), подключён
  во ВСЕ четыре лейаута, где приём встречается: `admin`, `frontend`, `guest`, `app`.
  Проверено пробой в браузере: `translateY(-50%)` даёт `matrix(1,0,0,1,0,-10)` при
  высоте 20px, комбинация — `(-10,-10)`. Как проверять наличие класса в сборке:
  `grep -acF '.-translate-y-1\/2{' public/assets/css/tailwind.min.css` (0 = отсутствует).
  Урок шире: **перед использованием любой Tailwind-утилиты сложнее базовой — грепни
  сборку**; «не работает вёрстка» тут почти всегда означает отсутствующий класс, а не
  ошибку в разметке.
- **`ghostClass`/`chosenClass`/`dragClass` у SortableJS — РОВНО ОДИН токен, не список
  Tailwind-классов.** SortableJS применяет их через `el.classList.add()/remove()`, а тот
  по спецификации кидает `InvalidCharacterError`, если в токене есть пробел. В
  `menu/edit.blade.php` эти опции были заданы как многосложные строки
  (`ghostClass: 'opacity-50 bg-indigo-100 dark:bg-indigo-900'`,
  `chosenClass: 'bg-indigo-50 dark:bg-indigo-900 border-indigo-400'`) — и на КАЖДЫЙ старт
  перетаскивания в `_onDragStart` летел `InvalidCharacterError`, обрывая жест. Это и был
  **настоящий** корень «drag-and-drop не работает» (23.07.2026, репортнул владелец с живой
  консолью браузера), а не только перекрытие sticky-панелью из части 2 сессии 22.07.2026:
  та панель мешала нижним пунктам, но добавленный тогда же `forceFallback: true` лишь
  довёл жест ДО `_onDragStart` — где он и падал на этих классах (без forceFallback
  нативный DnD не стартовал вовсе, поэтому краш просто не всплывал — маскировка одним
  багом другого). Починка: односложные `mi-ghost`/`mi-chosen`/`mi-drag`, а раскраска —
  литеральным CSS в `<style>` того же файла (indigo-палитра; в этой Tailwind-сборке
  `bg-indigo-*`/opacity всё равно рендерятся не полностью). Проверять другие места:
  `grep -rn 'ghostClass\|chosenClass\|dragClass' --include=*.blade.php` — на 23.07.2026
  многосложных значений больше нет (`admin/modules.blade.php` и `Slideshow/…/edit.blade.php`
  используют дефолтные односложные классы Sortable). Диагностика: если DnD «не тащится» и
  в консоли `InvalidCharacterError … DOMTokenList … contains HTML space characters` со
  стеком `_onDragStart`/`_onDrop` — это оно.
- **Вложение пункта в пустой (бездетный) пункт не работало — у приёмника был
  `display:none`.** После починки краха выше переупорядочивание заработало, но
  ВЛОЖЕНИЕ одного пункта в другой — нет (репортнул владелец, 23.07.2026). В
  `renderList()` под каждым пунктом создаётся дочерний `<ul>`-приёмник; у листьев он
  рендерился с классом `hidden` (`display:none`) и пустым — а у элемента с нулевым
  layout-боксом у SortableJS нет хитбокса, `elementFromPoint` его не вернёт, `drop`
  туда невозможен. Ронять можно было только в НЕпустые списки (у пунктов, где дети уже
  есть). Починка: пустой приёмник помечается классом `.mi-childlist` (вместо `hidden`)
  и во время перетаскивания (`body.mi-dragging`, ставится в `onStart`/снимается в
  `onEnd`) раскрывается в видимую пунктирную drop-зону 30px литеральным CSS
  (`.mi-childlist:empty`), плюс в конфиг Sortable добавлен `emptyInsertThreshold: 15`
  (без него пустой `<ul>` не считается целью). Приёмник помечается droppable только у
  пунктов на `depth<=1` (ребёнок станет уровнем 2 или 3); на `depth==2` он был бы 4-м
  уровнем — там приёмник остаётся `hidden`, чтобы не предлагать недопустимую глубину
  (лимит 3 уровня дублируется на сервере в `MenuController::updateOrder`). Общий урок
  для любого вложенного SortableJS-дерева: пустой список-приёмник обязан иметь
  ненулевой бокс (хотя бы во время drag), иначе вложение в него невозможно в принципе.
- **Тесты/локальная разработка могли затирать боевой `public/sitemaps/sitemap.xml`
  реальными пустыми данными.** 22.07.2026 во время сессии этот файл (в нём —
  настоящие URL продакшен-сайта kursk-manipulator.ru, отслеживается git) внезапно
  превратился в пустой `<urlset></urlset>`. Восстановлен из git
  (`git checkout -- public/sitemaps/sitemap.xml`) без потерь, но точную
  единственную причину найти не удалось — в проекте **три независимые,
  накладывающиеся друг на друга системы генерации sitemap**:
  `app/Console/Commands/GenerateSitemap.php` (пишет в `storage/app/public/sitemap.xml`),
  `app/Jobs/GenerateSitemap.php` (пишет в `public/sitemap.xml`, этот путь в
  `.gitignore`, поэтому его перезапись тестами не страшна — `tests/Feature/JobsTest.php`
  так и делает), и `Modules\Seo\Services\SitemapBuilder` /
  `Modules\Seo\Jobs\BuildSitemaps` (пишут в `config('seo.sitemaps.output_dir')`,
  **по умолчанию** — `public_path('sitemaps')`, БЕЗ переопределения для тестов —
  вот этот путь и есть тот самый боевой файл). У `App\Console\Commands\GenerateSitemap`
  и `App\Console\Commands\GenerateSitemapCommand` вдобавок **одинаковая сигнатура**
  `sitemap:generate` — при регистрации команд побеждает только одна, какая
  именно — не проверяли, вне задачи сессии.
  **Профилактика (сделано):** `phpunit.xml` теперь задаёт
  `SEO_SITEMAPS_DIR=storage/framework/testing/sitemaps` — что бы ни вызвало
  сборку в следующий раз (наблюдение, ручная проверка через свой `php -S`,
  очередной тест), она уйдёт в изолированный каталог, а не в боевой
  `public/sitemaps/`. Три системы генерации sitemap не консолидировали —
  это отдельная, более крупная уборка технического долга.
- ~~`php artisan serve` 404-ит на голом `/admin`~~ — **исчезло 04.08.2026**
  вместе с каталогом `public/admin/`: в нём лежал только TinyMCE, а его
  удалили при переходе на свой редактор. Причина была в том, что путь запроса
  побайтово совпадал с именем реальной папки на диске, и встроенный сервер PHP
  искал в ней `index.php` вместо передачи запроса в `public/index.php`. Сейчас
  голый `/admin` отдаёт 302 на вход. Если каталог `public/admin/` заведут
  снова — вернётся и 404.

---

## 🧨 Архитектурные грабли (найдены и починены — не наступай снова)

- **`require_once` в файлах маршрутов — ошибка.** PHPUnit гоняет все тесты в одном
  процессе: первый бут «расходует» `require_once`, и все последующие инстансы приложения
  молча теряют маршруты. Та же беда была бы на Octane/RoadRunner. Всегда `require`.
- **Двойная регистрация провайдеров в `bootstrap/app.php`.** Файл вручную `require`-ил
  `bootstrap/providers.php`, а потом отдавал его же в `->withProviders($providers)`,
  который по умолчанию домешивает тот же файл ещё раз — `boot()` каждого провайдера
  выполнялся дважды (включая `Event::listen()` в `EventServiceProvider`). Теперь
  `->withProviders($providers, false)` и явный `->withEvents(discover: false)`.
- **Гейт `isInstalled()` был слишком широким.** Он закрывал загрузку *всех* модульных
  маршрутов и вьюх, но базовый лейаут и глобальные Blade-компоненты ссылаются на
  неймспейсы `Menu::`/`Notifications::`/`Categories::` на каждой странице, независимо от
  статуса установки. Теперь `loadLegacyModules()` отрабатывает безусловно в начале `boot()`.
  Новый legacy-модуль, нужный до установки, добавляй в массив `$legacyModules`.
- **`isInstalled()` ронял boot при недоступной БД.** `Schema::hasTable()` открывает
  соединение; во время установки реквизиты в `.env` могут вести в никуда, и PDO бросал
  `QueryException` — 500 получал даже сам мастер `/install/*`. Теперь обёрнуто в try/catch.
- **Модуль Captcha не мёржил свой конфиг.** `mergeConfigFrom()` не вызывался, поэтому
  `config('captcha.enabled')` всегда падал в дефолт `true`, а `CAPTCHA_ENABLED` из `.env`
  не влиял ни на что. Починено.
- **5 миграций делали MySQL-only запросы в `information_schema`** для проверки индексов и
  FK. Переписаны на `Schema::hasIndex()` / `Schema::getForeignKeys()` (кросс-БД, Laravel 11+).
  У одной (`news_category`) не было try/catch — на Postgres/SQLite она бы жёстко упала.
- **Truthy-проверки на нулевых значениях** — устойчивый баг-паттерн в этом коде:
  `if ($model->some_nullable_int)` там, где `0` — легитимное значение (дни доставки и т.п.).
  Пиши `!== null`.
- **Медиа-библиотека (модуль Files) НИ С ЧЕМ не связана и публична — помни при доработках.**
  Проверено 25.07.2026 по вопросу владельца: редактор новостей/страниц (TinyMCE) грузит
  медиа через ДРУГОЙ маршрут `admin.upload.media` → `App\Http\Controllers\Admin\UploadController`
  → `storeAs('uploads', …, 'public')`, и записей в таблице `files` НЕ создаёт. Модель
  `Modules\Files\Models\File` используется только внутри самого модуля; ни `News`, ни `Page`
  на неё не ссылаются (нет `file_id`/связей), обложка новости тоже не оттуда. То есть это
  параллельное хранилище, из которого файл попадает в контент только копированием ссылки.
  Интеграции («выбрать из библиотеки» в редакторе и в поле обложки) НЕТ — если её делать,
  начинать надо с `UploadController`, иначе останутся два несвязанных хранилища.
  **Безопасность:** файлы лежат в `storage/app/public` и через симлинк доступны по прямой
  ссылке БЕЗ авторизации — это нормально для картинок сайта, но приватные документы туда
  класть нельзя. Скачивание (`admin.files.download`) закрыто `auth+admin`, путь берётся из
  БД (не из запроса), traversal невозможен. А вот загрузка раньше НЕ ограничивала типы
  (`file|max:10240` и всё): можно было залить `.php` (исполнится через `public/storage`) или
  `.svg`/`.html` со скриптом (XSS от имени домена). Добавлены `FileController::ALLOWED_EXTENSIONS`
  /`ALLOWED_MIME_TYPES` (SVG намеренно запрещён; демо-SVG кладёт сидер в обход формы),
  проверка дублируется внутри цикла загрузки, а `InstallController::hardenPublicStorage()`
  кладёт `.htaccess` в `storage/app/public` (в git его нет — каталог игнорируется).
  Для nginx правило пишется в конфиг: `location ~* ^/storage/.*\.(php|phtml|phar|cgi|pl)$ { deny all; }`.
- **`@json(...)` внутри HTML-атрибута (`onclick="…"`) ломает атрибут — нужен `@js(...)`.**
  `@json` выводит JSON как есть, то есть строку в ДВОЙНЫХ кавычках, и первая же кавычка
  закрывает `onclick="`. Браузер получает обрубок `onclick="openEditModal(1, "` —
  обработчик не срабатывает вообще (никакой ошибки в консоли: это не JS-ошибка, а
  битая разметка). Так «не работала кнопка Редактировать» у слайда в
  `Slideshow/Views/admin/edit.blade.php` (репортнул владелец 25.07.2026). Правильно —
  `@js(...)` (`Illuminate\Support\Js`): она отдаёт строку в одинарных кавычках с
  экранированием (`JSON_HEX_QUOT` и др.) и безопасна в контексте атрибута; в том же
  проекте так уже сделано в `Slideshow/.../index.blade.php` (кнопка «скопировать
  шорткод»). Проверять по всему репо:
  `grep -rn 'on[a-z]*="[^"]*@json(' resources/views modules --include=*.blade.php`
  (на 25.07.2026 совпадений больше нет). Диагностика: если обработчик «молча не
  вызывается», сначала посмотри отрендеренный HTML атрибута, а не JS.
- **`str_word_count()` НЕ работает с кириллицей — молча врёт.** Функция
  ASCII-ориентированная: в русском тексте она считает только латинские фрагменты
  (у страницы из 125 слов вернула **3**, у новости из 11 слов — **1**). Из-за этого
  «время чтения» на фронте всегда показывало «~1 мин» независимо от объёма — для
  текста в 825 слов тоже «~1 мин» вместо ~6 (найдено 25.07.2026: владелец спросил,
  реально ли оно считается — нет, не считалось). Правильный подсчёт слов —
  `preg_match_all('/[\p{L}\p{N}]+/u', $text)`. Вынесено в хелпер
  `reading_time($html, $wpm = 150)` (`app/helpers.php`), используется вьюхами
  `frontend/news/show` и `Menu::frontend.page`. Тот же урок шире: **любая строковая
  функция PHP без `mb_`/`/u` на русском тексте — подозреваемая** (`strlen`,
  `ucfirst`, `substr`, `str_word_count`, `wordwrap`).
- **`$validated['nullable_поле']` при отсутствующем поле → 500.** Правило
  `'parent_id' => 'nullable|...'` НЕ кладёт ключ в результат `$request->validate()`,
  если поле вообще не пришло (снятый select без value). Обращение
  `if ($validated['parent_id'])` тогда даёт `Undefined array key` (E_WARNING), а
  `HandleExceptions` в Laravel конвертирует это в `ErrorException` → 500. Именно
  так `MenuItemController@store` годами отдавал 500 на добавлении ЛЮБОГО корневого
  пункта меню (без родителя) — самый частый сценарий. Лечится нормализацией сразу
  после `validate()`: `$validated['parent_id'] = $validated['parent_id'] ?? null;`
  (сделано в `validateMenuItem()`, рядом с `active`). Правило: любое `nullable`-поле,
  которое потом читаешь по ключу, нормализуй к явному `null`, а не полагайся на
  присутствие ключа. `ConvertEmptyStringsToNull` (глобальный middleware) снимает
  только случай `поле=''`, но не случай «поля нет вовсе».
- **`syncModuleMetadata()` умел только обновлять, но не создавать записи.** После чистой
  установки таблица `modules` оставалась пустой (миграции её создают, наполняет отдельная
  команда `modules:sync`, а мастер её не звал). Следствие: вкладка «Модули» в админке
  пустая, `loadActiveModules()` не грузит ничего, и работают только модули из жёсткого
  списка `$legacyModules`. Теперь метод делает upsert (уже установленные системы чинятся
  сами при первом запросе), читает таблицу одним запросом и пишет только при реальных
  изменениях — раньше он делал по два десятка UPDATE на каждый запрос.
- **`\Log::` без импорта** работает лишь через алиасы фасадов и ломает статический анализ.
  Импортируй `Illuminate\Support\Facades\Log`.
- **Классы в `module.json` экранируются как `\\`, а не `\\\\`.** У Localization стояло
  четыре слэша: JSON декодировал их в две обратные косые, `class_exists()` возвращал
  false, и провайдер модуля молча не регистрировался.
- **`php artisan storage:link` — часть установки, а не ручной шаг.** Без симлинка
  `public/storage` любая загруженная картинка отдаёт 404, потому что ссылки строятся
  через `asset('storage/...')`. Мастер теперь делает это сам и предупреждает на финальном
  экране, если хостинг запрещает симлинки.
- **Незакрытый `@section('content')` — не косметика, а порча ответа целиком.**
  `create/edit/settings.blade.php` модуля Localization открывали `@section('content')`,
  но не закрывали его `@endsection` (закрывался только вложенный `@push('scripts')`).
  Blade/`@extends` компилирует так, что страница всё равно рендерится с виду нормально —
  но в PHP остаётся один незакрытый output-buffer на весь запрос. При завершении
  скрипта PHP сам сбрасывает этот буфер в вывод — **после** того как
  `CompressResponse`-миддлварь уже посчитала `Content-Length` и сжала тело в gzip,
  и лишние байты уезжают в хвост ответа. Браузер получает `net::ERR_CONTENT_DECODING_FAILED`
  на 200 OK. Симптом виден и в тестах: PHPUnit помечает такой тест не failed, а **risky**
  («did not close its own output buffers») — если видишь risky у теста, рендерящего
  вьюху, в первую очередь ищи именно непарный `@section`/`@push`/`@component`.
  Починено 22.07.2026, добавлен грепнутый по всему репо чек на баланс
  `@section('x')` / `@endsection` (без учёта регистра — `@endSection` тоже валиден,
  PHP резолвит имена методов регистронезависимо).
- **Blade не отличает CSS/HTML-комментарий от кода — директива срабатывает,
  даже если она упомянута просто текстом внутри `/* ... */`.** При переработке
  `layouts/admin.blade.php` (скругления → прямые края, 22.07.2026) в поясняющем
  CSS-комментарии была фраза «литеральный CSS (border-radius в `@push('styles')`
  того же дашборда)» — простое упоминание директивы для примера. Blade
  компилирует весь файл построчно ДО того, как что-либо становится CSS: строка
  внутри `<style>...</style>` для Blade — такой же текст шаблона, как и всё
  остальное, `/* */` он не распознаёт вообще. В итоге `@push('styles')` внутри
  комментария открыл настоящий, непарный push-стек на весь остаток файла —
  ровно тот же класс бага, что и с незакрытым `@section` выше, только источник
  куда менее очевиден (страница рендерилась без видимой ошибки, ловилось только
  PHPUnit-флагом risky «did not close its own output buffers» на ВСЕХ тестах,
  рендерящих `layouts.admin`, — дашборд, категории, локализация). Затем при
  попытке описать сам этот случай в соседнем комментарии тем же файлом —
  наступил на ту же грабли повторно, упомянув `@section` голым текстом уже в
  исправленной версии. Итоговое правило: в `.blade.php` нельзя писать `@word(...)`
  буквально нигде, включая комментарии любого рода (Blade `{{-- --}}`, CSS `/* */`,
  HTML `<!-- -->`) — ни в качестве примера, ни в качестве предупреждения. Если
  очень нужно сослаться на директиву текстом, комментарий про это должен жить
  вне `.blade.php` (README/CLAUDE.md), не внутри самого шаблона.
- **Контроллеры могут молча не отдавать вьюхе то, что она ожидает.**
  `LocalizationController@create/edit/settings` отдавали вьюхи без переменных
  (`$dateFormats`, `$timeFormats`, `$types`, `$groups`, `$presets`...), которые эти
  вьюхи используют с первой строки — реального 200 никогда не было. У `settings()`
  вдобавок была вторая, самостоятельная ошибка: настройки грузились через
  `->groupBy('group')` (коллекция коллекций), а вьюха ждёт плоский `key => value`
  массив — как раз то, что уже умеет отдавать `LocalizationSetting::getAllForCountry()`.
  Раз уж такое всплыло — прежде чем чинить у контроллера конкретный репортнутый
  маршрут, стоит дёрнуть и соседние экшены того же контроллера, если раньше по ним
  не было тестов: скорее всего, их тоже никто не открывал.
- **Alpine грузился ДВАЖДЫ — каждый `x-for` рендерился в двойном количестве.**
  `layouts/admin.blade.php` подключал `@vite(['resources/js/app.js'])` (а там
  `import Alpine` + `Alpine.start()`) И отдельным тегом `local_js('alpine.min.js')`.
  Второй экземпляр повторно инициализировал разметку: в подсказках поиска вместо
  4 пунктов выводилось 8, и так в любом списке админки на `x-for`. Обнаружено
  26.07.2026 живым замером в браузере (`shortcuts.length === 4`, а `.ags-item` в
  DOM — 8). Лишний тег из админки убран. ⚠️ **В `layouts/frontend.blade.php` та
  же двойная загрузка ОСТАЛАСЬ** (проверено: `build/assets/app-*.js` +
  `assets/js/alpine.min.js`) — на главной `x-for` нет, поэтому визуально не
  проявляется, но мина та же. В `frontend-install.blade.php` отдельный
  `alpine.min.js` нужен и должен остаться: там `@vite` нет вовсе.
  Проверять: `grep -rn "alpine.min.js" resources/views/layouts/`.
- **`strlen()` на кириллице врёт про длину — «минимум 2 символа» пропускало одну
  букву.** `GlobalSearchController::search()` отсекал короткие запросы через
  `strlen($query) < 2`, а одна кириллическая буква в UTF-8 — это ДВА байта: по
  запросу «т» сервер честно шёл искать и отдавал мусор. Исправлено на
  `mb_strlen()`. Это ровно тот же класс, что и `str_word_count()` выше: **любая
  строковая функция PHP без `mb_`/`/u` на русском тексте — подозреваемая**.
- **Главная страница панели отдавала 500 на PostgreSQL, а тесты были зелёными.**
  `DashboardController::getStats()` считал `Message::where('read', false)`, но
  колонка называется `is_read` (см. `create_messages_table`) → «столбец "read"
  не существует». Тесты не ловили: в тестовой БД таблица `modules` пуста, модуль
  Messages не регистрируется, и до запроса дело не доходит. **Урок: зелёный
  прогон НЕ доказывает, что страница открывается на боевой базе — код за
  `class_exists()`/активностью модуля в тестах просто не исполняется.** Проверять
  такие страницы живым рендером (см. приём с `Request::create` + `Kernel::handle`).
  NB: у соседней `admin_notifications` колонка `read` существует по-настоящему —
  `NotificationController` там прав, не «чини» его заодно.
- **`assertDontSee` ловит текст из CSS/HTML-комментариев в том же ответе.**
  Проверка «на английской локали нет строки „На сайт“» падала, потому что фраза
  упомянута в поясняющем комментарии внутри `<style>` шапки — а он уезжает в
  ответ как есть. Проверяй разметку, а не подстроку: `assertDontSee('>На сайт</span>')`.
  (Раньше на те же грабли уже наступали с `fx-themed`/`hdr-theme-dot`.)
- **`flex-wrap` отключает сжатие: элемент переносится вместо того, чтобы ужать
  соседей.** В подвале ряд чипов ровно заполнял строку, и блок соцсетей уезжал
  на вторую — при этом `flex-shrink` не срабатывал НИКОГДА, потому что элементы
  на строке не переполняли её (перенос случился раньше). Ни `flex:1 1 auto`, ни
  `flex:0 1 auto` на соседях это не чинят. Лечится композицией: блок переехал в
  нижнюю мета-полосу, где место есть. Общее правило: **если контейнер
  `flex-wrap`, не рассчитывай на `flex-shrink` для удержания элементов в строке.**
- **`LocalizationServiceProvider` не мёржил свой конфиг** — тот же баг-паттерн, что
  у Captcha (см. выше), только на этот раз молчаливой жертвой был не флаг, а массив:
  `modules/Localization/Config/localization.php` содержит `preset_countries`
  (7 стран для «Импорта» на `index` и автозаполнения на `create`), но
  `config/localization.php` в корне — более ранняя, вручную опубликованная копия без
  этого ключа. Без `mergeConfigFrom()` `config('localization.preset_countries')` тихо
  возвращал `[]`, и обе фичи были no-op. Добавлен `mergeConfigFrom()`; ключи, уже
  объявленные в корневом конфиге (`date_formats`/`time_formats` — там другая, по
  локали, а не по формату, форма), приоритета не теряют — `array_merge` в Laravel
  оставляет более раннюю (app-level) конфигурацию как есть.

---

## 🧪 Тесты

**Текущее состояние: 189/189 проходят, 0 ошибок, 0 падений.**
(166 было до сессии 22.07.2026, +4 на 500-ку в `/admin/localization/*`,
+1 на главную вьюху дашборда — `tests/Feature/AdminDashboardTest.php`,
+18 на модуль Menu — `tests/Feature/MenuModuleTest.php`.)
Замечание: PHPUnit стабильно печатает ~110 deprecations — это шум PHPUnit 11 /
Laravel-testing, не падения и не из нашего кода.

```bash
php vendor/bin/phpunit --no-coverage
php vendor/bin/phpunit --no-coverage --filter ИмяТеста
```

Конвенции:
- **`use RefreshDatabase;` обязателен** в любом Feature-тесте, который трогает БД или
  рендерит страницу: `LocalizationMiddleware` дёргает `countries` на каждом запросе.
  Несколько тестов раньше его не имели и молча ничего не проверяли.
- **Не зови `$this->seed()`** в юнит-тестах, проверяющих точное число строк:
  `PaymentDeliverySeeder` вставляет ~20 реальных записей и ломает `assertCount(1, ...)`.
- **`phpunit.xml` подхватывает только `tests/Unit` и `tests/Feature`.**
  Тесты внутри `modules/<Name>/Tests/` **не запускаются** (например,
  `modules/Localization/Tests/LocalizationServiceTest.php` — мёртвый груз).
  Новые тесты клади в `tests/`.
- Тесты, трогающие файлы переводов, обязаны переопределять путь через
  `$this->app->useLangPath($tmp)` — не правь реальный `resources/lang`.
- **`assertEquals(0, null)` проходит** (слабое сравнение, `0 == null` → true) —
  так замаскировался 500 на создании корневого пункта меню: `->value('order')`
  вернул `null` (пункт не создался), а `assertEquals(0, null)` не заметил.
  Для «строка должна была создаться» проверяй сам факт (`assertNotNull` на
  модели, `assertRedirect` вместо молчаливого 500), а не только значение поля.

---

## 📌 Где остановились (сессия 26.07.2026 — шапка и подвал админки)

Триггер: «переделать верхнюю панель админки». По ходу нашлись четыре живых
бага, которых не видел ни один из 289 тестов.

**Шапка** (`layouts/admin/header.blade.php`):
- **Поиск был кнопкой**, стилизованной под поле («Поиск… Ctrl+K»): печатать в
  него нельзя, ввод жил в модалке, без JS не работал вовсе. Переписан в
  настоящее поле (`components/admin/global-search.blade.php`): GET-форма на
  `admin.search.index` (работает без JS), подсказки под полем, стрелки/Enter,
  группировка по типу, «показать все результаты», Ctrl+K ставит курсор в поле.
- **Селект стран вместо языка.** «RU RU» выглядел языковым переключателем, но
  менял часовой пояс и форматы, а стран в базе ОДНА — список с единственным
  вариантом. Убран; добавлен настоящий переключатель языка (7 локалей,
  `frontend.locale.set` → `session('app_locale')`, как на сайте). Страны и
  форматы — в «Локализации», ссылка в меню профиля.
- **Переключатель оформления панели** вместо кнопки-луны: список тем из модуля
  Темы, личный выбор в `session('admin_theme')`, маршрут `admin.theme.set`
  (+`reset`), композер в `ThemeServiceProvider` раздаёт `panelTheme`/
  `panelThemes`/`panelThemeSlug`, `--admin-primary` в лейауте следует за ним.
  Луна убрана: `dark:`-вариантов в сборке нет (см. «Особенности песочницы»),
  класс `.dark` красил лишь пару блоков — «тёмная тема» выглядела сломанной.
  Флаг `localStorage('darkMode')` теперь ЧИСТИТСЯ, иначе кто успел включить —
  остался бы в ней навсегда. Удалены неиспользуемые
  `components/admin/dark-mode-toggle` и `components/user-dropdown`.
- Крошка «Панель /» убрана (ссылка называлась «Панель», а вела на `/admin/news`),
  добавлена отдельная кнопка «На сайт» (`target=_blank`), блок профиля выделен
  в карточку с аватаром/ролью, иконки собраны в **обоймы** `.ahd-group` по
  смыслу, полоса растянута на всю ширину (был `max-w-screen-2xl mx-auto` —
  на широком окне контейнер центрировался и всё «висело» посередине).

**Подвал** (`layouts/admin/footer.blade.php`): убраны колонка «Быстрые ссылки»
(в ней жила мёртвая кнопка на несуществующий `/admin/help`), кнопка «Вверх» и
подпись «Обновлено: <время отрисовки>» (всегда «сейчас», не значила ничего).
Добавлены техсводка чипами (PHP/Laravel/БД/кеш/очередь/окружение/пояс/язык +
**предупреждение о включённой отладке**), статистика с объёмом медиатеки
(кеш на час — обход `storage/app/public` нельзя делать на каждой странице),
блок разработчика и дата установки (mtime `storage/install.lock`). Соцсети —
настоящие брендовые глифы Font Awesome (`fa-vk/fa-telegram/fa-whatsapp/
fa-github`, все есть в локальной сборке) вместо самодельных SVG для MAX/Rutube.
Версия проекта переехала сюда из сайдбара (была в двух местах). Высота
подвала 105px вместо ~250. Зона фрагмента `admin.footer` сохранена.

**Сайдбар и мобильное меню** (часть 2 той же сессии):
- **Цвета взяты из темы.** Раньше сайдбар брал у неё только шрифт, а акцент был
  прибит литералами (`bg-indigo-600`, `from-indigo-500`) — при смене оформления
  он не менялся вовсе. Теперь всё на `--admin-primary`/`--admin-accent`.
- **`readable_ink()` (`app/helpers.php`) — цвет надписи поверх акцента.**
  Акценты тем очень разной яркости: белый текст на светло-голубом «Графите»
  (`#38bdf8`) давал контраст **2.14:1**. Считается относительная яркость по
  WCAG (с гамма-коррекцией sRGB), порог 3:1 — эти надписи полужирные, а порог
  4.5 переворачивал бы и фирменный индиго `#6366f1` (4.47:1), то есть весь
  установившийся вид. Результат — в `--admin-on-primary`/`--admin-on-accent`.
- **Отдельного пункта «Дашборд» нет** — логотип и так ведёт туда же, второй
  ссылки рядом владелец не захотел. Зато логотип получил состояние «я здесь»
  (`.asb-brand.is-active`), которого раньше не было ни у чего.
- **Мобильный drawer жил со своим списком из 5 пунктов** и давно разъехался с
  сайдбаром: с телефона половина разделов была недоступна в принципе. Переведён
  на тот же `AdminSections`.
- **`app/Support/AdminCounters.php`** — счётчики «есть новое» (заказы,
  сообщения, уведомления) одним источником для шапки и сайдбара, с мемо на
  время запроса: иначе те же `COUNT(*)` выполнялись бы в каждом шаблоне.

**Модуль Каптча**: у него была готовая вьюха `Views/admin/index.blade.php` и
**ни одного админского маршрута** — страница существовала и была недостижима.
Заведены `admin.captcha.index` + `CaptchaController::admin()`.

### Каптча: конструктор пресетов (часть 3 сессии)

Модуль оказался сломан глубже, чем предполагалось. Найдено и починено:

- **Правильный ответ отдавался клиенту в разметке.** `generateMath()` выводил
  `<input type="hidden" name="captcha_math_answer" value="42">`, слайдер —
  свою позицию. Каптчу можно было пройти, открыв исходный код страницы.
- **Слайдер не проходился в принципе.** `render()` подставлял
  `<input name="captcha" value="slider">`, а `verify()` делает `(int)` от
  значения: `(int)'slider' === 0`, и `abs($target - 0) < 10` при цели 30..170
  не выполнялось никогда. Обработчика перетаскивания при этом не существовало
  вовсе — тащить было нечем. Теперь есть pointer-события + клавиатура, а
  метка цели рисуется ВНУТРИ png-дорожки: с `left:207px` бот просто читал бы
  ответ из стиля.
- **Код хранился в одной ячейке сессии** — две каптчи на странице затирали
  друг друга. Теперь `session('captcha.instances')[id]`, у каждого экземпляра
  свой id (уходит в форму скрытым `captcha_id`), TTL 10 минут, лимит 12 штук,
  чистка при каждой выдаче и **одноразовость** (угаданный код нельзя
  переиспользовать повторной отправкой формы).
- `verify($input, $type, $id = null)` — старая сигнатура сохранена: на ней
  держатся правило `captcha:image` и модуль Комментариев (`CommentService`
  зовёт `verify($data['captcha'], config('captcha.default_type'))`).
  Правило `captcha` **без параметра** берёт тип из самого экземпляра по
  `captcha_id` — только так две каптчи на странице различимы.

Конструктор: `/admin/captcha` — сборка мышью с живым превью (строится тем же
`CaptchaService::render()`, что работает на сайте), сохранение в
`captcha_presets` (миграция в `database/migrations/`, модель
`Modules\Captcha\Models\CaptchaPreset`), дублирование/удаление, готовые
сниппеты для копирования.

Вставка в материалы: **шорткод `[captcha preset="slug"]`**. Своей системы
шорткодов в проекте не было (у Slideshow «шорткод» — это сырой
`@include(...)`, работающий только в Blade-шаблоне). Добавлен
`render_shortcodes()` (`app/helpers.php`), подключён в двух местах, где
выводится содержимое материала: `frontend/news/show` и `Menu::frontend.page`.
Рядом — `strip_shortcodes()` для мест, где содержимое превращается в простой
текст: без него `[captcha preset="…"]` уезжал в мета-описание страницы и был
виден в выдаче поисковика (`Seo\Services\SeoSyncService::truncateDesc` и два
места в `Seo\Providers\SeoSyncService`).

Выбор сборки в редакторах — партиал `Captcha::partials.editor-picker`,
подключён в 4 вьюхи (News create/edit, Pages create/edit): вставляет шорткод
в позицию курсора активного TinyMCE. Нет сохранённых сборок — вместо пустого
выпадающего списка ссылка в конструктор.

Тесты: `CaptchaPresetTest.php` (26) и `CaptchaModuleTest.php` (8).

**`app/Support/AdminSections.php` — единый список разделов** для сайдбара,
мобильного меню, крошки в шапке и глобального поиска. Раньше он жил только в
разметке сайдбара, и поиск про разделы не знал: найти «Темы» или «Локализацию»
было негде. Новый раздел добавляй ТОЛЬКО туда. У пунктов есть `keywords` —
синонимы для поиска («оформление» → Темы, «перевод» → Локализация) и
необязательный `counter` — ключ из `AdminCounters`.

**Переводы:** в `resources/lang/*/admin.php` добавлены секции `header` (41 ключ)
и `footer` (20) во все 7 локалей.

### 🌍 Перевод админки: сделано и очередь

**Измеренный объём (26.07.2026):** 2839 русских фрагментов в 130 файлах —
328 во вьюхах ядра (26 файлов), 1988 во вьюхах модулей (60 файлов), 523 в
admin-контроллерах (44 файла). Это не «дописать словарь», а работа на
несколько заходов, поэтому она разбита на транши.

**Транш 1 — сделан.** Всё, что видно на КАЖДОЙ странице панели:
- Названия разделов, группы и синонимы для поиска переехали в словари.
  `AdminSections::link()/url()` теперь принимают КЛЮЧ (`news`, `themes`), а не
  готовую подпись, и берут `admin.sections.<key>` + `admin.section_keywords.<key>`.
  Синонимы лежат строкой через запятую — так `lang:check` сверяет ключи, а не
  длину вложенных массивов. **Без перевода синонимов поиск разделов на других
  языках слепнет:** «appearance» не нашёл бы «Themes».
- У пункта появился `key` — по нему тесты сверяют раздел, не завися от языка.
- Типы результатов глобального поиска (`Раздел`/`Новость`/…) — из словаря.
- Общие компоненты: `flash`, `error-alert`, `bulk-actions`, `filter-panel`,
  `notifications-center`.
- Итого 140 ключей × 7 локалей, 100% по `lang:check`.

**Транш 2 — сделан.** Разделы «Новости» и «Страницы» целиком (6 вьюх:
`News/Views/admin/{index,create,edit,bulk-edit}`,
`Menu/Views/admin/pages/{index,create,edit}`) плюс партиал вставки каптчи
в редакторах. Словарь вырос до **282 ключей × 7 локалей**, 100% по `lang:check`.

Замер до/после на английской локали (русские фрагменты в содержимом страницы):
`/admin/news` 32 → 8, `/admin/news/create` 43 → 14, `/admin/pages` 18 → 4,
`/admin/pages/create` 29 → 4. **Весь остаток — это ДАННЫЕ из базы**
(названия категорий, заголовки новостей и страниц, имена шаблонов), а не
интерфейс. Их перевод — отдельная подсистема `content_translations`
(вкладка «Переводы» в форме материала), а не словари.

Что пришлось учесть по ходу:
- Больше половины строк живут не текстом между тегами, а в атрибутах
  `label="…"` / `hint="…"` компонентов `<x-admin.input>` / `<x-admin.select>`
  и в `@section('title', '…')`. Наивный поиск `>текст<` их не находит.
- Строки в JS (`confirm`, `alert`, `textContent`) выводятся через `@js(__())`,
  а не `{{ }}`: внутри скрипта фигурные скобки дали бы сырой текст в кавычках.
- Тесты, сверявшие русские подписи, пришлось привязать к локали
  (`withSession(['app_locale' => 'ru'])`) — иначе они падают, потому что
  тестовое окружение стартует на `en`.

**Транш 3 — сделан.** Раздел «Меню» целиком (`menu/{index,create,edit}`).
Словарь: **413 ключей × 7 локалей**, 100% по `lang:check`.

Замер на английской локали: `/admin/menus/create` — **0** русских фрагментов
(данных из базы там нет вовсе), `/admin/menus` 19 → 4, `/admin/menus/1/edit`
54 → 5. Остаток — названия меню и пунктов из базы.

Отдельно: **склонение «N пунктов» было захардкожено по-русски** прямо во
вьюхе (`$m10 === 1 ? 'пункт' : …` с учётом 11–14). На любом другом языке
форма была бы неверной. Заменено на `trans_choice('admin.menu.items_plural')`
с диапазонами Laravel (`{1} …|[2,4] …|[5,*] …`); у русского, беларуского и
казахского три формы, у остальных две. **Важно:** `trans_choice` НЕ
подставляет число сам — в строке обязателен `:count`, иначе счётчик выводит
только слово.

**Транш 4 (частично) — сделан раздел «Пользователи».** Пять вьюх
(`Users/Views/admin/{index,create,edit,login-history,password}`).
Словарь: **495 ключей × 7 локалей**.

Замер на английской локали: `/admin/users` 44 → 11, `/admin/users/create`
32 → 8, `/admin/users/1/edit` 30 → 9, `/admin/users/1/password` 12 → 1.
Остаток целиком — **названия ролей и их описания из таблицы `roles`** плюс
имя самого администратора.

Две вещи по ходу:
- **Плоские ключи `admin.users`, `admin.modules`, `admin.dashboard` и др.
  удалены.** Они дублировали `admin.sections.*`, нигде не использовались
  (проверено грепом `__('admin.X')`) и мешали завести вложенную секцию с тем
  же именем: `array_merge()` падал на строке вместо массива.
- **Склонение «право/права/прав» было захардкожено во вьюхе** с прямым
  комментарием «trans_choice зависит от текущей локали и на en дал бы неверную
  форму». Это ровно то поведение, которое нужно: форма обязана следовать
  языку. Переведено на `trans_choice('admin.users.rights_plural')`.

**Транш 4 (закончен) — Модули, Файлы, Дашборд.** Словарь: **635 ключей × 7
локалей**. Замер на английской локали: `/admin/files` 18 → **0**,
`/admin` 36 → **1** (остался логин админа), `/admin/modules` 42 → 19 —
остаток целиком названия модулей из `module.json`.

Находка: **`resources/views/admin/files/` и `App\Http\Controllers\Admin\FileController`
— мёртвый код.** Маршрут `admin.files.index` ведёт в
`Modules\Files\Controllers\Admin\FileController`, а одноимённая пара в ядре
ни на что не заявлена. Сначала перевёл именно её и не увидел изменений на
странице — только это и вскрыло дубль. **Перед переводом вьюхи проверяй по
`route:list`, какая именно рендерится.**

> ✅ **Обе удалены 05.08.2026** вместе с дублирующими маршрутами в
> `routes/web.php`. Пока дубль жил, выключение модуля Файлы не убирало раздел,
> а переводило его на ДРУГОЙ код: маршруты модуля грузятся только при активном
> модуле, а маршруты ядра — всегда, и отвечал зарегистрировавшийся последним.
> Модуль добавлен в `$legacyModules` (его маршруты нужны редактору материалов и
> быстрым действиям на главной).

Ещё: у французского словаря апостроф в `n'est` сорвал строку PHP — генератор
экранировал его, а ручная вставка ключей мимо генератора нет. Все словари
после правок прогоняй через `php -l`.

**Транш 5а — сделаны «Отзывы» и «Импорт/Экспорт».** Словарь: **728 ключей
× 7 локалей**. На английской локали `/admin/reviews` — **0** русских
фрагментов, `/admin/news-io` 56 → 3 (названия категорий из базы).

⚠️ **Грабли автозамены: шаблон `</i> Слово` без правой границы.** Правило
для «Все» съело начало «Всего:» — на странице появилось «Allго: 2».
Поймано только живым рендером. **Любая автозамена строк в шаблонах обязана
иметь границу справа** (`>Слово<`, `</i> Слово` + конец тега), иначе
короткое слово режет длинное. Проверять после прогона:
`grep -rn "}}[а-яА-Я]" modules resources --include=*.blade.php`.

**Транши 6–7 — сделаны крупные вьюхи модулей:** Slideshow, Delivery, SEO
(`robots`/`sitemaps`/`template-badge`), Translations. Замер на английской
локали: остаток везде — только данные из базы (названия слайдшоу, методов
доставки, URL страниц), интерфейс переведён полностью.

⚠️ **Подписи не всегда живут внутри `>текст<`.** Во вьюхах Delivery они
стоят отдельными строками с отступом (`<label>` в несколько строк). Первый
проход отрапортовал 47 замен, а страница не изменилась вообще. С тех пор
в генераторе замен есть и построчный паттерн
`^(\s+)ТЕКСТ\s*$` вдобавок к вариантам с тегами.

**Транш 8 — флеш-сообщения контроллеров, часть 1.** 30 вызовов
`->with('success'|'error', '…')` в девяти контроллерах: News, Menu,
MenuItem, Page, Categories, Delivery, Notifications, Localization,
Accessibility.

**Транш 9 — флеш-сообщения контроллеров, часть 2 (остаток простых).** 27
вызовов в семи контроллерах: Users, Slideshow, Payments, Messages,
System (Модули), Reviews, Localization. Словарь: **1124 ключа × 7 локалей**,
паритет полный.

**Приём для флеш-строк:** замена ограничена контекстом
`->with('success', '…')` целиком, а не голым литералом — иначе задело бы
одинаково звучащие строки в другом месте того же контроллера (валидация,
логи, JSON-ответы).

**Транш 10 — сообщения контроллеров закрыты полностью.** 52 замены в 17
файлах; русских литералов в `->with('success'|'error', …)` по всей панели
**0**. Сообщения с интерполяцией переведены на именованные параметры
(`:name`, `:count`, `:id`, `:from`, `:to`, `:message`) — `"{$переменная}"`
вычисляется до вызова `__()` и в словаре жить не может.

Три места пришлось переработать глубже, потому что они **склеивали
переведённое слово с остальной фразой** — в других языках порядок слов иной:
- Отзывы: `$actionText` («одобрен»/«отклонен») подставлялся в
  `"Отзыв {$actionText} успешно"` → `match` теперь выбирает сразу готовый
  ключ сообщения.
- Сообщения: массив `$actions` хранит ключи словаря, а не русские причастия.
- Модули: `$status` держит `module_enabled`/`module_disabled`.

**Приём против множественных форм:** число вынесено в конец фразы
(«Заархивировано. Сообщений: :count»). У русского, беларуского и казахского
три формы, и `trans_choice` на семь действий дал бы 49 строк ради
формулировки, которая и так читается естественно.

**Транш 11 — ошибки форм `withErrors()`.** 29 сообщений в 6 контроллерах
(бэкапы, обновления, категории, пункты меню, слайдшоу, установка модулей).
Заведена **отдельная секция `admin.errors`**: это не флеш после редиректа,
а ошибка рядом с полем формы — по ключу видно, где сообщение появится.
Словарь: **1206 ключей × 7 локалей**.

**Регрессия закреплена тестом.** `AdminTranslationTest::
test_admin_controllers_have_no_hardcoded_flash_messages` грепает все
admin-контроллеры двумя шаблонами (`->with('success'…)` и `withErrors([…])`)
и падает на любом новом русском литерале. Второй тест проверяет подстановку
именованных параметров на ru и en.

**Транш 12 — раздел «Локализация», список стран (`admin/localization/index`).**
Заведена секция `admin.localization`, словарь: **1242 ключа × 7 локалей**.
Замер живым рендером под админом: на `ru` 10 русских подписей вьюхи, на `en`
и `de` — **0**, код 200 везде.

Три места не поддались построчной замене:
- Плитки сводки строились из массива с русскими подписями, а `data-stat`
  считался как `Str::slug($label)` — на другом языке атрибут менялся бы вместе
  с переводом. Массив теперь несёт устойчивый ключ, подпись и сноска берутся
  из словаря по нему.
- Счётчик наборов вызывал `trans_choice('страна|страны|стран', …)` прямо во
  вьюхе — русские формы показывались на любом языке.
- Подтверждение удаления собиралось через `addslashes()` внутри `onsubmit`
  → заменено на `@js()` (см. правило про `@json` в атрибутах).

**Транши 13–14 — раздел «Локализация» закрыт целиком** (`create`, `edit`,
`settings`). Словарь: **1330 ключей × 7 локалей**. Живой рендер под админом:
`create` 8→0, `edit` 11→0, `settings` 12→0 русских подписей при переходе
ru → en/de, код 200 везде.

Что всплыло по ходу:
- **Плейсхолдеры и описания — тоже интерфейс.** `placeholder="Россия"` в поле
  названия и `value="Ставка налога (%)"` в скрытом поле `description` быстрых
  настроек: первый показывал русский пример на любом языке, второй писал
  русское описание **в базу** независимо от языка панели.
- **Строки внутри `<script>` вьюхи выводятся через `@js()`**, не `{{ }}` —
  фигурные скобки дали бы сырой текст в кавычках. Счётчик в подтверждении
  импорта подставляется на клиенте: `@js(__('…')).replace(':count', N)`,
  потому что число известно только после разбора файла.
- **Маршрут `admin.localization.edit` — это `admin/localization/edit/{code}`,
  а не `/{id}/edit`.** Проверять живым запросом по `route:list`, иначе ловишь
  404 и думаешь, что сломал вьюху.

**Транш 15 — `install.php` для de/fr/it.** Словарей у этих трёх локалей не
было вовсе, все 246 строк мастера уезжали в английский fallback. Теперь
install.php есть у **всех семи** локалей, наборы ключей совпадают с эталоном.

- Порядок ключей в новых файлах строится **по эталону**, а не по алфавиту —
  диффы между локалями остаются читаемыми, а пропущенный ключ виден сразу
  (генератор падает на нём, а не пишет файл молча).
- **Плейсхолдеры сверяются отдельным проходом.** Потерянный `:error` или
  `:version` превращает сообщение в бессмысленное, а грепом это не ловится:
  сравнивай `preg_match_all('~:[a-z_]+~')` по каждому ключу против ru.
- Не переводятся: имена переменных окружения (`APP_NAME`, `MAIL_HOST`,
  `DEVELOPER_MODE`), строка cron, формат лицензионного ключа, пути вроде
  `storage/install.lock`.

⚠️ **Тест `LocalizationTest::test_missing_key_falls_back_to_english`
проверял ровно то, что словарей нет** («install.php у de/fr/it пока нет»),
и после этого транша стал неверным по существу, а не «сломался». Заменён на
два: сверку наборов ключей `install.php` по всем локалям и проверку, что
fallback остаётся английским. Общий урок: тест, документирующий пробел,
надо переписывать вместе с закрытием пробела, а не подгонять под новый
результат.

### ⚠️ Поправка: панель переведена НЕ ВСЯ (замер 28.07.2026)

Формулировка «локализация панели закрыта» из траншей 12–15 **неверна**.
Закрыты: сообщения контроллеров (флеш + `withErrors`), обвязка (шапка,
сайдбар, подвал, названия разделов) и вьюхи разделов Новости, Страницы,
Меню, Пользователи, Модули, Файлы, Дашборд, Отзывы, Импорт/Экспорт,
Slideshow, Доставка, SEO (robots/sitemaps), Переводы, Локализация.

**Осталось ~1650 русских фрагментов в 74 вьюхах** (замер по исходникам без
`{{-- --}}`, `<style>`, `<script>` и HTML-комментариев). По модулям:

| Модуль | Фрагментов | Крупнейшие вьюхи |
|---|---|---|
| ядро (`resources/views/admin`) | 442 | `comments/index` (30), `analytics/index` (25) |
| Seo | 241 | `redirects/index` (39), `redirects/form` (36), `edit` (34), `partials/seo-widget` (31) |
| Menu | 194 | остаток после транша 3 — проверить, что именно |
| Categories | 146 | `index` (62), `edit` (41), `create` (36) |
| Payments | 104 | `partials/form` (45) |
| Captcha | 101 | `index` — конструктор пресетов (90) |
| Messages | 93 | `index` (32) |
| Slideshow / Search / News / Users | 58 / 53 / 47 / 36 | |
| Comments / NewsIO / Localization / Accessibility / Delivery / Files / Reviews | 28 / 27 / 23 / 19 / 19 / 13 / 12 | |

⚠️ **Счётчик завышен и мерить по нему нельзя.** Он ловит русский в
`@php`-блоках, `//`-комментариях и атрибутах вроде `data-*`. Menu и News уже
переведены траншами 3 и 2, но всё равно висят в таблице — значит, их остаток
почти целиком комментарии. **Перед траншем всегда делай живой рендер вьюхи на
`ru` и `en` и сравнивай числа**, а не доверяй грепу по исходнику.

**Очередь по убыванию отдачи:** Категории (3 вьюхи, часто открываемый
раздел) → Каптча (конструктор, 90) → SEO (4 вьюхи) → Платежи → Сообщения →
Комментарии и Аналитика в ядре.

### Фронтенд: замер и что там на самом деле (28.07.2026)

⚠️ **Прежняя запись «фронтовые шаблоны в основном с захардкоженным русским»
устарела.** Словарь `resources/lang/*/frontend.php` существует (112 ключей ×
7 локалей, секции `header/footer/news/search/account/common/cart`), и вся
обвязка сайта на него уже переведена: шапка, подвал, новости, поиск, личный
кабинет, корзина, сайдбар меню.

**Замер: 1871 русский фрагмент в 63 вьюхах ядра + 159 в 13 модульных.**
Но бо́льшая часть — **НЕ интерфейс**:

| Файл | Фрагментов | Что это |
|---|---|---|
| `frontend/pages/faq` | 242 | текст вопросов и ответов |
| `frontend/pages/contacts` | 116 | адреса, реквизиты |
| `frontend/pages/terms` / `privacy` | 105 / 102 | юридические тексты |
| `frontend/pages/developers` / `about` / `concept` / `partnership` | 97 / 58 / 49 / 43 | маркетинговые тексты |

Это **копирайт-контент, а не подписи интерфейса**. Загонять юридический текст
в языковой словарь неправильно: для содержимого в проекте есть отдельная
подсистема `content_translations` (вкладка «Переводы» в форме материала).
Прежде чем «доперевести фронт», реши с владельцем, эти страницы вообще
остаются статичными вьюхами или переезжают в материалы.

Дополнительно счётчик завышен: он ловит **комментарии** в коде и в CSS
(`layouts/admin.blade.php` показывает 86 фрагментов, хотя панель на других
языках рендерится без единого русского слова). Мерить надо по отрендеренной
разметке, а не по исходнику.

**Транш 16 — хвост обвязки.** Из шапки и подвала мимо словаря остались
строки, которых не видно при беглом просмотре: `aria-label` ссылки на главную
и кнопки «Наверх», `alt` логотипа, подпись корзины и три строки в
Alpine-обработчике формы «Напишите нам» (текст ошибки, тема и тело письма —
собираются на клиенте, поэтому через `@js()`).

⚠️ Первый замер после правки показал «Логотип» и «Корзина» на английской
локали — оба оказались **CSS-комментариями внутри `<style>` шапки**, а не
разметкой. Ровно тот случай, про который предупреждает запись про
`assertDontSee` выше: проверяй `alt="…"` / `>текст</span>`, а не голую
подстроку.

**Транш 17 — страница результатов поиска** (`frontend/search/results`).

⚠️ **Главное наблюдение: ключи в словаре УЖЕ БЫЛИ.** Секции
`frontend.search` (16 ключей) и `frontend.news` (19) заполнены на все семь
локалей и используются другими вьюхами, а `results.blade.php` дублировал те
же строки захардкоженными. Новых ключей не добавлено ни одного — только
подключены существующие. **Практическое правило: перед тем как заводить
секцию словаря для «непереведённой» вьюхи, грепни, нет ли уже готовых
ключей** — иначе получишь второй набор с теми же текстами и расхождение при
следующей правке. Проверять так:
```bash
php -r '$a=include "resources/lang/ru/frontend.php"; print_r(array_keys($a));'
```

**Транш 18 — виджет доступности** (`Accessibility::frontend.widget`, секция
`frontend.a11y`, словарь 128 ключей × 7 локалей). Опции описаны JS-объектом
внутри `<script>`, поэтому подписи выводятся через `@js()`.

⚠️ **Замер по странице может показать ноль просто потому, что блока на ней
нет.** Виджет подключается в `layouts/frontend` только при
`$accessibility->enabled`, а на боевой базе он выключен — рендер главной
давал 0 русских строк на ВСЕХ локалях, включая `ru`, что выглядело как
успешный перевод, а означало отсутствие виджета. Проверять такие
условно-подключаемые блоки надо прямым рендером вьюхи с заглушкой:
```php
view('Accessibility::frontend.widget', ['settings' => $stub])->render();
```
Правило шире: **ноль совпадений на эталонной локали — это не успех, а
сигнал, что проверяешь не то.** Сначала убедись, что на `ru` строки есть.

Заодно: тесты `SearchFrontendTest::test_search_page_opens_without_query` и
`test_too_short_query_is_reported` проверяли русские подписи и упали, потому
что тестовое окружение стартует на `en`. Привязаны к локали через
`withSession(['app_locale' => 'ru'])` — тот же приём, что уже применён в
тестах панели (см. транш 2).

**Решение по структуре словарей:** пока остаётся один `admin.php` с
вложенными секциями (`header`, `footer`, `sections`, `common`…). Дробить на
`admin/news.php` и т.п. осмысленно с 2-го транша, когда появятся сотни ключей
на раздел; сейчас дробление дало бы семь файлов по десятку ключей и лишний
шум в проверке паритета.

⚠️ **Команды `lang:check` в проекте НЕТ** (проверено 28.07.2026 —
`artisan` предлагает `lang:publish`/`license:check`). Более ранние записи
в этом файле ссылаются на неё ошибочно. Паритет ключей проверяется
разовым скриптом: рекурсивно уплощаешь `resources/lang/ru/admin.php`
в dot-ключи, сравниваешь `array_diff_key` с остальными шестью локалями
в обе стороны. Ожидаемо: 1124 ключа везде, 0 расхождений.

**Что переводить НЕЛЬЗЯ:** имена драйверов (`pgsql`, `file`, `sync`), слаги,
коды локалей, названия модулей и названия языков в переключателе (их принято
показывать на самом языке). Закреплено тестом
`AdminTranslationTest::test_technical_identifiers_are_not_translated`.

Тесты: `AdminHeaderTest.php` (23), `AdminFooterTest.php` (12),
`AdminSidebarTest.php` (11), `CaptchaModuleTest.php` (8).

## 📌 Где остановились (сессия 24.07.2026 — редизайн фронтенда)

Целостный редизайн всего фронта в стиле админки/Install (владелец одобрил по макету).
Единый дизайн-слой добавлен литеральным CSS в `layouts/frontend.blade.php` (перед
`</head>`) — классы `.fx-*` (indigo-акцент `#6366f1`/`#8b5cf6`):
- `.fx-topbar` / `.fx-accent-bar` — тонкая градиентная акцент-полоса (верх страницы,
  над подвалом); `.fx-badge` — градиентный бейдж-иконка у заголовков;
  `.fx-card` — стеклянная карточка (rgba+blur, подъём при наведении);
  `.fx-btn` — indigo-градиентная кнопка; `.fx-chip` — пилюля; `.fx-section-title/sub`.
- **Всё это literal CSS** (rgba/gradient/backdrop-filter реальные, не Tailwind-утилиты —
  в этой сборке `bg-*/NN`/arbitrary не рендерятся, см. «Особенности песочницы»).

Применено: акцент-полоса вверху `#wrapper`; убрана резкая чёрная рамка body;
новостные карточки (`frontend/templates/default.blade.php` — главная, и
`frontend/partials/news-card.blade.php` — грид /news) переведены на `.fx-card` +
`.fx-btn` + `.fx-chip`, эмодзи-бейджи (📰/💬) убраны, синий → indigo; мега-меню шапки
(`Menu::frontend.header`) — акцент indigo; выдвижной сайдбар (`Menu::frontend.sidebar`) —
indigo-градиентный язычок + стеклянная панель; подвал (`partials/footer`) — акцент-полоса
сверху + градиентный бренд-бейдж; логотип шапки — градиентный бейдж. Иконки контента —
FontAwesome (в шаблонах, грузится), перекрашены в indigo (`.fx-ico`). Проверено вживую
(computed-стили fx-*, консоль чистая), тесты 190/190.

**Шапка переработана полностью** (`layouts/partials/header`) в стиле `.fx-*`: стекло,
градиентный лого, пилюли-действия `.hdr-pill`, современный поиск, FA-иконки; убраны
старый JS-фолбэк «пустых» иконок и FORCE-LIGHT-блок. Логика (лого/тема/корзина/авторизация/
поиск) и `@include('Menu::frontend.header')` НЕ тронуты.

**Переключатель языка в шапке.** Хелпер `available_locales()` (app/helpers.php) тянет коды
из `resources/lang` (ru/en/be/kk/de/fr/it — «сколько их есть»). Роут
`GET /locale/{locale}` (`frontend.locale.set`) кладёт выбор в `session('app_locale')`.
`LocalizationMiddleware` теперь отдаёт `app_locale` ПРИОРИТЕТ над локалью страны (иначе при
единственной активной стране RU→ru выбор языка игнорировался; `?lang=`/SUPPORTED_LOCALES
охватывали только 4 локали и всё равно перебивались страной). `<html lang>` = текущая локаль.
NB: фронтовые шаблоны в основном с ЗАХАРДКОЖЕННЫМ русским (мало `__()`), поэтому смена языка
на фронте видна в UI-переключателе/`getLocale()`, но текст контента не переводится — это
отдельная задача (локализация фронтовых строк). Демо-стран в БД всего одна (RU), поэтому
country-switcher бесполезен как «языковой» — сделан именно language-switcher по locale.

---

## 📌 Где остановились (сессия 24.07.2026 — меню footer/sidebar на фронте)

Расширил меню на все три позиции (header уже был):

- **Три демо-меню при установке.** `SeedDefaultMenuCommand` теперь сидирует header
  («Главное меню»), footer («Меню подвала», 6 пунктов = прежняя колонка «Навигация»)
  и sidebar («Боковое меню», 5 пунктов) — все активны и заполнены. `definitions()` —
  массив по позициям; `seed($reset)` идемпотентно проходит по всем трём;
  `--reset` пересоздаёт пункты всех трёх. `InstallController::seedDefaultMenu` без
  изменений (зовёт `::seed(false)`).
- **Footer-меню → столбцы подвала. ОДНО footer-меню = ОДИН столбец** (заголовок
  столбца = название меню, пункты вертикально). Несколько footer-меню = несколько
  столбцов — так число столбцов регулируется количеством меню. `Menu::frontend.footer`
  выводит по одному `<nav.footer-col>` на меню (из `Menu::cachedByPosition('footer')`),
  сетка подвала (`.footer-grid`, literal CSS `repeat(auto-fit,minmax(200px,1fr))`)
  сама подстраивает число столбцов: слева «Разработчик», справа «Контакты», между —
  столбцы footer-меню. **Fallback** — один столбец «Навигация», если footer-меню пусто.
  Дефолт — ДВА коротких footer-меню по 3 пункта («Информация» + «Участие») = два
  столбца подвала. `SeedDefaultMenuCommand::definitions()` теперь ПЛОСКИЙ список
  (позиций может быть несколько на одну область — напр. два footer-меню).
- **Sidebar-меню → выдвижная панель.** Постоянной боковой колонки на фронте нет,
  поэтому `Menu::frontend.sidebar` — off-canvas: кнопка-язычок у левого края + панель
  (Alpine `x-data{open}`, слайд, затемнение с блюром, Esc/клик по фону закрывают).
  Подключён в `frontend.blade.php`. Рендерится только если sidebar-меню заполнено.
- **DRY:** `MenuItem::frontendUrl()` и `::displayIcon()` — общая логика ссылки и
  дефолтной иконки по типу (используют footer/sidebar-партиалы; header-mega-menu
  пока со своей копией).
- Админка позиций уже поддерживала (форма создания: header/footer/sidebar; фильтр
  в списке; edit позиция-нейтральный) — правок не потребовалось.
- Модель: `cachedByPosition($position)` (кеш `menu.$position`) и `flushCache()` уже
  чистили footer/sidebar — переиспользовано. Проверено вживую: footer 6 ссылок из БД,
  сайдбар открывается/закрывается. Тесты 190/190.

---

## 📌 Где остановились (сессия 23.07.2026 — модуль Menu: DnD + фронтенд)

Владелец сообщил, что в редакторе меню **не работает drag-and-drop**. Разбор (по
живой консоли браузера, которую прислал владелец) — три отдельные вещи, все
починены и описаны выше в «Особенностях песочницы»/«Архитектурных граблях»:

1. **Перетаскивание вообще не работало** — многосложные `ghostClass`/`chosenClass`/
   `dragClass` роняли `InvalidCharacterError` в `_onDragStart` (SortableJS применяет
   их через `classList.add/remove`, а тот требует один токен). Заменены на
   односложные `mi-ghost`/`mi-chosen`/`mi-drag` + литеральный CSS.
2. **Вложение в бездетный пункт не работало** — приёмник у листа был `display:none`
   (нет хитбокса). Помечен `.mi-childlist`, во время drag раскрывается в drop-зону
   (30px, пунктир) + `emptyInsertThreshold: 15`; предлагается только до глубины 3.
3. **Фронтенд: две навигационные полосы.** `layouts/partials/header.blade.php`
   рисовал ЗАХАРДКОЖЕННУЮ навигацию (`Главная/О нас/Вопросы/Контакты`) + поиск, а
   `layouts/frontend.blade.php` **вдобавок** подключал отдельным баром
   `Menu::frontend.header` (меню из БД) — получались две полосы, нижняя выглядела
   плохо. Свёл в одну по просьбе владельца:
   - `Menu::frontend.header` переписан в компактный инлайн-`<nav class="header-nav">`
     (без прежней широкой полосы `w-full border-y backdrop-blur`), с гардом
     `@if($rootItems->isNotEmpty())` — **источник правды только БД, статичного
     fallback НЕТ** (выбор владельца: пустое меню → в шапке остаётся только поиск).
   - В `partials/header.blade.php` захардкоженный массив ссылок заменён на
     `@include('Menu::frontend.header')`; у формы поиска добавлен `md:ml-auto`
     (держит поиск справа, когда меню пустое). Отдельный `@include` из
     `frontend.blade.php` удалён (проверено: больше нигде не подключается).
   - **Дефолтное меню-хедер сидируется при установке ВСЕГДА**, а не только с
     демо-данными: `InstallController::seedDefaultMenu()` (Главная `/`, О нас
     `/about`, Вопросы `/faq`, Контакты `/contacts`; `type='url'`/`active=true`
     явно, идемпотентно по title+position и menu_id+url) вызывается в `finish()`.
     Прежний inline-блок меню в `installDemoData()` заменён вызовом того же метода
     (одно определение). На фронте проверено живьём: в шапке ровно один `<nav>`
     (меню из БД) + поиск, второго бара нет. **NB:** сид отрабатывает только на
     установке — на УЖЕ установленной базе дефолтные пункты не появятся, меню
     правится в `/admin/menus` (у владельца сейчас там тестовые пункты).

Все тесты зелёные (190/190). Правки не коммичены.

### Часть 2: полировка фронтовой навигации (иконки, 3 уровня, адаптив)

Владелец гонял живое меню в браузере и указывал на проблемы по ходу. Итог:

1. **Дефолтные иконки на ВСЁ.** `menu-node.blade.php`: если у пункта нет своей
   иконки — берётся по типу (`url→link`, `page→file-text`, `category→tag`).
   Дефолтное меню (`SeedDefaultMenuCommand`): `home/info/help-circle/mail`.
   ⚠️ **Имена иконок сверять с бандлом `public/assets/js/lucide.min.js`** — там
   сборка со СТАРЫМИ именами: есть `home` (не `house`), `mail` (не `envelope`),
   `help-circle` (не `circle-help`); бренд-иконок (`whatsapp/telegram/vk`) НЕТ вовсе.
   Проверять: `grep -qi "[,{ ]Home:" public/assets/js/lucide.min.js` (PascalCase).
   `@themeIcon('X')` в lucide-режиме, если у имени нет алиаса в `ThemeServiceProvider::getAliases()['lucide']`,
   уходит в Lucide как есть → «icon name was not found». Отсюда были предупреждения
   `house/envelope/whatsapp/telegram` из футера — почищено (`house→home`,
   `envelope→mail`, `whatsapp→message-circle`, `telegram→send`).
2. **Чёрные точки в шапке.** В `partials/header.blade.php` есть JS-фолбэк, который
   заменяет «пустые» иконки (ширина <6px) на точку. Пункты подменю на момент замера
   `display:none` (ширина 0) → им ставились точки; и кнопка поиска (`@themeIcon('search')`)
   не успевала отрисоваться → тоже точка. Фикс: фолбэк теперь ПРОПУСКАЕТ элементы
   внутри `.header-nav` и вообще скрытые (`getClientRects().length===0`); кнопке поиска
   поставлен инлайн-SVG-лупа (не зависит от темы/Lucide/фолбэка).
3. **Простая ПРЯМАЯ вложенность до 3 уровней, без JS, работает на всех экранах.**
   Сначала сделал «красиво» (Alpine + hover + боковые flyout-ы) — владелец забраковал
   («выглядит убого») + **на тач-экранах hover не работал вовсе**. Переписал на чистый
   CSS без Alpine (`menu-node.blade.php` + стили в `Menu::frontend.header`):
   • десктоп (`min-width:1024px`) — простой выпадающий список у корня по `:hover`/
     `:focus-within`, вложенные уровни внутри него статично, отступом по глубине;
   • маленькие/средние (`max-width:1023px`) — вся навигация колонкой, всё дерево
     показано СРАЗУ раскрытым (без наведения/тапов), тем же отступом.
   Проверено вживую на 1280px (дропдаун `none`→`block` по hover, 3-й ур. inline) и
   375px (колонка, подменю `display:block` `static`, отступы 10→26px, стрелки скрыты).
4. **Единый источник дефолт-меню + ручной сброс.** Команда
   `modules/Menu/Console/Commands/SeedDefaultMenuCommand` (`php artisan menu:seed-default`
   / `--reset`), зарегистрирована в `MenuServiceProvider`. `InstallController::seedDefaultMenu()`
   делегирует ей (`::seed(false)`). Composer в `MenuServiceProvider` теперь грузит
   `items.activeChildren.activeChildren` (3-й уровень раньше молча не рисовался).
5. **Живое меню владельца сброшено к дефолту** (`--reset`): было «Главное меню» с
   мусором + лишнее header-меню «ууца» → стало одно «Главное меню» из 4 пунктов с
   иконками. Дальше владелец сам добавлял/переставлял пункты (это уже его данные —
   не трогаем).

Все тесты зелёные (190/190).

---

## 📌 Где остановились (сессия 22.07.2026 — модуль Menu)

Триггер: «проверь модуль меню, последние изменения были недоделаны». В рабочем
дереве лежали незакоммиченные правки прошлой сессии по модулю Menu (контроллеры,
роуты, `menu/edit.blade.php`) — их надо было доревизовать и довести до конца.
Разведка показала, что серверная часть контроллеров и JS в `edit.blade.php` уже
были цельными; доделаны хвосты + найден и починен критический баг.

**Самое важное (баг, а не доделка):** `MenuItemController@store` отдавал **500 на
добавлении любого КОРНЕВОГО пункта меню** (без родителя) — `$validated['parent_id']`
читался при отсутствующем ключе (`nullable` не кладёт его в результат валидации),
Laravel превращал `Undefined array key` в `ErrorException`. Тот же небезопасный
доступ был в `update()`. Починено нормализацией `parent_id` в `validateMenuItem()`.
Подробности и переиспользуемый урок — в «Архитектурные грабли». Предшествовал
сессии (строка не менялась прошлой правкой), но всплыл только когда по модулю
появились тесты — до этого по Menu не было НИ ОДНОГО теста.

**Второй живой 500 (репортнут владельцем прямо во время сессии):**
`PATCH /admin/menus/{id}/toggle` → `BadMethodCallException: This cache store does
not support tagging`. `Menu::flushCache()` и `cachedByPosition()` звали
`Cache::tags(['menus'])`, но боевой стор (file/database на Postgres-проде) теги
НЕ поддерживает — 500 на любом save/delete/toggle меню или пункта. В тестах стор
`array` (taggable), поэтому баг был невидим для CI и ловился только вживую.
Починка: теги только если `Cache::getStore() instanceof TaggableStore`, иначе
точечный `Cache::forget('menu.header|footer|sidebar')` (эти же ключи пишет
`cachedByPosition`). Регресс-тест `test_menu_save_and_toggle_work_on_non_taggable_cache_store`
принудительно ставит `file`-стор и бьёт по тому самому роуту. Урок вынесен в
«Особенности песочницы» — расхождение `CACHE_STORE=array` (тест) vs file/database
(прод) маскирует любые баги тегов кеша.

Доделано и проверено тестами (`tests/Feature/MenuModuleTest.php`, 17 шт.):

1. **`PageController::store()` не нормализовал чекбоксы** `published`/`show_on_homepage`
   через `has()` (в отличие от `update()`): снятая галочка полагалась на дефолт БД
   (`false`) — happy-path совпадал случайно, но логика зависела от схемы, а не от
   формы. Добавлена явная нормализация.
2. **Sticky-баг чекбокса «Опубликовать» в `pages/create.blade.php`:** при снятой
   галочке и ошибке валидации `old('published') == null` неотличимо от первой
   загрузки, чекбокс снова ставился checked — снять публикацию было нельзя. Добавлен
   скрытый маркер `_submitted`, логика: `old('_submitted') ? old('published') : default`.
3. **Поиск на `pages/index.blade.php` был чисто клиентским** (`oninput=filterPages()`
   прятал только текущие 10 строк), серверный `?q=` (который `index()` уже умеет) не
   вызывался — страницы за пределами первой десятки в поиск не попадали. Поле переведено
   в GET-форму с debounce-отправкой (400 мс), кнопка сброса, `@forelse`+пустое состояние
   («ничего не найдено» / «нет страниц»).
4. **Непарный `@push` в `menu/edit.blade.php`:** scripts-push не закрывался перед
   `@push('styles')` (оба `@endpush` свалены в конец) — Blade это переваривает через
   LIFO-стек ob-буферов, видимой поломки нет, но buffer scripts-push оставался открытым
   на весь styles-блок. Un-nested на чистую структуру (тот же класс хрупкости, что и
   незакрытый `@section` — при регрессе даёт risky-тесты).
5. **Иконки дерева меню под lucide (дефолт темы) были невидимы:** `@themeIcon`/`iconMarkup`
   под lucide отдают `<i data-lucide=…>`, а `createIcons()` header/footer дёргают один раз
   при загрузке — дерево и модалка строятся JS-ом ПОЗЖЕ и этот ранний вызов их не застаёт.
   Добавлен `refreshThemeIcons()` (no-op для FA/bootstrap/remix/tabler) после рендера
   дерева и вставки модалки.

Уже было сделано прошлой правкой (доревизовано, оставлено как есть): фикс роута
`admin.menus.destroy` (`menus/{menu}`), `nextOrder()` для нового пункта в конец уровня,
`with('children.children')` для 3-го уровня вложенности, реальный эндпоинт
`admin.menu_items.bulk` (было no-op), выборка категорий по `title` вместо
несуществующей `name`, экранирование/`iconMarkup` в JS-дереве.

**Не входило в коммит модуля Menu (оставлено как было, решать владельцу):** правка
`app/Providers/ThemeServiceProvider.php` (добавление иконки `globe` — в модуле Menu
не используется) и миграция `2026_07_21_120000_add_deleted_at_to_localization_settings_table`
(модуль Localization) — обе лежали в рабочем дереве до этой сессии.

### Часть 2: редизайн UI модуля Menu + два живых бага из браузера

Владелец попросил обновить UI всех admin-вьюх модуля Menu в общий стиль проекта
(редизайн 22.07.2026: прямые края, indigo-акцент, glass, акцентная полоса).
Перекрыты все 6 вьюх (`menu/{index,create,edit}`, `pages/{index,create,edit}`):
единая шапка с `.admin-accent-bar` + градиентным бейджем `.admin-icon-badge`,
indigo-кнопки вместо `bg-black`/`bg-green-600`, карточки `.admin-card`,
подсказки `.admin-hint`. В `layouts/admin.blade.php` добавлены общие утилиты
`.admin-icon-badge`/`.admin-card`/`.admin-hint`/`.admin-toggle` (literal CSS, т.к.
Tailwind-варианты в сборке частичны — см. «Особенности песочницы»).

Карточки меню на `menu/index` перекомпонованы (по просьбе владельца): бейджи
статуса/позиции в потоке под заголовком (убран `pr-28`-хак с absolute), три
кнопки действий **одного размера** (`grid grid-cols-3`, равная ширина + stretch),
чипы «N пунктов»/«ID» с русским склонением.

**Два бага, найденных владельцем в живом браузере (важнее косметики):**
1. **Drag-and-drop пунктов «внизу не работал».** Панель «Сохранить порядок» была
   `sticky bottom-3` ВНУТРИ контейнера дерева и висела над нижними пунктами во
   вьюпорте, перехватывая мышь (до редизайна `bg-white/90` не рендерился — панель
   была невидимой, но клики всё равно ловила; `.admin-glass` это проявил). Sortable
   исправно прицеплен ко всем `<ul>` — дело было только в перекрытии. Панель сделана
   статичной под списком (Ctrl+S сохраняет из любого места).
   > ⚠️ **Поправка от 23.07.2026:** это оказалось НЕ единственной (и не главной)
   > причиной. Перекрытие панелью мешало только нижним пунктам, но добавленный тогда же
   > `forceFallback: true` обнажил настоящий блокатор: многосложные `ghostClass`/
   > `chosenClass`/`dragClass` роняли `InvalidCharacterError` в `_onDragStart` на каждом
   > старте перетаскивания — DnD не работал совсем. Разбор и починка — в разделе
   > «Особенности песочницы» (пункт про SortableJS и односложные классы).
2. **Тумблеры «Активен/Показывать на сайте» «не работали и непонятны».** Сделаны на
   `peer-checked:`, которого в сборке НЕТ (см. «Особенности песочницы») — кнопка не
   ехала, цвет не менялся, плюс `rounded-full`→квадрат от `admin-sharp`. Заменены на
   рабочий `.admin-toggle` (`input:checked ~` — настоящий CSS) в форме добавления,
   в модалке редактирования (`menu/edit`) и в `menu/create`.

**Проверено вживую браузером** (playwright + рендер вьюх через временный тест-харнесс
в `public/`, статический `php -S`, headless-скриншот — vite-манифест на месте):
списки/формы/дерево рендерятся, кнопки равновелики, тумблеры переключаются, панель
сохранения не перекрывает дерево. ⚠️ При очистке превью-сервера ошибочно выполнен
широкий `taskkill` по всем `php.exe` — заодно погасил `php artisan serve` владельца
на :8000 (восстанавливается запуском `php artisan serve`). Впредь убивать превью-сервер
только по PID/порту, не по имени процесса.

---

## 📌 Где остановились (сессия 22.07.2026)

Триггер сессии: пользователь словил 500 на `/admin/localization` в браузере
(`View [Views.admin.modules] not found`). Разбор ушёл глубже одной строки —
весь раздел «Локализация» админки (4 страницы: список/создание/редактирование/
настройки страны) не открывался ни разу с рождения этого кода. Починено и
проверено тестами:

1. **`@extends('System::Views.admin.modules')` → `@extends('layouts.admin')`**
   в `index/create/edit/settings.blade.php` модуля Localization. Старая ссылка
   была структурно нерабочей (лишний `Views.` в пути неймспейса) — страница
   никогда не рендерилась, а не «работала случайно», как раньше говорилось
   в этом файле.
2. **Незакрытый `@section('content')`** в тех же `create/edit/settings.blade.php` —
   после фикса (1) страницы стали рендериться, но с незакрытой output-буферизацией,
   которая портила gzip-тело ответа (`net::ERR_CONTENT_DECODING_FAILED` в браузере
   при 200 OK). Добавлен `@endsection`. Подробности и как это диагностировать —
   в «Архитектурные грабли».
3. **`LocalizationController@create/edit/settings` не передавали вьюхам данные**
   (`$dateFormats`, `$timeFormats`, `$decimalSeparators`, `$thousandsSeparators`,
   `$types`, `$groups`, `$presets`, а `edit` ещё и `$settings`) — без них те же три
   экшена валились с «Undefined variable» сразу после починки (1)/(2). `settings()`
   вдобавок грузил настройки в неверной форме (`groupBy('group')` вместо плоского
   `key => value` от `LocalizationSetting::getAllForCountry()`).
4. **`LocalizationServiceProvider` не мёржил свой конфиг** — `mergeConfigFrom()`
   добавлен, `config('localization.preset_countries')` (7 стран) теперь реально
   доступен «Импорту» на `index` и автозаполнению на `create`.
5. **Регрессионный тест** `tests/Feature/LocalizationAdminViewsTest.php` (4 шт.) —
   бьёт по всем четырём страницам от лица админа, проверяет 200 и правильную вьюху.
   Раньше по этому контроллеру не было ни одного теста — отсюда и то, что баг дожил
   до ручной проверки в браузере.

Не трогали (не входило в задачу, но видно с этой сессии): Bootstrap-вёрстка тех же
4 вьюх и нерабочий `data-bs-toggle` у импорт-модала на `index` — см. «Не сделано».

### Часть 2: редизайн главной страницы админки («вау»-дашборд)

Владелец попросил переработать `admin/dashboard/index.blade.php` (маршрут `admin.dashboard`,
он же голый `/admin`) в духе мастера установки, но с полноценной поддержкой тёмной темы
(в install она принципиально светлая-only). Плюс три точечных, но сквозных правки по
всей админке:

1. **Дашборд переписан с нуля**, стилистика — «стекло» + цветная аура в духе
   `layouts/frontend-install.blade.php`: полупрозрачные карточки с блюром и подъёмом
   при наведении, градиентные бейджи-иконки, приветствие по времени суток и живые
   часы (Alpine, `dashGreeting()`), разгон чисел в карточках статистики при загрузке,
   быстрые действия перенесены из плавающей кнопки прямо в шапку (пилюли:
   тёмная основная + акцентные вторичные). Все PHP-данные из
   `DashboardController::index()` не менялись — только разметка и подача.
   Статусы системы (`backup/updates/cache/queue`) теперь по-русски, а не
   `ucfirst()` от английского ключа. Добавлен `@stack('styles')` в
   `layouts/admin.blade.php` специально под этот случай (см. выше).
2. **Chart.js (`public/assets/js/admin-dashboard.js`) переоформлен**: те же три
   метрики (новости/пользователи/заказы), но с градиентной заливкой под линией,
   стеклянными тултипами и цветами из той же палитры, что у карточек статистики.
   Файл используется только этой вьюхой — правка изолированная.
3. **Убраны бейджи `LOCAL`/`TESTING`/`PRODUCTION`** (`{{ strtoupper(app()->environment()) }}`)
   из `layouts/admin/navbar.blade.php` и `layouts/admin/footer.blade.php` — по прямому
   указанию владельца, окружение там просто не нужно было показывать.
4. **Плавающая синяя кнопка внизу справа убрана насовсем**: удалён компонент
   `components/admin/quick-actions.blade.php` и его `@include` в `layouts/admin.blade.php`
   (был единственным местом подключения — проверено `grep`). Функциональность не
   потеряна, см. пункт 1.
5. **Ссылки в футере**: YouTube/WhatsApp/Telegram заменены на MAX и Rutube
   (`layouts/admin/footer.blade.php`). Для обоих — собственный инлайн-SVG-глиф
   (диалоговое облако / плеер в рамке): их нет ни в Font Awesome, ни в
   Bootstrap/Remix/Tabler/Lucide, а точный товарный знак копировать незачем —
   общего обобщённого значка достаточно. У обеих ссылок `href="#"` — как и раньше
   было у YouTube, реальных адресов никто не давал, заполнить руками.
6. **Регрессионный тест** `tests/Feature/AdminDashboardTest.php` — раньше по
   `admin.dashboard` не было ни одного теста вообще.

**Важная побочная находка, не связанная с этой задачей** — см. новую запись про
`php artisan serve` и коллизию `/admin` с папкой `public/admin/` в разделе
«Особенности песочницы». Не чинили (вне задачи), только задокументировали.

### Часть 3: левый сайдбар — статичный, компактный, без прокрутки

Владелец попросил переработать сайдбар (`layouts/admin/sidebar.blade.php`) —
статичный (не сворачивающийся), компактный, красивый, в духе macOS/WordPress,
и чтобы все пункты помещались по высоте без вертикальной прокрутки.

1. **Убран переключатель «свернуть/развернуть»** и вся Alpine-логика вокруг
   него (`collapsed`, `localStorage('admin_sidebar_collapsed')`, авто-схлопывание
   на узких экранах). Сайдбар теперь fixed-width `w-60` (15rem), всегда в одном
   виде. Ниже брейкпоинта `lg` скрыт целиком (`hidden lg:flex`) — на мобильных
   и так есть отдельный выдвижной drawer (`layouts/admin/mobile-menu.blade.php`),
   дублировать его свёрнутой иконочной полосой не было смысла.
   `layouts/admin.blade.php`: `lg:pl-64` → `lg:pl-60` под новую ширину
   (JS-обсервер, который меряет реальную ширину сайдбара и подгоняет
   `padding-left` контента, не трогали — он и так динамический).
2. **Группы сведены с 4 до 3** («Доступность» с единственным пунктом
   присоединена к «Система» — экономия одного заголовка группы) и провизуалены
   собранее (`py-1` вместо `py-2` на пункт, `space-y-3` между группами вместо
   `space-y-5`) — иначе ~18 пунктов не помещались бы без скролла на типичной
   высоте окна. `overflow-y-auto` на `<nav>` оставлен как страховка на случай
   маленького экрана + все опциональные `Route::has`-пункты разом, не как
   расчётное поведение.
3. **Группы разделены по смыслу** — тонкая линия-разделитель (`border-t`) перед
   каждой группой, кроме первой, плюс маленькая точка-акцент перед заголовком
   группы.
4. **Логотип-квадрат «RU» заменён на иконку** (`fa-layer-group`, «слои» — тот
   же символ модульности, что в шапке мастера установки, `Install::welcome`) —
   раньше буквы «RU» в бейдже дублировали соседнюю подпись «RU CMS».
5. **В подвале убран повтор «RU CMS»**, вместо литерала `v1.0` теперь
   `config('app.version', '1.0.0')` — тот же источник, что уже использует
   `UpdateService::checkForUpdates()`. Ключа `version` в `config/app.php`
   до сих пор не было вообще (оба места молча падали в дефолт `1.0.0`) —
   **добавлен** `'version' => env('APP_VERSION', '1.0.0')` сразу после
   `'name'`, плюс `APP_VERSION=1.0.0` в `.env` (владелец тут же поставил
   `1.0.5` — значит, версия теперь реально из `.env`, как и просили).
6. **Активный пункт** — теперь заливка `bg-indigo-600` (акцент, как у
   дашборда и мастера установки) вместо плоского чёрного — визуально
   увязывает сайдбар с остальным редизайном сессии.

### Часть 4: единая шапка админки (было два бара с дублями)

Владелец попросил переработать хедер/панель («Панель / Меню», «Создать»,
«Поиск… (Ctrl+K)», «Админ»). Разбор показал, что `layouts/admin/header.blade.php`
и `layouts/admin/navbar.blade.php` — не два дополняющих друг друга бара, а
**два независимых бара с настоящим дублированием функциональности**:

- **Два обработчика Ctrl+K одновременно.** `navbar.blade.php` вешал свой
  `@keydown.window.prevent.ctrl.k` на `searchOpen`, а
  `components/admin/global-search.blade.php` (полноценный command-palette
  с автодополнением по модулям) уже имел собственный такой же обработчик.
  Простую форму-дублёр из navbar убрали, остался один — полноценный.
- **Два индикатора уведомлений на одни и те же данные.** Простой колокольчик
  в navbar считал `Notification::where('enabled',1)->count()` — ту же
  модель, что и полноценный `components/admin/notifications-center.blade.php`
  (выпадающая панель, «отметить все прочитанными» и т.д.). Оставлен только
  центр уведомлений.
- **Загрузка шрифта/иконок темы объявлялась только в `navbar.blade.php`**,
  хотя логически это общая для всей шапки настройка.

**Сделано:** оба файла слиты в один `layouts/admin/header.blade.php` (тёмный
бар — под него уже стилизованы global-search/notifications-center/country-switcher,
переделывать их под светлое стекло не стали, это отдельная, более рискованная
правка трёх независимых компонентов). `navbar.blade.php` удалён, `@include`
убран из `layouts/admin.blade.php`. «Ошибка»/«Геолокация»/«Система»
сведены к компактным одинаковым icon-only кнопкам (были самостоятельным текстом
с иконкой — заметно шире). Ссылку «На сайт» убрали совсем по прямой просьбе
(дублировала соседнюю кнопку уведомлений визуально, а не по смыслу — владелец
явно попросил убрать именно «иконку домика рядом с колокольчиком»). Кнопка
«Создать» — акцентная `bg-indigo-600` вместо
нейтральной рамки, в цвет остального редизайна сессии. `components/admin/dark-mode-toggle.blade.php`
переведён на тот же компактный icon-button (использовался только в этой шапке —
проверено `grep`, безопасно). Добавлен класс `.admin-glass-dark` в
`layouts/admin.blade.php` — тот же приём, что и `.admin-glass` (литеральный CSS
вместо неработающих `bg-*/NN`), но для постоянно-тёмной шапки.

### Часть 5: доработка глобального поиска (`Ctrl+K`)

Владелец попросил улучшить, расширить и «починить» поиск в шапке, и убрать
иконку-домик рядом с колокольчиком уведомлений (сделано в Части 4, домик
убран сразу после слияния шапки).

**Реальный баг, а не только полировка:** `App\Http\Controllers\Admin\GlobalSearchController::search()`
искал категории через `Category::where('name', 'like', ...)`, а у модели
`Modules\Categories\Models\Category` колонка называется `title` (проверено по
`$fillable` и по соседнему `Modules\Search\Controllers\Admin\SearchController`,
где для категорий правильно используется `title`). Ошибка молча гасилась
через try/catch с `Log::warning()` — поиск по категориям **всегда** отдавал
пустой результат, без единой видимой ошибки. Починено, проверено живым
запросом к `/admin/search/global` — категория находится.
Заодно поправлен `\Log::` без импорта в этом же файле (тот же паттерн, что
уже описан выше для другого места — добавлен `use Illuminate\Support\Facades\Log`).

**Расширено** (`resources/views/components/admin/global-search.blade.php`):
- **Гонка запросов.** При быстром наборе предыдущий (более медленный,
  по более раннему и общему запросу) `fetch` мог прилететь ПОСЛЕ последнего
  и перетереть более релевантные результаты. Добавлен счётчик `requestId` —
  применяется только ответ на самый последний запрос.
- **Debounce 250 мс** — раньше запрос уходил на каждое нажатие клавиши.
- **Группировка результатов по типу** (Новость / Страница / Категория / …)
  с подзаголовками — вместо плоского списка вперемешку.
- **Ссылка «Показать все результаты»** на `admin.search.index` (там уже
  есть полноценная страница поиска с фильтрами — просто не было перехода
  на неё из быстрого поиска).
- Прокрутка выбранного стрелками результата в видимую область.

**Важная находка при проверке (гораздо крупнее самой задачи)** — см. новую
запись «Тёмная тема во всей админке не работает» в «Особенностях песочницы»:
собранный `tailwind.min.css` не содержит вообще ни одного `dark:`-варианта,
плюс отсутствуют opacity-модификаторы (`bg-white/NN`) и произвольные значения
(`text-[…]`). Из-за этого пришлось на ходу поправить и уже сделанный в Части 2
дашборд (несколько мест использовали `dark:bg-gray-800/60` и `border-gray-900/5`
— заменены на сплошные цвета), и добавить `.admin-glass` — литеральный CSS-класс
в `layouts/admin.blade.php` взамен `bg-white/80 dark:bg-gray-900/80 backdrop-blur`
в навбаре и подвале админки (визуально то же самое стекло, но реально
рендерится). Сама тёмная тема — **не чинили**, это отдельная и рискованная
задача (см. запись в «Особенностях песочницы» про смешанные Tailwind 3/4
зависимости).

### Часть 6: прямые края везде + новая шапка/подвал + живые иконки соцссылок

Владелец попросил убрать во всей админке скруглённые края (только прямые),
перекомпоновать и «сделать интереснее» хедер и футер, и обновить иконки в
подвале (с разрешением скачать их из интернета при необходимости).

1. **Скругления убраны одним глобальным CSS-рубильником**, а не правкой
   `rounded-*` по десяткам вьюх и компонентов: `layouts/admin.blade.php` теперь
   вешает класс `admin-sharp` на `<body>` и объявляет
   `body.admin-sharp, body.admin-sharp * { border-radius: 0 !important; }`.
   `!important` на универсальном селекторе побеждает специфичность независимо
   от порядка подключения стилей — перекрывает и Tailwind-утилиты (`rounded-lg`,
   `rounded-full` и т.д.) по всей админке, и литеральный CSS вроде
   `.dash-card { border-radius: 1.25rem; }` из собственного стек-стиля
   дашборда. Область действия — только страницы с этим лейаутом (фронтенд и
   письма его не подключают, их не затронуло). Единственное, на что CSS
   `border-radius` не действует — атрибут `rx` у инлайновых SVG (это геометрия
   фигуры, не стиль): у Rutube-глифа в подвале `rx="4"` заменён на `rx="0"`.
2. **При этой правке словил и почти повторно наступил на баг-паттерн
   с незакрытым `@push`/`@section`** — см. новую запись в «Архитектурные
   грабли»: Blade компилирует директивы, даже если они просто упомянуты текстом
   внутри CSS-комментария. Стоило проверять `git stash` конкретных файлов и
   бисектить, прежде чем предположить регрессию где-то ещё — 8 risky-тестов
   («did not close its own output buffers») били ровно по тем тестам, что
   рендерят `layouts.admin` (дашборд, категории, локализация), что и указало
   на общий лейаут, а не на что-то специфичное для каждой из вьюх.
3. **`layouts/admin/header.blade.php` перекомпонован**: добавлена акцентная
   градиентная полоса сверху (`.admin-accent-bar`, переиспользуется и в
   подвале — визуально скрепляет верх/низ страницы), хлебная крошка переведена
   на моноширинный uppercase-стиль с цветной точкой, кнопка «Создать» получила
   срезанный угол (`.admin-clip-corner`, `clip-path` — тот же язык «прямо, но
   не скучно», что и у скруглений: угол именно срезан, а не скруглён). Правый
   кластер кнопок разбит разделителями на смысловые блоки (уведомления/деньги ·
   инструменты · персонализация · аккаунт) — раньше это была сплошная лента
   иконок без группировки.
4. **`layouts/admin/footer.blade.php` перекомпонован**: добавлен брендовый
   ряд сверху (иконка-логотип с срезанным углом + версия + тег-лайн +
   реальный стек `PHP/Laravel/DB/Cache/Queue` — значения `Cache`/`Queue`
   раньше считались в `$drivers`, но нигде не выводились, чистый мёртвый код;
   теперь показываются). Три колонки получили одинаковые подписи-заголовки
   (раньше подпись была только у одной из трёх). Внизу — отдельная закрывающая
   мета-полоса (копирайт + «Обновлено») вместо строки в первой колонке.
5. **Настоящий баг с иконкой VK, а не только эстетика.** Значение по умолчанию
   `icon_mode` темы — `lucide`, а в карте соответствий Lucide для `vk` стоял
   `venetian-mask` (маскарадная маска) с комментарием «близкой иконки VK в
   lucide нет» — то есть при дефолтной теме подвал реально показывал иконку
   маски вместо VK. Исправлено: у ссылок соцсетей в подвале (`VK`/`MAX`/
   `Rutube`/`GitHub`) свой собственный набор SVG, не зависящий от
   `icon_mode` темы. VK и GitHub — официальные глифы из Simple Icons
   (лицензия CC0, скачаны с `raw.githubusercontent.com/simple-icons/simple-icons`
   и вписаны инлайном, без внешнего CDN в рантайме). MAX и Rutube — по
   прежнему свои обобщённые глифы (как и раньше: точных логотипов этих
   сервисов нет ни в одном открытом наборе иконок, а копировать товарный
   знак незачем); у simple-icons есть слаг `max`, но это **HBO Max/логотип
   стримингового сервиса Warner Bros. Discovery, а не мессенджер MAX** —
   использовать его было бы явной путаницей брендов, поэтому для MAX
   оставлен прежний обобщённый рисунок. Пилюли соцссылок при наведении
   подсвечивают нижнюю грань цветом-ориентиром через CSS-переменную `--accent`
   (инлайн `style="--accent: #hex"` + `.admin-social-pill:hover` в общем
   `<style>`) — это условный акцент, не сертифицированный фирменный цвет.
6. **Проверено вживую** через уже отработанную методику (SQLite-скретч +
   собственный `router.php`), но в этот раз обычный HTTP-логин через curl
   упирался в CSRF/сессию (`storage/framework/sessions` — общий каталог на
   диске: файлы реальной, работающей на порту 8000 сессии владельца путались
   с тестовыми; конкретную причину, почему GET-запрос вообще не писал файл
   сессии на диск, не нашли — не стали убивать время на это дальше). Решение:
   рендер контроллера напрямую через `php artisan tinker --execute`
   (`Auth::login()` + вызов `DashboardController::index()` из контейнера,
   `Illuminate\Http\Request::create('http://127.0.0.1:PORT/admin', ...)` +
   `forceRootUrl()`, чтобы `asset()`/`url()` резолвили себя на тестовый порт,
   а не на `http://localhost`), результат сохранён как временный статический
   файл в `public/` (сразу удалён после скриншота) и открыт headless-браузером
   (`npx playwright screenshot`) — быстрее и надёжнее, чем добиваться живой
   сессии ради разовой визуальной проверки вёрстки.

---

## 📌 Где остановились (сессия 21.07.2026)

Сделано и проверено тестами:

1. **Полная локализация мастера установки на 4 языка.** Словари `install.php`
   (ru/en/be/kk, по 245 ключей, структура сверена скриптом), все 10 вьюх переведены
   на `__()` — захардкоженного текста не осталось (проверено скриптом, игнорирующим
   комментарии). Локализованы и строки контроллера: карточки возможностей, тексты
   ошибок, имена атрибутов валидации.
2. **Создан каталог `resources/lang/be/`** с нуля: `install`, `validation`, `messages`,
   `pagination`, `admin`. До этого беларуского в проекте не было вообще, и при выборе
   беларуского интерфейс уехал бы в английский fallback.
3. **Графический редактор переводов** в модуле Localization + создание любых новых
   локалей из админки (см. раздел «Локализация»).
4. **Флаг EN вернули на американский** (был Union Jack), пресет `US` снова
   `name = США`, `native_name = United States`.
5. **Шаг SMTP явно оформлен как необязательный.**

Что редактор сразу показал (реальные пробелы, не последствия правок):
`en` и `kk` не дотягивают по 10 ключей — русский `validation.php` (12 КБ) богаче
английского (6.7 КБ), в нём есть секции, которых нет в остальных языках.
Стоит выровнять, но в задачу сессии это не входило.

---

## 📌 Где остановились (сессия 04.08.2026 — страницы, слайдер, шапка)

Главный итог: **содержательных статических страниц во фронте больше нет.**
Всё, что было Blade-вьюхами в `resources/views/frontend/pages/`, переехало в
раздел «Страницы» и правится в визуальном редакторе. Осталась одна вьюха —
`sitemap.blade.php`, и она оставлена намеренно: там генерируемый список
разделов, а не редактируемый текст.

| Адрес | Слаг записи | Было |
|---|---|---|
| `/about` | `o-proekte` | вьюха про монтаж металлоконструкций (чужой проект) |
| `/faq` | `chastye-voprosy` | вьюха 806 строк |
| `/contacts` | `kontakty` | вьюха 768 строк с ЛИЧНЫМИ телефоном и почтой владельца |
| `/terms` | `soglashenie` | вьюха 200 строк |
| `/privacy` | `konfidencialnost` | вьюха |
| `/developers` | `razrabotchikam` | вьюха 252 строки |
| `/partnership` | `sotrudnichestvo` | вьюха 136 строк с ЧУЖИМ заголовком «Разработчикам» |
| `/donate` | `podderzhat-proekt` | вьюха 89 строк |
| `/concept` | → `o-proekte` | дубль «О проекте», удалён; адрес ведёт на ту же запись |

Приём переноса: `Route::get('/адрес', [PageController::class, 'show'])->defaults('slug', '…')`.
Красивый адрес сохраняется, запись доступна и по `/page/{slug}`. Все страницы
заведены в `SeedDefaultPagesCommand`, поэтому есть и на чистой установке.

Удалён `resources/views/frontend/pages/footer.blade.php` — мёртвый дубль на
156 строк, не подключался нигде (проверено грепом по всему проекту).

### Блоки содержимого — как верстать страницы без вёрстки

`public/assets/css/content-blocks.css` — ОДИН файл на два места: им оформляется
содержимое на сайте И он же подключается в визуальный редактор (`content_css` +
`body_class: 'page-content'`). Поэтому блок в редакторе выглядит ровно так, как
его увидит посетитель.

Классы: `pc-lead`, `pc-grid`/`pc-card`, `pc-ico`, `pc-stats`, `pc-steps`,
`pc-check`, `pc-note`, `pc-split`, `pc-tech`, `pc-cta`, `pc-faq`/`pc-faq__item`,
`pc-contacts`, `pc-map`.

В панели инструментов редактора есть кнопка **«Блоки»** — вставляет готовую
заготовку с примерным содержимым (`public/assets/js/editor-blocks.js`, один файл
на все четыре редактора: страницы и новости, создание и правка).

⚠️ **Стандартный список «Стили» для этого не годится.** Он рисуется выпадающим
списком с подписью «Абзац», визуально неотличимым от соседнего списка блоков.
Проверено перечислением кнопок панели: подряд идут «Абзац» (Block Paragraph) и
«Абзац» (Format Paragraph). Владелец справедливо не нашёл там ничего.

### 🧨 Новые грабли редактора (проверены на живом TinyMCE 8)

- **Редактор нормализует `<b>` в `<strong>`.** Вешать оформление на различие
  этих тегов нельзя: после первого же сохранения из панели подпись становится
  неотличима от значения. Так было в карточках контактов (подпись `<b>`,
  значение `<strong>`) и в строке цифр. Лечение: подпись — обычный `span`,
  значение — полужирный тег, а в правилах перечислены ОБА (`li > b, li > strong`).
- **Пустые инлайновые теги вычищаются.** Иконка `<i class="fas fa-…"></i>` без
  текста исчезала при сохранении вместе с обёрткой. Лечение:
  `extended_valid_elements: 'i[class|aria-hidden],span[class|style]'` — добавлено
  во ВСЕ семь редакторов проекта. У новостей список уже был, но перечислял
  только `iframe` — дополнен, а не заменён.
- **`<details>`/`<summary>` переживают редактор без настроек**, но внутри
  редактора НЕ раскрываются: клик там ставит курсор. Ответы были недоступны для
  правки. Лечение: правило для `.mce-content-body` показывает их все сразу —
  действует только внутри редактора, на сайте аккордеон работает.
- **Позиционные селекторы (`:first-child`) ломаются от значка.** Добавили иконку
  в карточку — нумерация съехала. Различать по тегу надёжнее.

### 🧨 Наложение и CSP

- **Две строки шапки получали одинаковый `z-index:1`** от правила стеклянной
  шапки (`.hdr-glass > *`), обе создавали свой контекст наложения, и при равном
  уровне выигрывала более поздняя — строка поиска. Выпадающий список языков был
  заперт в контексте своей строки: его собственный `z-index:1000` не мог поднять
  его над полем поиска. **Поднимать нужно СТРОКУ, а не элемент** —
  `.hdr-glass > .hdr-row1{ z-index:2 }`.
- **`frame-src 'self'` режет чужой iframe МОЛЧА.** Карта на контактах
  открывалась пустым прямоугольником без следа в интерфейсе. Домен карт
  разрешён точечно и только на сайте — в панели чужие фреймы запрещены.
- **Типографика содержимого вешает `margin` на `iframe`.** Внутри рамки карты
  оставались полосы фона по 25px сверху и снизу.

### Слайдер и просмотр картинок

- **Кадр слайдера следует пропорции первой картинки.** При фиксированной высоте
  вокруг картинки оставались размытые полосы: на десктопе 160px по бокам, на
  телефоне 38% площади. Высота из настроек стала НИЖНЕЙ границей.
- **Swiper замеряет высоту слайда один раз.** Пропорция кадра и подпись под ним
  меняют её уже после запуска — нужен явный пересчёт (`update` +
  `updateAutoHeight`), иначе нижняя строка подписи уходит под край.
- **Ключи JSON-LD `@context`/`@type` ломают ВЕСЬ шаблон.** Blade компилирует
  конструкции вида «собака плюс слово» даже внутри тега `script` — страница
  отдавала 500. Собирать ключи программно: `String.fromCharCode(64) + 'type'`.
  Это повтор известной мины из раздела «Архитектурные грабли», но в новом месте.
- **Просмотр картинки во весь экран — ОБЩИЙ на весь сайт**
  (`public/assets/js/image-viewer.js` + `image-viewer.css`, подключены в макете).
  Раньше жил внутри шаблона слайдшоу и работал только там, а в остальных местах
  клик перехватывали просмотрщики картинок из расширений браузера.
  Правило подбора картинок написано **от обратного**: перечислены служебные зоны,
  где трогать не надо (`header, footer, nav, aside, form, button`), а не места,
  где надо — иначе каждый новый шаблон пришлось бы вносить руками. Клик
  перехватывается на фазе погружения с остановкой всплытия; `mousedown`
  намеренно не трогаем, на нём Swiper начинает перетаскивание.

### Пагинация

- **Один файл на весь проект** — `resources/views/vendor/pagination/tailwind.blade.php`,
  его рендерят все 28 вызовов `links()` и в панели, и на сайте.
  `tailwind-rus.blade.php` была отдельной копией разметки и уже разъехалась —
  сведена к подключению основной.
- **Список новостей делится ЦЕЛЫМИ разделами.** Раньше резался плоский список по
  12 материалов, а группировка по шаблонам шла во вьюхе — раздел из пяти
  материалов оказывался разложен на две страницы. Единица разбиения теперь
  группа-шаблон (`GROUPS_PER_PAGE`). Инвариант закреплён тестом
  `FreshInstallRenderTest::test_news_pages_never_split_a_template_group`.
- ⚠️ **Группа `default` — это ТРИ состояния сразу**: шаблон записан буквально
  «default» (так пишет форма), либо пустая строка, либо NULL. Учитывать нужно всё.
- У списка новостей подпись пагинации задаётся вручную (`summary`): стандартный
  счётчик записей выдавал «Показано с 1 по 15 из 5» — пятнадцать карточек при
  пяти разделах.

### Подвал

Столбцы меню лежат в своей группе (`.footer-menus`), а не прямыми ячейками сетки.
Раньше каждый столбец получал столько же места, сколько бренд и контакты: при
двух меню — четыре колонки по 316px с шагом 48px, короткие списки ссылок
разъезжались. Теперь три смысловых блока (бренд, группа меню, контакты), шаг
внутри группы 28px. Три столбца встают в одну строку; на планшете группа
занимает всю ширину сетки, иначе три столбца ломались на 2+1.

**Третье footer-меню «Помощь»** заведено в сидер. Число столбцов по-прежнему
любое: добавили меню с позицией footer в панели — появилась колонка.

### Прочее из этой сессии

- **Автопрокрутка слайдшоу — 5 секунд.** Значение живёт в ПЯТИ местах (сидер,
  контроллер, модель, форма правки, публичная вьюха) и однажды уже разъехалось.
  Менять во всех сразу.
- **Обложки журнальных материалов** — `public/images/magazine/*.svg`, пропорция
  8:5. Значок внутри должен занимать 18–20% кадра: у «Шаблонов» он был 28% при
  пропорции 1,92 и смотрелся растянутым рядом с соседями.
- **`public/images/placeholder.png`** — заглушка для битых картинок. Её
  подставляет `resources/js/utils/resource-loader.js`, а файла в проекте не было:
  битая картинка давала второй 404.
- **Логотип шапки**: текстовый знак выводится ВСЕГДА и прячется лишь при живой
  картинке темы. Раньше сломанная ссылка на логотип оставляла шапку пустой.
  ⚠️ В обработчике `onerror` предка нужно искать ДО `remove()` — у оторванного
  от дерева узла `closest()` возвращает `null`.

---

---

## 📌 Медиатека: выбор, действия, постраничный вывод (05.08.2026)

Раздел показывал сетку и больше ничего: ни отметить, ни скачать, ни удалить —
всё по одному через карточку. Добавлены отметки на плитках, действия при
наведении (скачать/удалить) и полоса массовых действий, которая появляется
только когда что-то выбрано.

**Главное, что вскрылось по дороге — дубль маршрутов.** `admin.files.index`,
`upload`, `download` и `filter` были объявлены ДВАЖДЫ: в модуле и в
`routes/web.php`, где та же четвёрка вела в `App\Http\Controllers\Admin\FileController`.
Отвечал зарегистрировавшийся последним, а модульные маршруты грузятся только
при активном модуле — то есть выключение модуля Файлы не убирало раздел, а
переводило его на другой код (без чистки уменьшенных копий, с другой проверкой
типов при загрузке). Дубли и мёртвая пара контроллер+вьюха удалены, `Files`
добавлен в `$legacyModules`.

### Грабли: постраничный вывод «пропал», хотя всё работает

Общий компонент (`vendor/pagination/tailwind`) обёрнут в
`@if ($paginator->hasPages())` — при 18 файлах и минимальном размере страницы
24 он не рендерится ВООБЩЕ. Выглядит как поломка, а это ожидаемое поведение.
Две поправки: в список размеров добавлено 12 (разбиение достижимо на любой
библиотеке), а своя сводка «Показаны X–Y из Z» выводится под `@unless(hasPages())`
— иначе на нескольких страницах счётчик печатался бы дважды, свой и от компонента.

**Тот же компонент — во всех 28 списках проекта.** Отдельную разметку
постраничного вывода для раздела не пишем: «как в Кабинете» и «как в Медиатеке»
— это буквально один файл.

### Грабли: выдуманные ключи словаря

Завёл `admin.files.per_page` и `admin.files.selected`, не грепнув словарь — а
«Выбрано» и «Удалить выбранные» уже лежали в `admin.common` и использовались
другими разделами. На странице висели сырые ключи. **Перед тем как заводить
ключ, ищи готовый** (это правило уже было записано по итогам транша 17 — и я
на него наступил снова):

```bash
php -r 'print_r(array_keys((include "resources/lang/ru/admin.php")["common"]));'
```

Проверять результат надо рендером и грепом по отрендеренной разметке, а не
глазами: сырой ключ выглядит как обычный текст в вёрстке.

```bash
# сырые ключи в ответе (admin.blade.php в комментариях — ложное срабатывание)
preg_match_all("~admin\.[a-z]+\.[a-z_]+~", $html, $m);
```

Этим же приёмом нашлись `admin.dashboard.act_news` / `act_user` / `act_order` —
три ключа, которых не было в словаре ВООБЩЕ: лента последних действий на
главной панели печатала их сырыми на обоих языках. Заведены.

### Чистка словаря медиатеки

Секция `admin.files` похудела с 64 ключей до 51. Удалены 13: девять не
использовались нигде, четыре (`delete_one`, `delete_confirm`,
`delete_selected_confirm`, `none_selected`) обслуживали только удалённую вьюху
ядра. Два подтверждения одного действия сведены к одному: в модульной вьюхе
кнопка на плитке и кнопка в карточке спрашивали разными текстами.

Тесты: `tests/Feature/FilesLibraryTest.php` (4) — по медиатеке их не было
вовсе, хотя удаление здесь необратимо и трогает диск.

### Категории медиатеки — общие для проекта (05.08.2026)

У модуля были СВОИ категории: таблица `file_categories`, модель, контроллер,
маршруты `admin.file-categories.*`. Пользоваться ими было нечем — `index()`
рендерил `Files::admin.categories`, вьюхи с таким именем в модуле нет, то есть
маршрут отдавал 500 на каждый заход; в меню раздела не было. Таблица так и
осталась пустой, `category_id` у всех файлов — NULL.

Теперь файлы раскладываются по тем же категориям, что новости, страницы и
товары (`Modules\Categories\Models\Category`). Своя система удалена целиком.

**Расхождение было заложено двумя миграциями подряд.** Ранняя
(`create_files_table`) создаёт `files` со связью на `categories` — правильной.
Поздняя (`create_files_tables`) заводит `file_categories` и, раз таблица `files`
уже есть, уходит в ветку «дописать недостающие колонки»: связь она не переносит.
На обычной установке ключ и так смотрел куда надо, а лишняя таблица просто
висела рядом. Первая версия моей миграции обнуляла `files.category_id` целиком
«на всякий случай» — на такой установке это стёрло бы РЕАЛЬНЫЕ привязки.
Обнуляются только повисшие значения (`whereNotIn` по `categories`).

⚠️ **Внешний ключ снимай ПО КОЛОНКЕ, а не по имени.** `dropForeign('имя')` на
SQLite бросает `RuntimeException` («does not support dropping foreign keys by
name»), а `dropForeign(['колонка'])` он переваривает, перестраивая таблицу.
Тесты гоняются на SQLite — по имени миграция упала бы только там.

⚠️ **Тип категории — свободное поле, а не перечисление.** Владелец пишет его
руками (подсказка в форме: «Например: news, product»), и раздел «Категории»
показывает его как есть. Поэтому в выпадающих списках медиатеки тип служит
меткой `optgroup` без перевода — переводить пользовательские данные нельзя.
Пустой тип подписан «Без типа» (`admin.categories.no_type`), а не «Без
категории»: категория есть, у неё просто не задана группа.

Где категория теперь участвует: фильтр на странице медиатеки, выбор при
загрузке (иначе раскладывать пришлось бы по одному уже после), смена в карточке
файла отдельной кнопкой (не по `change`: прокрутка колесом над списком молча
переложила бы файл), и отбор в подборщике редактора — `browse()` отдаёт список
категорий вместе с первой страницей файлов, отдельного эндпоинта под него нет.

---

## 🗂️ Категории по умолчанию (05.08.2026)

`modules/Categories/Console/Commands/SeedDefaultCategoriesCommand` —
одиннадцать категорий и раскладка по ним уже существующих материалов.
Зовётся мастером установки (`finish()`) **последней из содержимого**: команда
не только заводит набор, но и привязывает новости, страницы и файлы — вызови
её раньше, привязывать будет нечего.

```bash
php artisan categories:seed-default            # дозаполнить недостающее
php artisan categories:seed-default --reset    # переписать названия и переприкрепить
```

Набор построен по тому, что в CMS реально есть после установки:

| Тип | Категории | По какому признаку привязка |
|---|---|---|
| `news` | Новости, Журнал, Клиника, Игры | шаблон материала |
| `product` | Товары | шаблон `products` |
| `page` | Информация, Участие, Помощь | слаг страницы |
| `file` | Оформление сайта, Фотографии, Видео и аудио | расширение имени |

Категории страниц названы **как меню подвала** (`Информация`/`Участие`/
`Помощь`, см. `SeedDefaultMenuCommand`) — навигация и таксономия сайта
совпадают, а не расходятся.

Что важно знать при правках:

- **Раньше набор дублировался.** Категории заводились прямо в
  `InstallController::installDemoData()` (Новости/Товары/Услуги), и два
  определения уже разошлись: новости шаблонов `magazine`, `clinic` и `gaming`
  не получали категорию вовсе, все десять страниц и вся медиатека — тоже, а
  заведённая там же «Услуги» не доставалась ни одной странице. Теперь набор
  описан в команде, а `installDemoData()` зовёт `Cats::ensure()` и получает
  карту «слаг → id».
- **Выбранное вручную не перетирается.** Без `--reset` трогаются только
  материалы, у которых категории нет вовсе. У файла категория одна, поэтому
  она не перезаписывается даже с `--reset`.
- **Пустое при этом дозаполняется.** «Новости» и «Товары» существуют с первых
  установок без иконки и порядка — они их получают, а название владельца
  остаётся как есть.
- **Шаблон новости — три состояния одной группы:** `default`, пустая строка и
  NULL. Все три обязаны попадать в «Новости» (та же ловушка, что в постраничном
  выводе ленты).
- **Иконка — эмодзи.** В форме категории так и написано («Эмодзи / HTML»), и
  значение выводится через `{!! !!}`: эмодзи не зависит от подключённого набора
  иконок и не ломается при смене темы.

Тесты: `tests/Feature/DefaultCategoriesTest.php` (6).

---

## ✍️ Свой редактор содержимого (RU Editor)

Заменил TinyMCE 8 целиком (сессия 04.08.2026). Причина — лицензия: TinyMCE 8
распространяется под GPLv2+ либо по коммерческому договору, а CMS продаётся
закрытой. Заодно ушло 5,3 МБ чужой сборки и баннер «Get all features».

**Файлы** (167 КБ вместе, без сборщика — обычные скрипты в `public/`):

| Файл | Что внутри |
|---|---|
| `public/assets/js/ru-editor.js` | ядро: рамка, выделение, история, чистка разметки, реестры |
| `public/assets/js/ru-editor-ui.js` | диалоги, вкладки, подтверждения |
| `public/assets/js/ru-editor-format.js` | начертания, блоки, шрифт, цвет, списки, ссылки |
| `public/assets/js/ru-editor-media.js` | картинки, видео, медиатека, загрузка |
| `public/assets/js/ru-editor-blocks.js` | таблицы, блоки `pc-*`, спецсимволы, каптча |
| `public/assets/js/ru-editor-tools.js` | развернуть на высоту окна, исходник, предпросмотр, поиск, черновик, клавиши |
| `public/assets/css/ru-editor.css` | оформление обвязки (не содержимого) |
| `resources/views/components/ru-editor.blade.php` | единственная точка подключения |

**Подключение:** `<x-ru-editor name="content" :value="$news->content" :height="520" />`.
Параметры: `preset` (`full`/`page`/`simple`/`mail`), `toolbar` (свой набор кнопок),
`placeholder`, `bodyClass`, `contentCss`, `height`, `id`.

### Что надо знать до правки

- **Рамка редактирования — IFRAME, и это не прихоть.** Оформление содержимого
  (`content-blocks.css`) написано под страницу сайта, а в панели вокруг лежат
  Tailwind-сброс, `body.admin-sharp` с `border-radius:0 !important` на всё
  подряд и стили раздела. В общем документе они протекают внутрь текста, и
  автор видит не то, что увидит посетитель. Не переводить на `div`.
- **История отмены своя.** Браузерная знает только про набор текста и
  `execCommand`; операции над таблицами и блоками написаны на Range и в неё не
  попадают. Снимок хранит разметку и путь до курсора списком индексов —
  ссылки на узлы после восстановления бессмысленны.
- **Всё уходящее в базу чистится по белому списку** тегов и атрибутов
  (`cleanOutput`). Содержимое выводится на сайт через `{!! !!}`, поэтому любой
  `on*`-атрибут оттуда — чужой код на странице посетителя. Возврат из режима
  исходного кода идёт через ту же чистку.
- **Начертания нормализуются** `b→strong`, `i→em`, `strike→s`: браузер по
  `execCommand` даёт одни теги, в сохранённых материалах лежат другие.
- **Шорткоды двусторонние.** В базе — текст `[captcha preset="x"]`, в
  редакторе — плашка. Превращение в ядре (`_chipify` / `cleanOutput`). Список
  распознаваемых имён — `RuEditor.shortcodes`, перечислен явно, чтобы обычный
  текст в скобках («[см. приложение 2]») не стал плашкой.
- **Медиатека.** `admin.files.browse` (JSON) завели специально: у модуля Файлы
  не было ни одного эндпоинта со списком. Маршрут стоит ДО `/{file}`, иначе
  слово `browse` уедет в привязку модели. Поиск — `ilike` на PostgreSQL и
  `like` на SQLite (тесты), оператор выбирается по драйверу.
- **Загрузка из редактора идёт в модуль Файлы**, а не мимо него: только так
  картинка из материала попадает в медиатеку и её можно выбрать повторно.
- **Полноэкранного перекрытия НЕТ и не надо его возвращать.** Оно было:
  `position:fixed` на весь экран, запрет прокрутки, изоляция слоя — и у
  владельца стабильно давало рябь по всему экрану, в том числе с выключенным
  аппаратным ускорением. Воспроизвести не удалось ни разу; три попытки чинить
  (убрать слои с `backdrop-filter`, добавить `contain`/`isolation`, вынести
  узел в `body`) не помогли, а последняя вдобавок стирала содержимое: перенос
  узла с `<iframe>` заставляет браузер перезагрузить фрейм, документ создаётся
  пустым, и ядро продолжает писать в прежний, уже отсоединённый. Вместо
  перекрытия кнопка растягивает рамку на высоту окна ПРЯМО НА СТРАНИЦЕ —
  обычной вёрсткой, без фиксированного позиционирования и слоёв.
  ⚠️ В окне предпросмотра НЕ проверяется и состояние по псевдоклассу:
  переключение `input.checked` меняет `matches(':checked')`, но computed-стили
  правил `input:checked ~ .track` остаются прежними — окно не перерисовывает
  дерево. Тумблер `.admin-toggle` там всегда выглядит так, каким отрисовался
  изначально. Проверять такие вещи только в настоящем браузере.
  ⚠️ Диагностировать перерисовку через окно предпросмотра нельзя:
  `requestAnimationFrame` там не обслуживается вовсе (0 кадров и в рамке, и в
  самой странице, независимо от режима). Замеры геометрии по таймеру при этом
  работают.

### Как добавить своё

Три реестра, встроенные кнопки заведены через них же:

```js
RuEditor.registerCommand('myThing', function (editor, value) { /* меняем документ */ });
RuEditor.registerButton('myThing', { icon: 'fas fa-star', title: '…', command: 'myThing',
                                     active: (ed) => ed.queryState('bold') });
RuEditor.registerPlugin('my-plugin', { init: function (editor) { /* подписки */ } });
```

Снимок для отмены, синхронизацию с полем формы и обновление панели ядро делает
само — команде остаётся только изменить документ. Кнопка попадает на панель,
когда её имя перечислено в `toolbar`.

Тип `menu` даёт выпадающий список (`items` массивом или функцией), `render` —
полностью свою разметку списка (так сделана палитра цветов и спецсимволы).

**Строки интерфейса — в словаре PHP**, `admin.editor.js` (132 ключа на ru и en),
уезжают в браузер через `RuEditor.setStrings()`. В скрипт русский текст не
зашивать: у каждой строки есть запасное значение только на случай отсутствия
ключа.

---

## 🔳 Двухфакторная проверка и свой QR (09.08.2026)

Владелец спросил, работает ли привязка с **Яндекс Ключом** или другим
российским приложением. Работает, и вот почему это можно утверждать:

- `pragmarx/google2fa` — лицензия MIT, автор Antonio Carlos Ribeiro,
  **наружу не ходит вообще**. «Google» — только в названии пакета, внутри
  обычный RFC 6238.
- Строка привязки полностью стандартная:
  `otpauth://totp/RU%20CMS:почта?secret=…&issuer=RU%20CMS&algorithm=SHA1&digits=6&period=30`.
- **Все 5 контрольных векторов RFC 6238 совпали** (59 → 287082,
  1111111109 → 081804, 1111111111 → 050471, 1234567890 → 005924,
  2000000000 → 279037). Значит, коды Яндекс Ключа примутся.

⚠️ **Проверять чужой TOTP надо через `oathTotp($secret, intdiv($t, 30))`.**
У `getCurrentOtp($secret, $t)` второй аргумент **игнорируется**: на всех пяти
векторах она вернула один и тот же `793438`, и «проверка» выглядела бы
пройденной, ничего не проверив.

### Настоящая поломка: QR не был картинкой

В `auth/two-factor-setup.blade.php` в `src` картинки подставлялась САМА
строка `otpauth://…` — то есть содержимое кода вместо изображения. Браузер
показывал пустую рамку, сканировать было нечего, оставался только ручной
ввод ключа. Найдено при проверке совместимости, а не по жалобе.

### `app/Support/QrCode.php` — свой генератор, без зависимостей

Владелец отказался ставить библиотеку (закрытый продукт — чем меньше чужого
кода, тем лучше). Реализовано по ISO/IEC 18004: байтовый режим, уровень
коррекции M, версии 1–10 (до 213 байт — строке привязки хватает с запасом),
Рид–Соломон над GF(256), все восемь масок с выбором по штрафным очкам,
вывод — SVG (не зависит от GD/Imagick, не мылится, вставляется разметкой).

**Три ошибки, каждая из которых делала код нечитаемым, и ни одна не видна
глазами** — неверный QR выглядит ровно как верный:

1. **Биты сведений о формате шли от младшего к старшему.** Стандарт кладёт
   их от СТАРШЕГО: `(8,0)` получает бит 14. Сканер читал чужой номер маски,
   снимал не тот узор — код не читался целиком, хотя данные разложены верно.
2. **Совмещающие узоры на версиях от 7 не рисовались.** Пропуск углов был
   написан как «ячейка уже помечена служебной», а центры вида `(6, N)` стоят
   НА полосе синхронизации, размеченной раньше. Пропускать надо ровно три
   угла по координатам. До версии 6 таких центров нет — поэтому ломались
   только 7, 9, 10 и это выглядело как «беда с блоками данных».
3. Опечатка в комментарии (иероглиф вместо слова) — безвредна, но показывает,
   насколько бесполезен просмотр такого файла глазами.

**Как проверять правильность QR (единственный работающий способ — прочитать
код обратно):**

- `tests/Unit/Support/QrCodeTest.php` разбирает картинку **своим** кодом,
  написанным по стандарту и не использующим внутренности генератора:
  снимает маску, идёт змейкой, **расчередует блоки** и достаёт текст.
  Ровно так поймались обе поломки.
- ⚠️ **Начиная с версии 4 слова разложены не подряд, а по кругу** (первое
  слово каждого блока, потом второе каждого). Читать подряд бессмысленно:
  получится связный мусор, у которого даже длина взята из чужого блока.
  Первая версия разборщика в тесте была именно такой и «нашла» несуществующую
  ошибку на версии 7.
- Внешняя сверка (разово, из песочницы, в проект НЕ добавляется):
  `pip install segno opencv-python-headless pyzbar pillow`, отрисовать
  матрицу и прочитать. Итог сплошного прогона длин 1–213: **zbar читает
  216 из 216**. OpenCV спотыкается на пяти — и на одном из них не читает
  **эталон segno тоже**, так что это слабость его разборщика, а не наша.
- ⚠️ **Побитово с segno код НЕ совпадает, и это нормально.** Segno дописывает
  лишний байт `0x00` перед заполнителями `EC 11`, у нас заполнители идут
  сразу после завершителя (как в стандарте). Данные другие → штрафы другие →
  выбирается другая маска. Сверять надо ЧИТАЕМОСТЬ, а не равенство матриц.
- `BarcodeDetector` в браузерной панели **отсутствует** — проверить кодом на
  странице не выйдет.

Картинка вставляется разметкой (`{!! $qrCodeSvg !!}`), а не ссылкой: не
зависит от политики безопасности страницы и масштабируется. Ширину задаёт
`.au-qr__img` в самой вьюхе.

### 🔴 Вход по коду НЕ РАБОТАЛ ВООБЩЕ — ни у кого (10.08.2026)

Владелец попросил открыть двухфакторный вход обычным пользователям. При
разборе выяснилось, что он не работал и у администратора: **пароля хватало
всем**.

`LoginController@login` звал `$request->authenticate()` (а это `Auth::attempt`,
то есть вход УЖЕ выполнен), после чего при включённой проверке делал редирект
на `two-factor.login`. Но эта страница лежит в группе middleware **`guest`** —
вошедшего с неё сразу уводило на сайт. Код не спрашивался никогда, а
`TwoFactorAuthenticationController::verify()` (он зовёт `Auth::loginUsingId`)
не вызывался вовсе.

Починка: при включённой проверке пароль принимается, но вход НЕ выполняется —
`Auth::logout()`, затем `regenerate()` (защита от фиксации сессии) и только
после этого в сессию кладётся `login.id`.

⚠️ **Такое не ловится ни одним тестом, который пользуется `actingAs()`** —
тот подставляет пользователя мимо контроллера. Проверять только реальным
`POST /login` с `assertGuest()` после него.

### Что ещё чинилось заодно

- **Коды восстановления не принимались НИГДЕ.** Генерировались, показывались,
  хранились (`encrypted:array`) — и всё. Потерянный телефон означал потерю
  доступа навсегда. Теперь `verify()` принимает их как альтернативу шести
  цифрам и **вычёркивает использованный**; на странице ввода есть переключатель
  «Телефон недоступен — ввести код восстановления» (он же меняет `inputmode`
  на `text` и `maxlength` на 16: в поле стояло `maxlength="8"`, а коды
  десятизначные — набрать их было физически нельзя).
- **`str_shuffle` для кодов восстановления** — это Mt19937, предсказуемый по
  состоянию, плюс перемешивание алфавита исключало повторы символов внутри
  кода. Заменено на `random_int` посимвольно.
- **У шага ввода кода не было ограничения попыток** (`throttle`), хотя у
  `/login` оно есть. Шесть цифр без ограничения перебираются.
- **Два контроллера одного входа уводили администратора в разные места:**
  `LoginController` → `admin.dashboard`, `verify()` → `/admin/modules`.
- **`POST /api/v1/auth/login` выдавал постоянный токен по одному паролю**, в
  обход проверки. Пошагового ввода кода в этом API нет, поэтому для таких
  учётных записей вход по паролю там закрыт (403).
- `modules/Users/Controllers/Frontend/AuthController` — свой `Auth::attempt`
  без всякой проверки, но маршрутов у него НЕТ (проверено `route:list`).
  Мёртвый код; если его когда-нибудь подключат, обход вернётся.

### Ссылка на шаг с кодом на форме входа

⚠️ **Заход на `/two-factor/login` без начатого входа отдавал 500.** У `show()`
тип возврата объявлен `View`, а ветка «нет `login.id`» возвращает редирект —
`TypeError`. Стало `View|RedirectResponse` + пояснение на форме входа.

На форме есть ссылка «Вход по коду из приложения»; при незавершённом входе
(в сессии есть `login.id`) она превращается в «Продолжить вход» — иначе
вернувшийся на `/login` набирал бы пароль заново.

⚠️ **Код — ВТОРОЙ рубеж и заменой паролю быть не может.** Ссылка ведёт на тот
же шаг проверки, а не в обход формы. Сканировать на этом шаге нечего:
приложение-аутентификатор показывает шесть цифр и ничего не отдаёт наружу.
Вход сканированием — это другая механика (подтверждение с устройства, где
вход уже выполнен), со своим хранилищем разовых меток и опросом.

⚠️ `.au-foot a` в лейауте перебивает одиночный класс по специфичности —
ссылка выходила индиговой с контрастом 4.27. Нужен селектор с родителем.

Тесты: `tests/Feature/TwoFactorLoginTest.php` (12) — бьют по настоящему
`POST /login`, а не через `actingAs`.

## 🖼️ Слайдшоу: перекомпоновка правки и создания (10.08.2026)

Владелец попросил переделать создание и редактирование, предупредив, что
модуль «многострадальный» — ломать нельзя. Механику не трогали вовсе:
имена полей, адреса форм, идентификаторы для JS (`dropbox`, `media`,
`browseBtn`, `fileName`, `previewBox`, `previewIcon`, `sortable-slides`,
`save-order`, `editModal` и его поля) и класс `.cursor-move`, за который
цепляется Sortable, остались как были. После правки проверено пофамильно:
все 18 полей на месте, оба слайда рисуются, полный прогон 659/659.

**Что переделано:**

- **Эмодзи в подписях** (🏷️ 📍 ⏱️ 🎬 📏 📝 🖼️ 🔍 🔗 🎨 🔢 ✅ 🔘 ⬅️➡️) —
  заменены значками Font Awesome. В остальной панели значки рисует набор
  темы, и раздел выбивался из ряда. На всех трёх экранах эмодзи теперь ноль.
- **Галочки → `.admin-toggle`** (общий тумблер, как в «Меню») и вынесены в
  отдельную полосу: раньше три галочки стояли ячейкой сетки среди полей и
  ряд разъезжался.
- **Порядок блоков.** Страница открывалась формой добавления из восьми
  полей, а сами слайды лежали под ней. Теперь список сверху, форма
  разворачивается кнопкой (и раскрыта сразу, если слайдов ещё нет).
  Высота страницы упала с ~2600px до 1290px при 1600px ширины.
- **Пример из подписи в подсказку:** «Высота (CSS, например: 500px или
  50vh)» делала подпись вдвое длиннее соседних.
- Сообщения из `alert()` («✅ Порядок слайдов сохранён!») переехали в
  словарь: секция `admin.slideshow` выросла на 12 ключей.

⚠️ **В разметке были классы, которых в сборке нет** и которые молча не
работали: `bg-gray-50/60`, `dark:bg-gray-800/60` (прозрачность через дробь),
`max-h-[90vh]` (произвольное значение), `ring-2 ring-blue-400` для подсветки
зоны перетаскивания. Всё переписано литеральным CSS (`.sl-*`).

⚠️ **Проверить тумблер в панели предпросмотра нельзя** — она не
перевычисляет `input:checked ~ .track` после программной смены `checked`
(уже записано выше). Доказательство брать из серверного состояния: у
включённых чекбоксов трек сразу отрисован цветом темы, а не серым.

### ⚠️ Флаг без ключа — не защита (10.08.2026)

Владелец сообщил, что код у него не спрашивают. Разбор: в его записи
`two_factor_enabled = true` и восемь кодов восстановления, но
`two_factor_secret = NULL` — **ключ обнулил я сам**, повторно вызывая
`$u->two_factor_secret = null; $u->saveQuietly();`, чтобы заставить
страницу привязки нарисоваться. Так делать нельзя: это боевая учётная
запись, и с обнулённым ключом она перестала быть защищённой.

`hasTwoFactorEnabled()` = `флаг && ключ`, поэтому вход честно пускал по
паролю. А вот интерфейс врал: кабинет, настройки аккаунта и сводка
«Безопасность» читали ГОЛЫЙ `two_factor_enabled` и писали «Включена».
**Везде теперь спрашивается действующее состояние** (в запросе —
`->whereNotNull('two_factor_secret')`, чтобы не тащить всех в память).

Запись владельца приведена в честный вид (проверка выключена, коды
очищены) — привязку нужно пройти заново.

### Брошенный шаг проверки жил вечно

`login.id` оставался в сессии, пока та не истечёт (по умолчанию 120 минут),
и на форме входа висела действующая ссылка «продолжить». Теперь у шага есть
отметка `login.at` и срок в 10 минут (`PENDING_MINUTES`), а сам шаг
очищается при обычном входе и при выходе.

⚠️ **Закрытие браузера сеанс не завершает** — `SESSION_EXPIRE_ON_CLOSE`
по умолчанию `false`, а `SESSION_LIFETIME` = 120 минут. Это стандартное
поведение Laravel, а не поломка двухфакторной проверки: если нужно, чтобы
вход спрашивали после каждого закрытия окна, ставится
`SESSION_EXPIRE_ON_CLOSE=true`.

⚠️ **Правило на будущее: не обнулять поля боевой учётной записи ради
рендера страницы.** Нужен экран в другом состоянии — заводи отдельного
пользователя (`User::factory()`), а не правь запись владельца.

### 🎨 Экраны входа переведены в язык «Фрагментов» и «Меню» (10.08.2026)

Владелец попросил сделать вход в стиле этих разделов панели и добавить
интересной типографики. Переведены ВСЕ гостевые экраны: вход, ввод кода,
регистрация, восстановление и сброс пароля, подтверждение пароля, проверка
почты, привязка и состояние двухфакторной проверки.

**Общий язык живёт в `layouts/guest.blade.php`, а не в каждой вьюхе:**

- прямые края одним рубильником — `body.au .au-card, body.au .au-card *
  { border-radius: 0 !important }` (тот же приём, что `body.admin-sharp`
  в панели). Перечислять классы по одному пришлось бы в каждой вьюхе: у
  привязки свои плитки, у ввода кода — свои;
- заголовок `clamp(1.75rem, 5.4vw, 2.15rem)` с трекингом −.035em;
- подписи полей и надзаголовок — моноширинным, .66rem, трекинг .1em,
  капсом. **Второй «шрифт» страницы бесплатный:** системный моноширинный
  стек уже используется в проекте для ключа привязки и кодов, ничего
  дозагружать не нужно (шрифты локальные, внешних запросов нет).

**Надзаголовок** — новая необязательная секция `@yield('eyebrow')` в шапке
карточки. У входа он называет шаг («ШАГ 1 ИЗ 2»), у остальных — раздел.

**Полоса шагов** (`auth/partials/sign-in-flow.blade.php`) — общая для формы
входа и страницы кода. Нумерация здесь не украшение: вход правда состоит из
двух шагов, и второй до сих пор был невидим. Второй шаг становится ССЫЛКОЙ
только когда он начат (в сессии есть `login.id`), а оговорка «если включена
проверка» показывается только пока неизвестно, включена ли она — иначе
полоса врала бы в одну или другую сторону.

**Левая колонка.** Фактура — настоящий QR-код адреса сайта
(`auth_pattern_svg()` в `app/helpers.php`, рисуется своим генератором,
кешируется на сутки), увеличенный и обрезанный кадром. Прежняя ровная сетка
42×42 не значила ничего. Знак — угол QR-кода инлайновым SVG: та же фигура,
что в фактуре и на странице привязки.

⚠️ **Логотип темы в этой колонке НЕ выводится намеренно.** Логотипы тем —
широкие надписи 200×40 под светлую подложку: на цветной заливке их тёмные
буквы почти не видны, а в рамке знака картинка сжималась в полоску 30×6.

**Экраны следуют темам** — проверено переключением: `--au-*` выведены из
`--color-primary/accent/bg` и общего набора поверхностей. Тёмный конец
градиента колонки — `color-mix` с акцентом, а не прибитое число.
⚠️ После проверки тему нужно вернуть **по слагу** и сбросить кеш:
`Theme::apply($старыйОбъект)` на устаревшей модели молча не сработал, и
сайт остался вообще без активной темы (цвета ушли в дефолты).

**Ловушки, на которые наступил:**

- у лейаута есть плотная раскладка `@media (min-width:1024px) and
  (max-height:800px)`, и в ней `.au-title` был прибит к 1.25rem — новый
  крупный заголовок на окне 1280×720 молча не применялся;
- там же `.au-back { margin-bottom }` сдвигал кнопку вверх относительно
  надзаголовка, с которым она теперь стоит в одну строку;
- `.au-foot a` перебивает одиночный класс по специфичности (см. выше);
- **нельзя прятать перенос подзаголовка через `white-space: nowrap` +
  многоточие**: на соседних экранах подзаголовок длиннее, и правило съело
  бы у них конец фразы. В одну строку укладываем ТЕКСТОМ — у входа фраза
  сокращена до «Почта и пароль, с которыми регистрировались».

Тесты по полосе шагов переписаны, а не подогнаны: прежние проверяли
пилюлю «Вход по коду» в подвале формы, которой больше нет. Ключи
`tfa_entry`/`tfa_continue` удалены из обоих словарей — они нигде не
использовались.

### UI страницы привязки и куда она подключена

Страница была «список — картинка — поле». Переработана: шаги с номерами в
кружках, код на белой плитке с подписью, **кнопка копирования** у ключа и у
кодов восстановления (ключ в 16 знаков набирали руками, а ошибка в одном
знаке даёт коды, которые просто не подходят, без объяснения).

⚠️ **`navigator.clipboard` может ОТКАЗАТЬ и на защищённом соединении** (запрет
в настройках браузера) — отказ приходит отклонённым обещанием. Первая версия
вызывала `.then(done)` без второго обработчика, и кнопка молча не делала
ничего: подпись не менялась, буфер оставался пустым. Запасной путь через
скрытое поле нужен обоим случаям — и http, и отказу. Проверять подменой
`navigator.clipboard.writeText` на запись аргумента: **читать буфер из панели
нельзя** (`NotAllowedError`).

Коды восстановления лежат каждый в своём теге, поэтому `textContent`
контейнера склеил бы их в одну строку — при копировании дети собираются
через `
`.

**Куда подключено.** До этой правки на привязку вела РОВНО ОДНА ссылка —
`admin/account/settings`. Обычный покупатель включить проверку не мог никак,
хотя страница работает для любого вошедшего. Добавлено:

| Место | Что показывает |
|---|---|
| `frontend/dashboard/index` — «Безопасность» кабинета | ссылка + плашка «Включена/Выключена» |
| `admin/account/settings` | та же плашка состояния в существующей строке |
| Дашборд → «Безопасность» | проверяемый пункт «Включена у N из M» по `is_admin` |

Признак «кто имеет доступ в панель» берём тот же, что проверяет
`AdminMiddleware` — `is_admin`. Колонки `role` у пользователей НЕТ (роли
живут в пивоте `role_user`), первая версия запроса на неё и опиралась.

⚠️ **`--surface-mute` на `--surface-2` даёт контраст 4.41** — для полужирной
подписи в 0.68rem этого мало. Плашка состояния красится смесью
`color-mix(in srgb, var(--surface-ink) 72%, var(--surface-2))` (6.73) —
следует теме так же, но читается. Тот же расчёт увёл индиго с кнопки
копирования: `#6366f1` на светлой подложке — 3.27, обычный цвет текста — 13,
поэтому акцент оставлен только на значке.

Тесты: `QrCodeTest` (4) и `TwoFactorSetupTest` (3) — по привязке их не было
вовсе.

---

## 🔌 Независимость от внешних служб

Проверено разведкой 05.08.2026 по прямой задаче владельца: «предположим, внешние
зависимости заблокируют — что рухнет».

**Не рухнет ничего.** Всё, что нужно для показа сайта и работы панели, лежит
локально: шрифты (`Inter` в `public/assets/fonts`, провайдер `local` —
умолчание), Alpine, Swiper, Lucide, Font Awesome, Chart.js, Tailwind, Swagger UI
(`validatorUrl: null`, обращения к validator.swagger.io нет). Внешних импортов в
исходниках Vite — ноль. Расписание задач пустое: по таймеру наружу не ходит
ничего.

### Что было и почему это чинили

- **`UpdateService::checkForUpdates()` — единственный вызов, работавший сам.**
  Главная панели ходила на `https://updates.rucms.ru/api/check` (чужой адрес по
  умолчанию, скорее всего несуществующий) с таймаутом 10 с, отправляя
  **лицензионный ключ**, версию CMS, PHP и Laravel. Теперь
  `config('app.update_server_url')` **пуст по умолчанию** — запроса нет вовсе.
  Свой сервер задаётся в `UPDATE_SERVER_URL`.
- **Оповещения об ошибках были прибиты к Telegram**, причём в
  `app/Exceptions/Handler.php` — через `file_get_contents` **без таймаута**.
  При недоступности сервиса каждая необработанная ошибка подвешивала запрос до
  `default_socket_timeout` (до минуты): внешняя служба могла положить сайт.
  Telegram убран целиком. Вместо него `send_alert()` (`app/helpers.php`) шлёт
  POST с JSON на адрес из `services.alerts.webhook` — куда угодно: бот MAX,
  корпоративный чат, своя страница-приёмник. Пусто = молчит.
  ⚠️ MAX намеренно НЕ зашит в код: его бот-API я проверить не мог, а угадывать
  контракт чужой службы нельзя. Общий адрес работает с любой.
- **31 исходящий вызов был без таймаута** (оплата, доставка, SMS, WebPush,
  Elasticsearch, каптча Яндекса, выгрузки SEO). По умолчанию это 30 секунд
  Guzzle: заблокированная служба держала бы страницу полминуты. Проставлены
  10–20 с по характеру вызова.

### Автономный режим

`APP_STANDALONE=true` — один рубильник: **наружу не уходит ничего**, даже если
ключи интеграций прописаны. Заслон стоит на самом HTTP-клиенте
(`AppServiceProvider::guardOutboundRequests()` через
`Http::globalRequestMiddleware`), а не в двадцати сервисах по отдельности —
иначе однажды пропустишь один и не заметишь. Действует и на код, который
напишут после.

Свои адреса остаются доступны: `localhost`, петлевые и частные диапазоны
(10./172.16–31./192.168.), `*.local` и собственный домен из `APP_URL`. Автономный
режим означает «не ходить в интернет», а не «остаться без базы».

Проверять в каждом новом исходящем вызове: `outbound_allowed()`. Хотя заслон
глобальный, явная проверка экономит бессмысленный вызов и делает намерение
видимым в коде.

### Что остаётся внешним по своей природе

Оплата (ЮKassa, Сбербанк, Т-Банк, CloudPayments, Qiwi), доставка (Boxberry,
Почта), SMS (sms.ru, Twilio), Web Push, Elasticsearch, выгрузки SEO (Метрика,
Вебмастер, IndexNow), каптча Яндекса, Яндекс.Метрика и Яндекс.Карты на сайте.
Все — с пустыми ключами по умолчанию, под `try/catch`, с таймаутами. Карты
подключаются по клику-согласию, Метрика за флагом `seo.features.metrica`.

## 🚚 Доставка: карточки, логотипы, своя служба (10.08.2026)

Раздел приведён к виду «Оплаты»: карточки в фирменных цветах служб с
настоящими знаками, сводка над списком, форма в две колонки.

- **Логотипы** — `public/images/delivery/<код>.png` (Почта России, СДЭК,
  Boxberry, Яндекс Доставка), обработаны из присланных владельцем
  исходников. Знаки приведены к КВАДРАТУ СО СВОИМ ФОНОМ 256×256, плитка
  показывает картинку целиком (`object-fit: cover`). Иначе никак: у
  Почты и Boxberry знак БЕЛЫЙ на фирменной подложке — на белой плитке
  его не видно вовсе. У Яндекса взят только круглый знак «Я»: широкая
  надпись в плитке 2.5rem нечитаема. Подробности — в
  `public/images/delivery/README.txt`.
- **Цвета и значки** — `DeliveryMethod::BRANDS` (по КОДУ службы) и
  `TYPE_BRANDS` (запасной набор по типу, для своих служб владельца).
  Цвета сняты с самих знаков, а не подобраны на глаз. `brand()` отдаёт
  цвет, значок, `readable_ink()` и логотип; `logoUrl()` ищет по коду,
  потом по типу, добавляет `?v=filemtime`.
- **Своя служба.** В выборе есть седьмая карточка «Другая служба»
  (`__custom`): открывает поля кода и типа и показывает ВСЕ шесть полей
  ключей, включая OAuth token. Переключатели служб теперь БЕЗ `name` —
  это только выбор в интерфейсе, а на сервер код уходит скрытым полем
  `:value="isCustom ? customCode : code"`. Серверная валидация своё
  разрешала и раньше (`code` — `nullable|string|max:50|unique`), просто
  форма не давала это ввести. Драйвера расчёта у своего кода нет —
  об этом прямо сказано подсказкой, стоимость остаётся фиксированной.
- **Живой баг:** `index()` отдавал `paginate(5)`, а ссылок на страницы
  вьюха не рисовала НИКОГДА — шестая служба («Курьер по городу») просто
  не показывалась и добраться до неё было нечем. Тот же класс, что в
  «Медиатеке»: постраничный вывод есть в контроллере и отсутствует в
  разметке. Заменено на `get()`.
- **Ключи спрашиваются только при включённом API.** Раньше красная
  плашка «нет ключей» висела и у службы с фиксированной ценой.
- Карточка была на 35px выше карточки «Оплаты» (цена и срок стояли
  столбиком) — на той же странице от этого появлялась вертикальная
  прокрутка. Подпись и значение сведены в строку: 271 → 240px.

⚠️ **Цвет ссылки нельзя брать чистым фирменным.** `.pm-docs`/`.dl-docs`
красились в `var(--pm)`: жёлтый Т-Банк (#FFDD2D) на белом — контраст
**1.3**, светло-зелёный СДЭК — **1.9**. Теперь
`color-mix(in srgb, var(--brand) 55%, #111827)` — цвет узнаётся, текст
читается (5.1 у СДЭК, 12.4 у Почты).

## 🔴 Смена статуса заказа: 500 на любом заказе (11.08.2026)

Владелец попробовал сменить статус в карточке заказа и поймал
`QueryException: столбец "old_status" в таблице "orders" не существует`.
Три отдельных бага на одном действии, каждый достаточен, чтобы оно не
работало:

1. **Модель писала несуществующую колонку.** В событии `updating` стояло
   `$order->old_status = $order->getOriginal('status')`. Свойства
   `old_status` у модели нет, поэтому присваивание заводило **атрибут**,
   и Eloquent добавлял его в UPDATE. Смена статуса из панели падала
   ВСЕГДА, для любого заказа. Запоминать ничего и не нужно:
   `syncOriginal()` выполняется уже ПОСЛЕ события `updated`, так что
   внутри него `getOriginal()` ещё отдаёт значение до сохранения, а
   `wasChanged('status')` — факт изменения.
2. **`SendOrderStatusChangedNotifications` тип-хинтил `Order` без
   импорта** — имя резолвилось в `App\Listeners\Order`, и первое же
   событие роняло `TypeError`. Ровно тот же баг уже был у
   `SendOrderCreatedNotifications` (описан в разделе про «Оплату»).
   Проверять при любой правке слушателей: `use` на модель есть?
3. **Набор статусов был описан ЧЕТЫРЕЖДЫ и разошёлся.** Вьюхи
   предлагали `pending/paid/completed/cancelled`, валидация принимала
   `pending/processing/completed/cancelled` — выбранный в списке
   «Оплачен» сохранить было невозможно; в слушателе лежала четвёртая
   копия подписей без «Оплачен» вовсе. Теперь единственный источник —
   `Order::STATUSES` и `Order::statusLabels()`, из них берут и валидация,
   и обе вьюхи, и слушатель, и выгрузка.

⚠️ **`Event::fake()` в `setUp()` глушит и события модели.** Замыкания
`static::updated(...)` регистрируются в том же диспетчере, поэтому под
глобальной заглушкой они не выполняются — тест «через HTTP» этот баг НЕ
воспроизводит. Ловит только выборочная заглушка
(`Event::fake([OrderStatusChanged::class])`) или её отсутствие.
Регресс-тесты проверены на прежнем коде: без починки они падают ровно
той ошибкой, что видел владелец.

## ✅ Сверка «чистая установка против боевого сайта» (11.08.2026)

Владелец собрался ставить CMS заново и хотел получить сайт один в один.
Проверка сделана не глазами: последовательность шагов
`InstallController::finish()` прогнана на пустой базе (SQLite — миграции
драйвер-нейтральные) и сопоставлена с боевой базой по каждой таблице и по
настройкам (что включено, какая тема, сколько чего).

⚠️ **`installDemoData()` выходит сразу, если нет администратора.** В
первой пробе шаг отрапортовал «ок», а новостей не появилось вовсе: админа
создаёт отдельный шаг мастера. Проба обязана заводить его первой, иначе
сравнение врёт.

**Найдено и закрыто четыре пробела:**

| Что | Было после установки | Стало |
|---|---|---|
| SEO-записи | 0 (в боевой 23) | 37 — все новости и страницы |
| Спецвозможности | строки нет, виджет выключен | включён, набор режимов как у владельца |
| Модуль «Каптча» | выключен (`module.json`) | активен |
| Оплата и доставка | ВСЕ выключены — заказ оформить нечем | включены те, кому не нужны ключи |

- **SEO-записи заводятся только при сохранении ЧЕРЕЗ МОДЕЛЬ.** Мастер
  вставляет демо-новости запросом (`DB::table()->insert`) — быстро и в
  одной транзакции, — поэтому события синхронизации не срабатывают.
  Добавлена команда `seo:seed-pages` (`SeedSeoPagesCommand`), шаг мастера
  идёт ПОСЛЕ всего содержимого: раньше описывать было бы нечего.
- **`AccessibilitySetting::settings()` создаёт строку лениво и
  выключённой.** Кнопки спецвозможностей на свежем сайте не было вовсе.
  Команда `accessibility:seed-default`.
- **Способы оплаты и доставки.** Правило «создаются выключенными и без
  ключей» остаётся для всего, чему нужны реквизиты. Но наличные, перевод,
  Почта, СДЭК и курьер работают с фиксированной ценой — они включены
  сразу, иначе корзина на свежем сайте нерабочая. Списки —
  `ENABLED_BY_DEFAULT` в обоих сидерах.

**Намеренно НЕ в сидере:** медиатека (27 личных загрузок владельца —
mp3/mp4/xlsx со случайными именами; три демо-заглушки сидируются),
заказы, сообщения, журналы (`activity_logs`, `content_views`,
`login_history`, `captcha_stats`), уведомления панели и ревизии — это
runtime-данные, а не продукт.

⚠️ **Тесты закрепляли прежний контракт «все способы выключены».** Пять
падений — переписаны под новый: проверяется, что включены ровно
`ENABLED_BY_DEFAULT` и что НИ ОДНА система, которой нужны реквизиты, не
пришла включённой. Полнота установки закреплена
`FreshInstallRenderTest::test_install_leaves_the_site_usable`.

## 🧾 Заказы: кабинет покупателя и карточка в панели (11.08.2026)

Оба списка заказов приведены к языку «Оплаты», «Доставки» и корзины:
способ оплаты и доставки показываются со ЗНАКОМ системы (тот же
`brand()`, что и везде), подписи — моноширинным капсом, суммы —
табличными цифрами.

**Кабинет (`/dashboard/orders`)** — таблица на семь колонок с
горизонтальной прокруткой на телефоне заменена карточками: номер, дата и
статус в шапке, ниже четыре факта (сумма, состав, оплата, доставка).
Шапка страницы — одной строкой (надзаголовок · заголовок · счётчик):
столбиком она занимала 107px ради одного заголовка, стало 45px.

- **Постраничный вывод БЫЛ и раньше**, но общий компонент прячется сам,
  когда страница одна (`hasPages()`), — владелец видел пустоту и решил,
  что его нет. Добавлена своя сводка «Показаны 1–1 из 1» под
  `@unless(hasPages())`. Та же ловушка уже описана в «Медиатеке».
- Состав заказа выводился как `0`, если строк товаров нет вовсе; теперь
  прочерк — ноль читается как «купили ничего».
- Тестов по этой странице не было ни одного: `tests/Feature/CabinetOrdersTest.php` (4).

**Карточка заказа в панели (`/admin/orders/{id}`)** — у заказа без строк
товаров таблица рисовала шапку и одно «Итого» на полстраницы пустоты.
Теперь явное пустое состояние с суммой из самого заказа.

⚠️ **`assertDontSee('#2')` — ловушка.** В ответе полно
шестнадцатеричных цветов (`#2563eb`), и проверка «не вижу заказ №2»
падала на них. Проверять по разметке: `>#2<` или количество карточек.

## 📧 Почта в запросе: недоступный SMTP ронял страницу (11.08.2026)

Сразу после починки статусов смена статуса упала снова — уже на письме:
`FatalError: Maximum execution time of 30 seconds exceeded` в
`SocketStream.php`. У транспорта стояло `'timeout' => null`, то есть
«ждать сколько угодно», а письмо уходило ПРЯМО В ЗАПРОСЕ.

- **`config/mail.php`: `'timeout' => env('MAIL_TIMEOUT', 5)`.** Здоровый
  сервер отвечает заметно быстрее; недоступный теперь срывается за 5
  секунд, а не держит запрос до фатального лимита PHP. Мастер установки
  пишет ключ в `.env` (иначе после чистой установки грабли вернулись бы).
- **`after_response()` (`app/helpers.php`)** — выполнить работу после
  того, как ответ ушёл в браузер (`app()->terminating`). Очередь в
  проекте по умолчанию `sync`, поэтому даже слушатель с `ShouldQueue`
  выполнялся бы в запросе. Оба слушателя заказов (создание и смена
  статуса) шлют письма через него; ошибки гасятся и уходят в лог с
  номером заказа — показать их всё равно уже некому.

⚠️ **`Application::terminate()` НЕ очищает список terminating-колбэков.**
Второй вызов выполняет их повторно — в тесте это выглядело как «письмо
ушло дважды». В обычном запросе terminate зовётся один раз, но при
переезде на Octane это станет реальной повторной отправкой.

⚠️ **`Mail::fake()` не записывает `Mail::send('вьюха', …)`** — фейк
регистрирует только объекты Mailable, остальное молча проглатывает.
Проверять отправку сырых писем надо транспортом `array`:
`config(['mail.default' => 'array']); Mail::mailer('array')->getSymfonyTransport()->messages()`.

**Диагноз по конкретной машине владельца:** порт 465 закрыт (проверено —
`smtp.mail.ru:465` и `smtp.yandex.ru:465` не отвечают, обрыв по таймауту),
а **587 и 25 доступны**. То есть письма не уходили вообще: нужно
`MAIL_PORT=587`, `MAIL_SCHEME=smtp`, `MAIL_ENCRYPTION=tls` (STARTTLS)
вместо `465/smtps/ssl`.

## 🛒 Корзина: выбор способов карточками (10.08.2026)

Способ оплаты и доставки выбирались двумя `<select>`: покупатель видел
одни названия и открывал список ради каждого сравнения. Теперь карточки
с логотипом, названием, пояснением и ценой — знак и фирменный цвет
берутся из `brand()` тех же моделей, что и в панели.

- **Перекомпоновка.** Выбор переехал из боковой колонки в левую: слева
  пустовало полстраницы, а подписи в карточках обрезались на 140px
  ширины. Слева три шага (`01` товары, `02` оплата, `03` доставка), в
  липкой правой колонке — итог, что именно выбрано, и кнопка.
- **Типографика** как на страницах входа: моноширинный надзаголовок
  капсом, заголовок `clamp(1.6rem, 4vw, 2.15rem)` с трекингом −.03em,
  номера шагов плашкой в цвете темы, суммы — `tabular-nums` (иначе
  колонка цифр «пляшет» по разрядам).
- **Подложки обязательны.** У тем есть фоновая картинка: надзаголовок,
  подписи шагов и ссылка «Продолжить покупки» на ней сливались с фоном.
  Каждой дана плашка `--surface` + рамка, обнимающая содержимое
  (`inline-block`/`inline-flex`, не на всю ширину).
- **Контраст.** Чистый `--color-primary` у оранжевых тем даёт 3.67 —
  мелкому тексту мало. Надзаголовок и ссылка красятся
  `color-mix(in srgb, var(--color-primary) 72%, var(--surface-ink))`.
- Имена полей (`payment_method_id`, `delivery_method_id`) и `data-*`
  сохранены — сервер и пересчёт итога видят то же самое.

⚠️ **Тест `test_selects_and_script_reference_the_same_elements` закреплял
именно `<select id="delivery-method">`.** Он документировал реальную
поломку (разметку однажды перевели на радиокнопки, а скрипт продолжал
искать select, падал с TypeError и итог навсегда оставался 0,00).
Разметка изменена ОСОЗНАННО, поэтому тест переписан под новый контракт
(переключатели по `name`, селектор `:checked` в скрипте, `data-price`
/`data-free-from`/`data-commission` в разметке), а не подогнан под
результат — тот же приём, что с `install.php` в транше 15.

## 🌓 `prefers-color-scheme` — это НАСТРОЙКА ОС, а не тема продукта

Владелец сообщил, что на `/admin/orders` сумма заказа не читается. Причина:
`.ord-sum b{ color:#f3f4f6 }` внутри `@media (prefers-color-scheme: dark)`.
У владельца тёмная СИСТЕМА и светлая ПАНЕЛЬ — текст становился почти белым
на белом. Тему панели задают класс `.dark` и переменные `--admin-*`, тему
сайта — `body.fx-theme-dark` и `--surface-*`; настройка ОС к ним отношения
не имеет и только ломает их.

Блок найден в **10 вьюхах**, убран из восьми (заказы ×2, сообщения ×3,
спецвозможности, две страницы ошибок) и из центра уведомлений в шапке —
там он перекрашивал выпадающий список в тёмный посреди светлой шапки.
На месте каждого оставлено пояснение, чтобы не завели снова. Две фронтовые
вьюхи (`Menu::frontend.homepage-pages`, `Payments::public.cart`) оставлены
на отдельный разбор.

Заодно бледные подписи в заказах (`#9ca3af`, `text-gray-400`) выведены из
переменных панели через `color-mix`: контраст 2.4 → 5–6.6.

⚠️ **Панель предпросмотра не прокручивает CSS-переходы.** Она не рисует
кадры (по той же причине не работает `requestAnimationFrame` и не делается
снимок экрана), поэтому свойство с `transition` застревает на НАЧАЛЬНОМ
значении, если класс поставили после загрузки. Так `.dm-type.is-active`
выглядела неприменённой: `border-color` показывал серый, хотя правило
совпадало и переменная резолвилась. Проверять так: снять переход
(`el.style.transition = 'none'`) и замерить снова — значение станет верным.

## 🚧 Не сделано (никто пока не просил)

- **Мажорные апгрейды зависимостей.** Сознательно НЕ трогали, каждый требует отдельного
  прохода: Laravel 12→13, Tailwind 3→4, Vite 6→8, PHPUnit 11→13, laravel/tinker 2→3,
  spatie/laravel-sitemap 7→8. Остальное подтянуто до свежего в рамках текущих мажоров.
- ~~Вьюхи модуля Localization на Bootstrap-разметке~~ — **сделано.** Проверено
  28.07.2026: `index/create/edit/settings` уже на Tailwind + Alpine, модал импорта
  открывается через `x-show`. Запись выше устарела.
- **Выравнивание `validation.php` между языками** (см. выше про 10 ключей).
- **Демо-контент при установке остаётся русским** независимо от выбранного языка
  (`installDemoData()` в `InstallController`): это данные в БД, а не строки интерфейса.
- ~~`resources/views/frontend/pages/footer.blade.php` — осиротевший дубликат~~ —
  **проверено и удалено 04.08.2026.** Грепом по всему проекту: не подключался
  нигде, 156 строк мёртвого кода.
- `routes/auth.php` — стоковый файл Laravel Breeze, **ниоткуда не подключается**
  (`routes/web.php` его не `require`-ит). У приложения свои контроллеры логина/регистрации.
  Безвредный мёртвый код, но если кто-то ищет «почему не работает Breeze-маршрут X» —
  вот почему. Реально нужные маршруты оттуда (`verification.*`, `password.confirm`,
  `password.update`, `profile.*`) перенесены в `routes/web.php` поштучно: подключать файл
  целиком нельзя, его `login`/`register`/`password.*` конфликтуют с кастомными.
- **CDN-независимость** уже сделана в более ранней сессии: локальные веб-шрифты
  (`@fontsource`, latin+cyrillic), локальный swagger-ui, Яндекс.Карты по клику-согласию,
  Метрика за конфиг-флагом, security-заголовки (CSP/HSTS) в
  `app/Http/Middleware/ContentSecurityPolicy.php` и `bootstrap/app.php`.

---

## 🧾 Полезная археология

У двух коммитов длинные содержательные сообщения с разбором первопричин —
прочитай `git show <sha>` прежде чем заново выводить те же выводы:

- `61648cf` и `dcb8329` — путь тестов от «не грузится вообще» → 142 ошибки →
  30 ошибок / 48 падений → 0/0.

---

## 📌 Модуль «Оплата» (сессия 02.08.2026)

**Сделано:** типовые для РФ методы при установке, переработка UI списка и
формы, проверка связи, запуск платежа при оформлении заказа и закрытие
дыры в приёме уведомлений.

### 🔴 Дыра, которую нашли и закрыли — читать перед правкой любого драйвера

Маршрут уведомлений `POST /payment/webhook/{gateway}` **публичный и без
CSRF** — иначе платёжная система до него не достучится. При этом драйверы
верили телу запроса: брали `event`/`status` и `order_id` и переводили заказ
в `completed`. То есть любой мог отправить

```
{"event":"payment.succeeded","object":{"metadata":{"order_id":5}}}
```

и получить оплаченный заказ бесплатно. Так было у ЮKassa, СБП и Сбербанка.

**Правило: телу уведомления верить нельзя никогда.** Подтверждать оплату
можно двумя способами — переспросить статус у API системы по её
идентификатору платежа (ЮKassa, Сбербанк) либо проверить подпись
(Т-Банк, `generateToken`). **Сверка суммы обязательна в обоих случаях:**
подпись подтверждает отправителя, но не сумму — без неё можно заплатить
рубль за заказ на полторы тысячи.

**У СБП обработка уведомлений намеренно отключена:** `getPaymentStatus()`
там заглушка, читающая статус из НАШЕЙ базы, а не у банка — подтвердить
оплату нечем. Пока драйвер не дописан, неоплаченный заказ лучше бесплатного.
Не «чини» это, вернув старое поведение.

### Что было сломано в связке (и почему оплата не работала вовсе)

- `CartController` создавал заказ и вёл на подтверждение — **платёж не
  создавался никогда**. Теперь для онлайн-методов зовётся
  `PaymentGatewayService::createPayment()` и покупатель уходит на оплату.
- Карта драйверов знала коды `tinkoff`/`sberbank`, а сидер заводит методы
  как `tbank`/`sberpay` — метод оставался без драйвера. Оба набора кодов
  теперь поддерживаются.
- `AbstractPaymentGateway::getWebhookUrl()` строил `route('payments.webhook')`,
  а маршрут называется `payment.webhook` — «Route not defined».
- `App\Listeners\SendOrderCreatedNotifications`: тип-хинт `Order` **без
  импорта** резолвился в `App\Listeners\Order`. Любое создание заказа
  роняло слушателя `TypeError`.

### Сидирование методов

`modules/Payments/Console/Commands/SeedDefaultPaymentMethodsCommand`
(`php artisan payments:seed-default [--reset]`), вызывается из
`InstallController::finish()`. Шесть методов: ЮKassa, СБП, SberPay,
Т-Банк, наличные, банковский перевод.

**Методы создаются ВЫКЛЮЧЕННЫМИ и с пустыми `settings`.** Боевых ключей в
коде нет и быть не должно — репозиторий публичный. `credentialFields()` —
единый источник для сидера и формы: какие реквизиты нужны какому драйверу.

⚠️ Тинькофф переименован в **Т-Банк** (2024), эквайринг — T-Bank Acquiring,
документация `developer.tbank.ru/eacq/api`. Старое имя не используем.

### Что НЕ проверено (важно перед боем)

- **Живой приём денег не проверялся** — нужен мерчант-аккаунт. Все тесты
  на `Http::fake()`.
- **Путь webhook через HTTP не проверен end-to-end:** в тестовой среде
  маршруты модуля грузятся иначе (таблица `modules` пуста) и запрос уходит
  на логин, поэтому тесты бьют по драйверу напрямую.
- Событие `OrderCreated` уходит в броадкастер: при
  `BROADCAST_CONNECTION=pusher` без установленного пакета создание заказа
  падает. В тестах события глушатся через `Event::fake()`.
- `createPayment` реально реализован только у ЮKassa. СБП, SberPay и
  Т-Банк **в бою не включать**.
